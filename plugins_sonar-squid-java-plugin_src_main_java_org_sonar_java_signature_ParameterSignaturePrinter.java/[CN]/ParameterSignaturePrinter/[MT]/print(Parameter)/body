{
  StringBuilder signature=new StringBuilder();
  ;
  if (parameter.isArray()) {
    signature.append(ARRAY);
  }
  signature.append(parameter.getJvmJavaType());
  if (parameter.isOject()) {
    signature.append(parameter.getClassName());
    signature.append(';');
  }
  return signature.toString();
}
