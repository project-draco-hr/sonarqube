{
  NewRule newRule=NewRule.createForManualRule("MANUAL_RULE").setHtmlDescription("Some description");
  try {
    creator.create(newRule);
    fail();
  }
 catch (  Exception e) {
    assertThat(e).isInstanceOf(IllegalArgumentException.class).hasMessage("The name is missing");
  }
}
