{
  DbSession session=dbClient.openSession(false);
  try {
    new DepthTraversalTypeAwareCrawler(new FileSourceCrawler(session)).visit(treeRootHolder.getRoot());
  }
  finally {
    MyBatis.closeQuietly(session);
  }
}
