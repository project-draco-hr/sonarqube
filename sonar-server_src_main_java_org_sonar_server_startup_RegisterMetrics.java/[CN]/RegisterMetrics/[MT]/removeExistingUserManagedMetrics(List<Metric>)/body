{
  return newArrayList(Iterables.filter(metrics,new Predicate<Metric>(){
    @Override public boolean apply(    Metric metric){
      return !metric.getUserManaged() || measuresDao.getMetric(metric) == null;
    }
  }
));
}
