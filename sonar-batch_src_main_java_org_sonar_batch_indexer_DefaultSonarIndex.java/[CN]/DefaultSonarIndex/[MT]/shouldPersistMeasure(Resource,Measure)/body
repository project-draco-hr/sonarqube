{
  Metric metric=measure.getMetric();
  return measure.getPersistenceMode().useDatabase() && !(ResourceUtils.isEntity(resource) && metric.isOptimizedBestValue() == Boolean.TRUE && metric.getBestValue() != null && metric.getBestValue().equals(measure.getValue()) && !measure.hasOptionalData());
}
