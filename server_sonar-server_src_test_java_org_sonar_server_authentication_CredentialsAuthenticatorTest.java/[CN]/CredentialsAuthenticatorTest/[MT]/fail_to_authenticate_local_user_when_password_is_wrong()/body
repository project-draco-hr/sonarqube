{
  insertUser(newUserDto().setLogin(LOGIN).setCryptedPassword("Wrong password").setSalt("Wrong salt").setLocal(true));
  expectedException.expect(UnauthorizedException.class);
  executeAuthenticate();
}
