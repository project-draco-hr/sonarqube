{
  NewController controller=context.createController(ENDPOINT).setDescription("Get information on Compute Engine tasks.");
  for (  CeWsAction action : actions) {
    action.define(controller);
  }
  controller.done();
}
