{
  if (s != null) {
    Preconditions.checkArgument(s.length() < Issue.DESCRIPTION_MAX_SIZE,"Description must not be longer than " + Issue.DESCRIPTION_MAX_SIZE + " characters (got "+ s.length()+ ")");
  }
  this.description=s;
  return this;
}
