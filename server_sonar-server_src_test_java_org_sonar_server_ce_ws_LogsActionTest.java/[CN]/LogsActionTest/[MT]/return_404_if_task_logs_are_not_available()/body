{
  globalAdministrator();
  insert("TASK_1",null);
  when(ceLogging.getFile(new LogFileRef(CeTaskTypes.REPORT,"TASK_1",null))).thenReturn(Optional.<File>absent());
  tester.newRequest().setParam("taskId","TASK_1").execute();
}
