{
  BooleanColumnDef def=new BooleanColumnDef.Builder().setColumnName("enabled").setIsNullable(false).build();
  assertThat(def.getName()).isEqualTo("enabled");
  assertThat(def.isNullable()).isFalse();
}
