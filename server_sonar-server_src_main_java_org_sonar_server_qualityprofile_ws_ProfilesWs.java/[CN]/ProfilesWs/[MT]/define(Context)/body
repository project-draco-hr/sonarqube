{
  NewController controller=context.createController("api/profiles").setDescription("Former quality profiles web service");
  defineBackupAction(controller);
  defineRestoreAction(controller);
  controller.done();
}
