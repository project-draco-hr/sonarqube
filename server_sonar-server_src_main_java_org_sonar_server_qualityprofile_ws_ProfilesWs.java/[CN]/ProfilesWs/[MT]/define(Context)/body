{
  NewController controller=context.createController(API_ENDPOINT).setDescription("Old Quality Profiles. Deprecated since 5.2").setSince("4.4");
  restoreAction.define(controller);
  defineListAction(controller);
  defineIndexAction(controller);
  controller.done();
}
