{
  NewController controller=context.createController(API_ENDPOINT).setDescription("Old Quality Profiles").setSince("4.4");
  restoreAction.define(controller);
  defineListAction(controller);
  controller.done();
}
