{
  try {
    this.deleteDocument(this.getKeyValue(item.getKey()));
  }
 catch (  Exception e) {
    LOG.error("Could not DELETE _id:{} for index {}: {}",this.getKeyValue(item.getKey()),this.getIndexName(),e.getMessage());
  }
}
