{
  try {
    UpdateRequest doc=normalizer.normalize(item);
    String keyValue=getKeyValue(item.getKey());
    this.updateDocument(doc,keyValue);
  }
 catch (  Exception e) {
    LOG.error("Could not update document for index {}: {}",this.getIndexName(),e.getMessage());
  }
}
