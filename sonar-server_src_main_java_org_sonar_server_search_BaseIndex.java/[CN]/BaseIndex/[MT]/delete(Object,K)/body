{
  if (this.normalizer.canNormalize(obj.getClass(),key.getClass())) {
  }
 else {
    throw new IllegalStateException("Index " + this.getIndexName() + " cannot execute DELETE for class: "+ obj.getClass());
  }
}
