{
  LOG.info("Update document with key {}",key);
  IndexResponse result=getClient().index(new IndexRequest().type(this.getType()).index(this.getIndexName()).id(this.getKeyValue(key)).source(this.normalize(key))).actionGet();
}
