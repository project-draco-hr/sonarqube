{
  try {
    this.deleteDocument(key);
    for (    KEY additionalKey : keys) {
      this.deleteDocument(additionalKey);
    }
  }
 catch (  Exception e) {
    LOG.error("Could not DELETE _id = '{}' for index '{}': {}",this.getKeyValue(key),this.getIndexName(),e.getMessage());
  }
}
