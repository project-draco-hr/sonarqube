{
  GetResponse response=getClient().prepareGet().setType(this.getIndexType()).setIndex(this.getIndexName()).setId(this.getKeyValue(key)).setRouting(this.getKeyValue(key)).get();
  if (response.isExists()) {
    return toDoc(response.getSource(),QueryOptions.DEFAULT);
  }
  return null;
}
