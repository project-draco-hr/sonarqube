{
  org.sonar.api.measures.Metric<G> m=metricFinder.findByKey(metric.key());
  Measure<G> measure=sensorContext.getMeasure(m);
  if (measure == null) {
    return null;
  }
  return AnalyzerMeasure.<G>builder().onProject().forMetric(metric).withValue(measure.value()).build();
}
