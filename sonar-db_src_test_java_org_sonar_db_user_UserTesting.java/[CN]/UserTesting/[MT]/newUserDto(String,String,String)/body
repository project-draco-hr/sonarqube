{
  return new UserDto().setActive(true).setName(name).setEmail(email).setLogin(login).setExternalIdentity(login).setExternalIdentityProvider("sonarqube").setCreatedAt(nextLong()).setUpdatedAt(nextLong());
}
