{
  Container childModule=new TaskBootstrapContainer(taskCommand,reactor);
  try {
    installChild(childModule);
    childModule.start();
  }
  finally {
    childModule.stop();
    uninstallChild();
  }
}
