{
  return getSession().createQuery("FROM " + Rule.class.getSimpleName() + " r WHERE r.pluginName=:pluginName and r.status<>:status").setParameter("pluginName",repositoryKey).setParameter("status",Rule.STATUS_REMOVED).getResultList();
}
