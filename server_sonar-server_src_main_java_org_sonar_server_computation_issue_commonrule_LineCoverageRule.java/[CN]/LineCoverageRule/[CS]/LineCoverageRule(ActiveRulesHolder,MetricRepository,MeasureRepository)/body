{
  super(activeRulesHolder,CommonRuleKeys.INSUFFICIENT_LINE_COVERAGE,measureRepository,metricRepository.getByKey(CoreMetrics.LINE_COVERAGE_KEY),metricRepository.getByKey(CoreMetrics.UNCOVERED_LINES_KEY),metricRepository.getByKey(CoreMetrics.LINES_TO_COVER_KEY));
}
