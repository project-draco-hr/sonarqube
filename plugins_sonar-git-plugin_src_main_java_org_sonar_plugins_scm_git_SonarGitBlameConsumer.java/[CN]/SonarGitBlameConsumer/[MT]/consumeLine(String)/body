{
  if (line == null) {
    return;
  }
  if (expectRevisionLine) {
    consumeRevisionLine(line);
  }
 else {
    if (extractCommitInfoFromLine(line)) {
      return;
    }
    if (line.startsWith("\t")) {
      consumeContentLine();
    }
  }
}
