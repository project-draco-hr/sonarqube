{
  return new DumbComponent(ComponentImpl.convertType(component.getType()),component.getRef(),build_options.uuid ? uuidOf(component.getRef()) : null,build_options.key ? keyOf(component.getRef()) : null,buildChildren(component,build_options));
}
