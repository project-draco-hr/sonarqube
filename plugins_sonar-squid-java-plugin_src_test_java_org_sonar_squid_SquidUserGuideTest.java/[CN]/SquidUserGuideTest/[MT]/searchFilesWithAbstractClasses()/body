{
  Collection<SourceCode> files=squid.search(new QueryByType(SourceFile.class),new QueryByMeasure(Metric.ABSTRACT_CLASSES,Operator.GREATER_THAN,0));
  assertEquals(32,files.size());
  assertTrue(files.contains(new SourceFile("org/apache/commons/collections/keyvalue/AbstractMapEntry.java")));
}
