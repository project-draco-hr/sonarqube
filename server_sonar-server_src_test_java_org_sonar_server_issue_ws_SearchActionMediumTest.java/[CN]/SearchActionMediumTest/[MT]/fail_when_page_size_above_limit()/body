{
  expectedException.expect(IllegalArgumentException.class);
  WsTester.TestRequest request=wsTester.newGetRequest(IssuesWs.API_ENDPOINT,SearchAction.SEARCH_ACTION).setParam(WebService.Param.PAGE_SIZE,String.valueOf(MAX_LIMIT + 10));
  request.execute();
}
