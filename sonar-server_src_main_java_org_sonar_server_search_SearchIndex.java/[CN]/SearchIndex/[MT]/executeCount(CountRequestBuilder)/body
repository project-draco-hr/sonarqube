{
  StopWatch watch=createWatch();
  long count=0;
  try {
    count=builder.execute().actionGet().getCount();
  }
  finally {
    CountRequest request=builder.request();
    watch.stop("Counted %d documents on ind(ex|ices) '%s'",count,Arrays.toString(request.indices()));
  }
  return count;
}
