{
  assertThat(chunk("0b10"),isNumericLiteral());
  assertThat(chunk("0b10l"),isNumericLiteral());
  assertThat(chunk("0b10L"),isNumericLiteral());
  assertThat(chunk("0B10"),isNumericLiteral());
  assertThat(chunk("0B10l"),isNumericLiteral());
  assertThat(chunk("0B10L"),isNumericLiteral());
}
