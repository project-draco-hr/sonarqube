{
  assertThat(chunk("0xFF"),isNumericLiteral());
  assertThat(chunk("0xFFl"),isNumericLiteral());
  assertThat(chunk("0xFFL"),isNumericLiteral());
  assertThat(chunk("0XFF"),isNumericLiteral());
  assertThat(chunk("0XFFl"),isNumericLiteral());
  assertThat(chunk("0XFFL"),isNumericLiteral());
}
