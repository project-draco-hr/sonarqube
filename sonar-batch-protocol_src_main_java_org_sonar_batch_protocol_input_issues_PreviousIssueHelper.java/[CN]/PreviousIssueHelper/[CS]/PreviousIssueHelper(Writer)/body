{
  try {
    this.writer=new JsonWriter(out);
    writer.setIndent("  ");
    writer.beginArray();
  }
 catch (  IOException e) {
    throw new IllegalStateException("Unable to open writer",e);
  }
}
