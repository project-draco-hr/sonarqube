{
  try {
    if (jsonreader.hasNext()) {
      return true;
    }
    jsonreader.endArray();
    jsonreader.close();
    return false;
  }
 catch (  IOException e) {
    throw new IllegalStateException("Unable to iterate over JSON file ",e);
  }
}
