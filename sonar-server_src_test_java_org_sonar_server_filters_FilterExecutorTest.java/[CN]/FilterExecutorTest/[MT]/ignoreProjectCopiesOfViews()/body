{
  setupData("views");
  FilterExecutor executor=new FilterExecutor(getSession(),new Derby());
  Filter filter=new Filter().setQualifiers(Sets.newHashSet(Qualifiers.PROJECT));
  FilterResult result=executor.execute(filter);
  assertSnapshotIds(result,1);
}
