{
  setupData("views");
  FilterExecutor executor=new FilterExecutor(getSession());
  Filter filter=new Filter().setPath(2,2,"",true).setQualifiers(Sets.newHashSet(Resource.QUALIFIER_SUBVIEW,Resource.QUALIFIER_PROJECT));
  FilterResult result=executor.execute(filter);
  assertSnapshotIds(result,3,4);
}
