{
  setupData("shared","measures");
  FilterExecutor executor=new FilterExecutor(getSession());
  Filter filter=new Filter().setQualifiers(Sets.newHashSet(Resource.QUALIFIER_CLASS)).addMeasureCriterion(new MeasureCriterion(2,">",5.0)).addMeasureCriterion(new MeasureCriterion(1,">",5.0)).setSortedMetricId(2);
  FilterResult result=executor.execute(filter);
  assertSnapshotIds(result,6,5);
}
