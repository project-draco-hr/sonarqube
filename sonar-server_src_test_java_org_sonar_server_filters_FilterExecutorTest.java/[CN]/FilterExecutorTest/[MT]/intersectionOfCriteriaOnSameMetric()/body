{
  setupData("shared","measures");
  FilterExecutor executor=new FilterExecutor(getSession(),new Derby());
  Filter filter=new Filter().setQualifiers(Sets.newHashSet(Qualifiers.CLASS)).addMeasureCriterion(new MeasureCriterion(1,">",400.0,false)).addMeasureCriterion(new MeasureCriterion(1,"<",600.0,false));
  FilterResult result=executor.execute(filter);
  assertSnapshotIds(result,5);
}
