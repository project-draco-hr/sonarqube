{
  NewController controller=context.createController("api/issue_filters").setSince("4.2").setDescription("Issue Filters management");
  appAction.define(controller);
  showAction.define(controller);
  favoritesAction.define(controller);
  controller.done();
}
