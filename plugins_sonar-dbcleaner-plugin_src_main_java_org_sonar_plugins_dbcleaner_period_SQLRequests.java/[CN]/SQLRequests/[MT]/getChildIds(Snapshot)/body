{
  Query query=session.createQuery("select sp.id FROM " + Snapshot.class.getSimpleName() + " sp WHERE sp.rootId  = :rootId or id = :rootId");
  query.setParameter("rootId",parentSnapshot.getId());
  return query.getResultList();
}
