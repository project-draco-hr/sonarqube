{
  try {
    ClusterHealthStatus status=node.client().admin().cluster().prepareClusterStats().get().getStatus();
    return status != null && status == ClusterHealthStatus.GREEN;
  }
 catch (  Exception e) {
    return false;
  }
}
