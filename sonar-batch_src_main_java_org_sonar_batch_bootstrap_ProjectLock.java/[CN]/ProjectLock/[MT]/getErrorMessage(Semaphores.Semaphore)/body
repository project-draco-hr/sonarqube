{
  long duration=semaphore.getDurationSinceLocked();
  DurationLabel durationLabel=new DurationLabel();
  String durationDisplay=durationLabel.label(duration);
  return "It looks like an analysis of '" + getProject().getName() + "' is already running (started "+ durationDisplay+ "). "+ "If this is not the case, it probably means that previous analysis was interrupted "+ "and you should then force a re-run by using the option '"+ CoreProperties.FORCE_ANALYSIS+ "=true'.";
}
