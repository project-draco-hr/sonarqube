{
  PropertyDefinitions def=new PropertyDefinitions(PropertyDef.builder("inCateg").name("In Categ").category("categ").build(),PropertyDef.builder("noCateg").name("No categ").build());
  assertThat(def.getCategory("inCateg")).isEqualTo("categ");
  assertThat(def.getCategory("noCateg")).isEmpty();
}
