{
  return Iterables.find(requirementsForRule,new Predicate<RequirementDto>(){
    @Override public boolean apply(    @Nullable RequirementDto input){
      return input != null && input.isEnabled();
    }
  }
,null);
}
