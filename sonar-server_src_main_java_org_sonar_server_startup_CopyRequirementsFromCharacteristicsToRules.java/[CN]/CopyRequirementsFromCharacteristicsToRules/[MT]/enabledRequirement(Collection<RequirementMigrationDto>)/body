{
  return Iterables.find(requirementsForRule,new Predicate<RequirementMigrationDto>(){
    @Override public boolean apply(    @Nullable RequirementMigrationDto input){
      return input != null && input.isEnabled();
    }
  }
,null);
}
