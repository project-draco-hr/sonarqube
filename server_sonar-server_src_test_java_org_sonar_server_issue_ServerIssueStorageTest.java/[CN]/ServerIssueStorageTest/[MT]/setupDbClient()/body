{
  System2 system=mock(System2.class);
  when(system.now()).thenReturn(2000000000L);
  dbClient=new DbClient(dbTester.database(),getMyBatis(),new ComponentDao(),new IssueDao(getMyBatis()),new ResourceDao(getMyBatis(),system));
  storage=new ServerIssueStorage(new FakeRuleFinder(),dbClient,mock(IssueIndexer.class));
}
