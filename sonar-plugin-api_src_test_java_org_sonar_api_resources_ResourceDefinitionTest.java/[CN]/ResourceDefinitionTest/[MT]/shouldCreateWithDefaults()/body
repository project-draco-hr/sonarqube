{
  ResourceDefinition def=ResourceDefinition.builder("qualifier").build();
  assertThat(def.getQualifier(),is("qualifier"));
  assertThat(def.getName(),is("qualifier"));
  assertThat(def.getIconPath(),is("/images/q/qualifier.png"));
}
