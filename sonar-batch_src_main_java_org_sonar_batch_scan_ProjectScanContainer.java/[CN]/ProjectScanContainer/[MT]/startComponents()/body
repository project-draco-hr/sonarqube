{
  try {
    return super.startComponents();
  }
 catch (  Exception e) {
    ProjectLock lock=getComponentByType(ProjectLock.class);
    if (lock != null) {
      lock.stop();
    }
    throw e;
  }
}
