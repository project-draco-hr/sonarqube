{
  LOG.info("Truncate es indices");
  Client client=esNode.client();
  client.prepareDeleteByQuery(client.admin().cluster().prepareState().get().getState().getMetaData().concreteAllIndices()).setQuery(QueryBuilders.matchAllQuery()).get();
}
