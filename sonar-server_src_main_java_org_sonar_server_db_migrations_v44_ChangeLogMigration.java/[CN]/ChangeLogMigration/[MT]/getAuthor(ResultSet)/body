{
  try {
    String author=result.getString(USER_LOGIN);
    if (StringUtils.isNotEmpty(author) && !author.equals("null")) {
      return author;
    }
 else {
      return "unknown";
    }
  }
 catch (  Exception e) {
    return "unknown";
  }
}
