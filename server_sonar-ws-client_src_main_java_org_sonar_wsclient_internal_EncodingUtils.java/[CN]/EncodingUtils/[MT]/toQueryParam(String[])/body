{
  StringBuilder sb=new StringBuilder();
  boolean first=true;
  for (  String string : strings) {
    if (!first) {
      sb.append(',');
    }
    sb.append(string);
    first=false;
  }
  return sb.toString();
}
