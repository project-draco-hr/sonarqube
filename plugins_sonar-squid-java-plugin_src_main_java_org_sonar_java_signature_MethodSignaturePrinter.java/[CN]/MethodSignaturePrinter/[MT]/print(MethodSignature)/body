{
  StringBuilder builder=new StringBuilder();
  builder.append(methodSignature.getMethodName());
  builder.append("(");
  for (  Parameter argumentType : methodSignature.getArgumentTypes()) {
    builder.append(ParameterSignaturePrinter.print(argumentType));
  }
  builder.append(")");
  builder.append(ParameterSignaturePrinter.print(methodSignature.getReturnType()));
  return builder.toString();
}
