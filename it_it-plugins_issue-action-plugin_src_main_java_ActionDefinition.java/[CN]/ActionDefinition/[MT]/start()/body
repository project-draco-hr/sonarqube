{
  actions.add("fake").setConditions(new HasResolution(Issue.RESOLUTION_FIXED)).setFunctions(new Function(){
    @Override public void execute(    Context context){
      context.setAttribute("fake","fake action");
      context.addComment("New Comment from fake action");
    }
  }
);
}
