{
  if (measures == null) {
    return 0d;
  }
  double sum=0d;
  for (  Measure measure : measures) {
    if (measure.getData() != null) {
      sum+=Double.parseDouble(measure.getData());
    }
  }
  return sum;
}
