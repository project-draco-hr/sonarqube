{
  if (measures == null) {
    return 0d;
  }
  double sum=0d;
  for (  Measure measure : measures) {
    String data=measure.getData();
    if (data != null) {
      sum+=Double.parseDouble(data);
    }
  }
  return sum;
}
