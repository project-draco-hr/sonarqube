{
  return newArrayList(Iterables.transform(actionPlanDtos,new Function<ActionPlanDto,ActionPlan>(){
    @Override public ActionPlan apply(    @Nullable ActionPlanDto actionPlanDto){
      return actionPlanDto.toActionPlan();
    }
  }
));
}
