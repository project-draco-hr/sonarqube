{
  if (i < 0 || j > _size || i > j) {
    throw new IndexOutOfBoundsException();
  }
 else   if (i == 0 && j == _size) {
    return this;
  }
 else {
    return new CursorableSubList(this,i,j);
  }
}
