{
  SourceCode currentResource=peekSourceCode();
  CheckMessage message=new CheckMessage(this,"The 'continue' branching statement prevent refactoring the source code to reduce the complexity.");
  message.setLine(ast.getLineNo());
  SourceFile sourceFile=currentResource.getParent(SourceFile.class);
  sourceFile.log(message);
}
