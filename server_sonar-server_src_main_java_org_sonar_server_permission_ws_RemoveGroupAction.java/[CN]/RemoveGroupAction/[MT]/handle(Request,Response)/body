{
  String permission=request.mandatoryParam(PARAM_PERMISSION);
  String groupName=request.mandatoryParam(PARAM_GROUP_NAME);
  permissionService.removePermission(new PermissionChange().setPermission(permission).setGroup(groupName));
  response.noContent();
}
