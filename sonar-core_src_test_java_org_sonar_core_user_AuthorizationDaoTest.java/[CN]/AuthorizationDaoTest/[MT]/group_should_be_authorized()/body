{
  setupData("group_should_be_authorized");
  AuthorizationDao authorization=new AuthorizationDao(getMyBatis());
  Set<Long> componentIds=authorization.keepAuthorizedComponentIds(Sets.<Long>newHashSet(PROJECT,PACKAGE,FILE,FILE_IN_OTHER_PROJECT),USER,"user");
  assertThat(componentIds).containsOnly(PROJECT,PACKAGE,FILE);
  componentIds=authorization.keepAuthorizedComponentIds(Sets.<Long>newHashSet(PROJECT,PACKAGE,FILE,FILE_IN_OTHER_PROJECT),USER,"admin");
  assertThat(componentIds).isEmpty();
}
