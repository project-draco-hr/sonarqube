{
  MODEL result=null;
  JSONArray array=(JSONArray)JSONValue.parse(json);
  if (array.size() >= 1) {
    JSONObject elt=(JSONObject)array.get(0);
    if (elt != null) {
      result=parse(elt);
    }
  }
  return result;
}
