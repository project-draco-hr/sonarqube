{
  JavaSquidConfiguration conf=createJavaSquidConfiguration(analyzePropertyAccessors,fieldNamesToExcludeFromLcom4Computation,sourcesCharset);
  squid=new Squid(conf);
  this.messageDispatcher=messageDispatcher;
}
