{
  if (o != null && o instanceof IssueFilterDto) {
    IssueFilterDto otherFilter=(IssueFilterDto)o;
    return referenceFilter.isShared() == otherFilter.isShared() && referenceFilter.getUserLogin() == otherFilter.getUserLogin() && referenceFilter.getDescription() == otherFilter.getDescription() && referenceFilter.getName() == otherFilter.getName() && referenceFilter.getData() == otherFilter.getData();
  }
  return false;
}
