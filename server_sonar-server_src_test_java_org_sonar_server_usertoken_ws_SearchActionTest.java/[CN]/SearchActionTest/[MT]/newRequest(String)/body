{
  TestResponse response=ws.newRequest().setMediaType(MediaTypes.PROTOBUF).setParam(PARAM_LOGIN,login).execute();
  try {
    return SearchWsResponse.parseFrom(response.getInputStream());
  }
 catch (  IOException e) {
    Throwables.propagate(e);
  }
  throw new IllegalStateException("unreachable");
}
