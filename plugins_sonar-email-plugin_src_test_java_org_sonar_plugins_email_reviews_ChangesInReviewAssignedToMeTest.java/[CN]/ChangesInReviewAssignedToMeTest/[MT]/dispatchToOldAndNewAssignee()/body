{
  Notification notification=new Notification("review-assignee-changed").setFieldValue("author","freddy").setFieldValue("old.assignee","godin").setFieldValue("assignee","simon");
  dispatcher.dispatch(notification,context);
  verify(context).addUser("godin");
  verify(context).addUser("simon");
  verifyNoMoreInteractions(context);
}
