{
  json.name("rules").beginArray();
  for (  Rule rule : result.rules()) {
    json.beginObject().prop("key",rule.ruleKey().toString()).prop("name",rule.getName()).prop("desc",rule.getDescription()).prop("status",rule.getStatus()).endObject();
  }
  json.endArray();
}
