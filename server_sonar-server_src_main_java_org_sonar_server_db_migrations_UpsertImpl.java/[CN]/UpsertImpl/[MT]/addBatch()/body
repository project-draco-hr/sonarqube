{
  pstmt.addBatch();
  batchCount++;
  if (batchCount % BatchSession.MAX_BATCH_SIZE == 0L) {
    pstmt.executeBatch();
    pstmt.clearParameters();
    pstmt.getConnection().commit();
  }
  return this;
}
