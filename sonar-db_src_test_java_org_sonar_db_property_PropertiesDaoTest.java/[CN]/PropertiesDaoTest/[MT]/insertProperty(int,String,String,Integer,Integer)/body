{
  dbTester.executeInsert("PROPERTIES","ID",valueOf(id),"prop_key",key,"text_value",value,"resource_id",resourceId == null ? null : valueOf(resourceId),"user_id",userId == null ? null : valueOf(userId));
  dbTester.commit();
}
