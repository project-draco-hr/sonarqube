{
  int size=0;
  if (m_end < m_start) {
    size=maxElements - m_start + m_end;
  }
 else   if (m_end == m_start) {
    size=(m_full ? maxElements : 0);
  }
 else {
    size=m_end - m_start;
  }
  return size;
}
