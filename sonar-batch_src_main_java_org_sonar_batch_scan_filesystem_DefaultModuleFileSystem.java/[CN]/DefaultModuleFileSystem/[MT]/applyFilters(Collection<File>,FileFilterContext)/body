{
  if (!fileFilters.isEmpty()) {
    Iterator<File> it=files.iterator();
    while (it.hasNext()) {
      File file=it.next();
      if (!accept(file,context)) {
        it.remove();
      }
    }
  }
}
