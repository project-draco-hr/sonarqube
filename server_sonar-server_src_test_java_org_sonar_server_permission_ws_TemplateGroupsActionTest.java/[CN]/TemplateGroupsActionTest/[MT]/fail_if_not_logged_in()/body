{
  expectedException.expect(UnauthorizedException.class);
  userSession.anonymous();
  ws.newRequest().setParam(PARAM_PERMISSION,UserRole.USER).setParam(PARAM_TEMPLATE_UUID,template1.getUuid()).execute();
}
