{
  expectedException.expect(UnauthorizedException.class);
  userSession.anonymous();
  ws.newRequest().setParam(PARAM_PERMISSION,UserRole.USER).setParam(PARAM_TEMPLATE_ID,template1.getUuid()).execute();
}
