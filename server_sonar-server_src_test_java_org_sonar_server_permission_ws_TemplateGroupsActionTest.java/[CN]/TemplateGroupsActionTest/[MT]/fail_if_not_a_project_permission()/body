{
  expectedException.expect(BadRequestException.class);
  ws.newRequest().setParam(PARAM_PERMISSION,GlobalPermissions.DASHBOARD_SHARING).setParam(PARAM_TEMPLATE_UUID,template1.getUuid()).execute();
}
