{
  System.out.println(Charsets.UTF_8.name());
  MeasureData data=new MeasureData();
  String s="accents ???? and special characters ?????";
  data.setData(s.getBytes(Charsets.UTF_8));
  assertThat(data.getText()).isEqualTo(s);
}
