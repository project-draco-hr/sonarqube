{
  if (checked) {
    checked=false;
    return false;
  }
  int lastChar=code.lastChar();
  beginOfLine=(lastChar == -1 || lastChar == (int)'\n');
  int peek=code.peek();
  endOfLine=(peek == (int)'\n' || peek == -1);
  checked=beginOfLine || endOfLine;
  return checked;
}
