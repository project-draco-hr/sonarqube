{
  assertThat(toEntries(measureRepository.getAddedRawMeasures(componentRef))).containsOnly(entryOf(NEW_LINES_TO_COVER_KEY,newMeasureBuilder().create(valueLinesToCover)),entryOf(NEW_IT_COVERAGE_KEY,newMeasureBuilder().create(valueItCoverage)));
}
