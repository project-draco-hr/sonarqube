{
  Object out=actions.poll();
  while (out == null) {
    try {
      Thread.sleep(200);
    }
 catch (    InterruptedException e) {
      Log.error("Oops");
    }
    out=actions.poll();
  }
  return out;
}
