{
  Notification notification=new Notification("review-comment-added").setFieldValue("author","simon").setFieldValue("creator","simon");
  dispatcher.dispatch(notification,context);
  verifyNoMoreInteractions(context);
}
