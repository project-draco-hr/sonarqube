{
  Notification notification=new Notification("review-comment-added").setFieldValue("author","godin").setFieldValue("creator","simon");
  dispatcher.dispatch(notification,context);
  verify(context).addUser("simon");
  verifyNoMoreInteractions(context);
}
