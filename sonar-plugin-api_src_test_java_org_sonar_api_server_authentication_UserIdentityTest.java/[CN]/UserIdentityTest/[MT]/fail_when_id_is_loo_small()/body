{
  thrown.expect(IllegalArgumentException.class);
  thrown.expectMessage("User id size is incorrect (Between 3 and 255 characters)");
  UserIdentity.builder().setId("ab").setName("John").setEmail("john@email.com").build();
}
