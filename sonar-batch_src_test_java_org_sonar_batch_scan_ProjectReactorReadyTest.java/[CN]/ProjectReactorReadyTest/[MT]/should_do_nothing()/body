{
  ProjectReactorReady barrier=new ProjectReactorReady(mock(ProjectExclusions.class),mock(ProjectReactor.class),new ProjectBuilder[]{mock(ProjectBuilder.class)},mock(ProjectReactorValidator.class));
  barrier.start();
}
