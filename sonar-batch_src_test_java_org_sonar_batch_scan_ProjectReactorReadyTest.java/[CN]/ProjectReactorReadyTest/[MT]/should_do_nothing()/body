{
  ProjectReactorReady barrier=new ProjectReactorReady(mock(ProjectExclusions.class),mock(ProjectReactor.class),new Settings(),new ProjectBuilder[]{mock(ProjectBuilder.class)});
  barrier.start();
}
