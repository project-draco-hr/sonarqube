{
  AnalysisReportDto report=AnalysisReportDto.newForTests(1L).setStatus(PENDING).setProjectKey(DEFAULT_PROJECT_KEY);
  report.setCreatedAt(DateUtils.parseDate("2014-09-30")).setUpdatedAt(DateUtils.parseDate("2014-09-30"));
  return report;
}
