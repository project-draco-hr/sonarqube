{
  return newArrayList(Iterables.filter(rootCharacteristics,new Predicate<Characteristic>(){
    @Override public boolean apply(    Characteristic input){
      return input.isRoot();
    }
  }
));
}
