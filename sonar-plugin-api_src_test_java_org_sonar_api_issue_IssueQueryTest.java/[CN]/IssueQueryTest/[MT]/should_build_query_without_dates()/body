{
  IssueQuery query=IssueQuery.builder().issueKeys(newArrayList("ABCDE")).build();
  assertThat(query.issueKeys()).containsOnly("ABCDE");
  assertThat(query.createdAfter()).isNull();
  assertThat(query.createdBefore()).isNull();
}
