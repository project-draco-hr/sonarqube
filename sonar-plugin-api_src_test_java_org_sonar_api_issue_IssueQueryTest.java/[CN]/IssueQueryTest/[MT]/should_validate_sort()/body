{
  try {
    IssueQuery.builder().sort("INVALID SORT").build();
    fail();
  }
 catch (  Exception e) {
    assertThat(e).hasMessage("Sort should contain only : [created, updated, closed, assignee]").isInstanceOf(IllegalArgumentException.class);
  }
}
