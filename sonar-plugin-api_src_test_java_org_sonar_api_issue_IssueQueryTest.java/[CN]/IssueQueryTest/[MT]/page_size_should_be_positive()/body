{
  try {
    IssueQuery.builder().pageSize(0).build();
    fail();
  }
 catch (  Exception e) {
    assertThat(e).hasMessage("Page size must be greater than 0 (got 0)").isInstanceOf(IllegalArgumentException.class);
  }
}
