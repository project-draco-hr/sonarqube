{
  try {
    IssueQuery.builder().pageSize(10000).build();
    fail();
  }
 catch (  Exception e) {
    assertThat(e).hasMessage("Page size must be less than 1000 (got 10000)").isInstanceOf(IllegalArgumentException.class);
  }
}
