{
  AuthorMapper authorMapper=session.getMapper(AuthorMapper.class);
  Date now=new Date();
  AuthorDto authorDto=new AuthorDto().setLogin(login).setPersonId(personId).setCreatedAt(now).setUpdatedAt(now);
  authorMapper.insert(authorDto);
}
