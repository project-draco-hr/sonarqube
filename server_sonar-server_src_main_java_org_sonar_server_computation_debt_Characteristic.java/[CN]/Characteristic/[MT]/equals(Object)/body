{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  Characteristic that=(Characteristic)o;
  if (id != that.id) {
    return false;
  }
  return key.equals(that.key);
}
