{
  logger.info("cache: cleaning");
  try {
    lock();
    deleteCacheEntries(createCleanFilter());
  }
 catch (  IOException e) {
    logger.error("Error cleaning cache",e);
  }
 finally {
    unlock();
  }
}
