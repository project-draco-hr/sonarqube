{
  logger.info("cache: clearing");
  try {
    lock();
    deleteCacheEntries(createClearFilter());
  }
 catch (  IOException e) {
    logger.error("Error clearing cache",e);
  }
 finally {
    unlock();
  }
}
