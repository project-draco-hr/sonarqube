{
  BasicBridge bridge=new FakeBasicBridge();
  BasicBridge spy=spy(bridge);
  spy.onProject(null,null);
  spy.onPackage(null,null);
  spy.onFile(null,null);
  verify(spy,times(3)).onResource(null,null);
}
