{
  assertThat(new RuleQuery("java").setActive(true).getUrl(),is("/api/rules?language=java&status=ACTIVE&"));
  assertThat(new RuleQuery("java").setActive(false).getUrl(),is("/api/rules?language=java&status=INACTIVE&"));
}
