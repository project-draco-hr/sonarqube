{
  if (s1 == s2) {
    return 0;
  }
  int h1=s1.hashCode();
  int h2=s2.hashCode();
  if (h1 < h2) {
    return -1;
  }
 else   if (h1 > h2) {
    return 1;
  }
 else {
    return s1.compareTo(s2);
  }
}
