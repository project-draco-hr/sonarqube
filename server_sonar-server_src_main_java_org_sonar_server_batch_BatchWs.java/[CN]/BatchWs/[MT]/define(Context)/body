{
  NewController controller=context.createController("batch").setSince("4.4").setDescription("Get JAR files for batch");
  defineIndexAction(controller);
  defineFileAction(controller);
  controller.done();
}
