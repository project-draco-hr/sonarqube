{
  json.name("sources").beginArray();
  for (int i=0; i < lines.size(); i++) {
    String line=lines.get(i);
    json.beginArray();
    json.value(i + from);
    json.value(line);
    json.endArray();
  }
  json.endArray();
}
