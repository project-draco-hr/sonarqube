{
  try {
    visitImpl(component);
  }
 catch (  RuntimeException e) {
    VisitException.rethrowOrWrap(e,"Visit failed for Component %s:%s%s",component.getType(),component.getKey(),new ComponentPathPrinter<>(stack));
  }
}
