{
  insertMeasure(1,"U1");
  insertMeasure(2,null);
  db.commit();
  underTest.execute();
  assertThat(idsOfRows()).containsOnly(1L);
  underTest.execute();
  assertThat(idsOfRows()).containsOnly(1L);
}
