{
  StringBuilder message=new StringBuilder();
  message.append("ES delete request of doc ").append(request.id()).append(" in index ").append(request.index()).append("/").append(request.type());
  return message.toString();
}
