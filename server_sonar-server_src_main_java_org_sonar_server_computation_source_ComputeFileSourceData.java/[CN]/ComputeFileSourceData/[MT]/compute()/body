{
  Data data=new Data(numberOfLines);
  while (linesIterator.hasNext()) {
    currentLine++;
    read(data,linesIterator.next(),hasNextLine());
  }
  if (hasNextLine()) {
    currentLine++;
    read(data,"",false);
  }
  return data;
}
