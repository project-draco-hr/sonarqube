{
  i*=blockInts;
  j*=blockInts;
  for (int k=0; k < hashInts; k++, i++, j++) {
    if (blockData[i] < blockData[j]) {
      return true;
    }
    if (blockData[i] > blockData[j]) {
      return false;
    }
  }
  return false;
}
