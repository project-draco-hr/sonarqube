{
  JsonWriter jsonWriter=response.newJsonWriter();
  jsonWriter.beginObject().prop("operational",false).prop("state",STATUS_NOT_SUPPORTED).prop("message","Upgrade is not supported on embedded database.").endObject();
  jsonWriter.close();
}
