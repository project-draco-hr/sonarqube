{
  JsonWriter jsonWriter=response.newJsonWriter();
  jsonWriter.beginObject().prop(PROPERTY_STATE,statusToJson(RUNNING)).prop(PROPERTY_MESSAGE,MESSAGE_STATUS_RUNNING).propDateTime(PROPERTY_STARTED_AT,databaseMigration.startedAt()).endObject();
  jsonWriter.close();
}
