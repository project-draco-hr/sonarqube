{
  JsonWriter jsonWriter=response.newJsonWriter();
  jsonWriter.beginObject().prop("operational",false).prop("state",statusToJson(RUNNING)).prop("message",MESSAGE_STATUS_RUNNING).propDateTime("startedAt",databaseMigration.startedAt()).endObject();
  jsonWriter.close();
}
