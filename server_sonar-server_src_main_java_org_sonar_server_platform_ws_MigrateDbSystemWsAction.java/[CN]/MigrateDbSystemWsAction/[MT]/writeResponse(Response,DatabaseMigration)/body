{
  JsonWriter jsonWriter=response.newJsonWriter();
  jsonWriter.beginObject().prop("operational",isOperational(databaseMigration)).prop("state",statusToJson(databaseMigration.status())).prop("message",buildMessage(databaseMigration)).propDateTime("startedAt",databaseMigration.startedAt()).endObject();
  jsonWriter.close();
}
