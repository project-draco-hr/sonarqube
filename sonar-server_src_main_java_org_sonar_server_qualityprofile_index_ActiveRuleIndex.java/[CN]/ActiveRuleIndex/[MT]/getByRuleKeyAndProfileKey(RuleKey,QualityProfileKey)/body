{
  return toDoc(getClient().prepareGet().setRouting(ruleKey.toString()).setType(this.getIndexType()).setIndex(this.getIndexName()).setId(ActiveRuleKey.of(qualityProfileKey,ruleKey).toString()).get().getSource(),QueryOptions.DEFAULT);
}
