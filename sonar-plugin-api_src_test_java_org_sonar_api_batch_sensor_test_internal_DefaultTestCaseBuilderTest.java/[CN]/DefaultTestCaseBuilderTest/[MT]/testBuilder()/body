{
  DefaultTestCaseBuilder builder=new DefaultTestCaseBuilder(parent,"myTest");
  assertThat(builder.durationInMs(1).message("message").stackTrace("stack").status(Status.ERROR).type(Type.UNIT).build()).isEqualTo(new DefaultTestCase(parent,"myTest",1L,Status.ERROR,"message",Type.UNIT,"stack"));
}
