{
  return Iterables.find(components,new Predicate<Component>(){
    @Override public boolean apply(    Component component){
      return issue.componentKey().equals(component.key());
    }
  }
,null);
}
