{
  return builder(reportComponent).addChildren(toArray(buildChildren(reportComponent,latestModuleKey),Component.class)).setKey(componentKey).setUuid(uuidFactory.getOrCreateForKey(componentKey)).build();
}
