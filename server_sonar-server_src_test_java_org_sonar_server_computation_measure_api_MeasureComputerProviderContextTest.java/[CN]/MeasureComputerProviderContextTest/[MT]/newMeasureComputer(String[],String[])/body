{
  return new MeasureComputerImpl.MeasureComputerBuilderImpl().setInputMetrics(inputMetrics).setOutputMetrics(outputMetrics).setImplementation(new MeasureComputer.Implementation(){
    @Override public void compute(    Context ctx){
    }
  }
).build();
}
