{
  userSession.login("john").addProjectUuidPermissions(SCAN_EXECUTION,PROJECT.uuid());
  CeActivityDto activityDto=createActivityDto("TASK_1").setComponentUuid(PROJECT.uuid());
  persist(activityDto);
  ws.newRequest().setMediaType(PROTOBUF).setParam("id","TASK_1").execute();
}
