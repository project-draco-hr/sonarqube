{
  System2 system2=mock(System2.class);
  when(system2.now()).thenReturn(DateUtils.parseDateTime("2003-03-23T01:23:45+0100").getTime());
  RenameIssueWidgets task=new RenameIssueWidgets(new DbClient(dbTester.database(),dbTester.myBatis(),new WidgetDao(system2),new WidgetPropertyDao(system2),new IssueFilterDao(dbTester.myBatis()),new LoadedTemplateDao(dbTester.myBatis())));
  task.start();
  task.stop();
}
