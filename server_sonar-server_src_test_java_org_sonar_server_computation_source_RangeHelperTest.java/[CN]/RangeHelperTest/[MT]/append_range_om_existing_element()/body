{
  StringBuilder element=new StringBuilder("1,2,a");
  RangeHelper.appendRange(element,BatchReport.Range.newBuilder().setStartLine(1).setEndLine(1).setStartOffset(3).setEndOffset(4).build(),1,5);
  assertThat(element.toString()).isEqualTo("1,2,a;3,4,");
}
