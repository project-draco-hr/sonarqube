{
  StringBuilder element=new StringBuilder();
  RangeHelper.appendRange(element,BatchReport.Range.newBuilder().setStartLine(1).setEndLine(3).setStartOffset(2).setEndOffset(3).build(),3,5);
  assertThat(element.toString()).isEqualTo("0,3,");
}
