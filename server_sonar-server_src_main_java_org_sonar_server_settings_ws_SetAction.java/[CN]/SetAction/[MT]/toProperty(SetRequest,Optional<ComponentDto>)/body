{
  PropertyDto property=new PropertyDto().setKey(request.getKey()).setValue(request.getValue());
  if (component.isPresent()) {
    property.setResourceId(component.get().getId());
  }
  return property;
}
