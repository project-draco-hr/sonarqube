{
  return SetRequest.builder().setKey(request.mandatoryParam(PARAM_KEY)).setValue(request.mandatoryParam(PARAM_VALUE)).setComponentId(request.param(PARAM_COMPONENT_ID)).setComponentKey(request.param(PARAM_COMPONENT_KEY)).build();
}
