{
  return SetRequest.builder().setKey(request.mandatoryParam("key")).setValue(request.mandatoryParam("value")).setComponentId(request.param("componentId")).setComponentKey(request.param("componentKey")).build();
}
