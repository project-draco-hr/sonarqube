{
  if (array == null || array.length() == 0) {
    return true;
  }
  if (value != null) {
    for (int index=0; index < array.length(); index++) {
      if (value.equals(array.get(index))) {
        return true;
      }
    }
  }
  return false;
}
