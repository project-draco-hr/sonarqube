{
  if (Qualifiers.PROJECT.equals(resource.getQualifier()) || Qualifiers.VIEW.equals(resource.getQualifier()) || Qualifiers.SUBVIEW.equals(resource.getQualifier())) {
    return resource instanceof Project;
  }
  return false;
}
