{
  File file=temp.newFile();
  inputFile=new DefaultInputFile("module_key","src/Foo.java").setLines(3).setEncoding(Charsets.UTF_8.name()).setFile(file);
  FileUtils.write(file,"one\ntwo\nthree\n");
  output=sut.createForSource(inputFile);
}
