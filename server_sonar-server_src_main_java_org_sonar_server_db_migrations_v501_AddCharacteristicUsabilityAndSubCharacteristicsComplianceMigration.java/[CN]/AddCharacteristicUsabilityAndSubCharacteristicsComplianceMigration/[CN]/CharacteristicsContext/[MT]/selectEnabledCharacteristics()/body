{
  return context.prepareSelect("SELECT c.id, c.kee, c.name, c.characteristic_order, c.parent_id FROM characteristics c WHERE c.enabled=? ORDER BY c.characteristic_order").setBoolean(1,true).list(new CharacteristicReader());
}
