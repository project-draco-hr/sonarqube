{
  ProjectQgateAssociation project=new ProjectQgateAssociationDto().setId(1L).setName("polop").setGateId("10").toQgateAssociation();
  assertThat(project.id()).isEqualTo(1);
  assertThat(project.name()).isEqualTo("polop");
  assertThat(project.isMember()).isTrue();
}
