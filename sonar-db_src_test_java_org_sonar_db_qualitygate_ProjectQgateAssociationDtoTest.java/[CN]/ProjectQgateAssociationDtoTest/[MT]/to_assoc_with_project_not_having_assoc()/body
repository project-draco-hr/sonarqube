{
  ProjectQgateAssociation project=new ProjectQgateAssociationDto().setId(1L).setName("polop").setGateId(null).toQgateAssociation();
  assertThat(project.id()).isEqualTo(1);
  assertThat(project.name()).isEqualTo("polop");
  assertThat(project.isMember()).isFalse();
}
