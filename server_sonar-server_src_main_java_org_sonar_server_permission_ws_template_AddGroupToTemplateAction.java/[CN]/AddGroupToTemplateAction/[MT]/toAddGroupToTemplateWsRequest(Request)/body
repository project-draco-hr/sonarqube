{
  return new AddGroupToTemplateWsRequest().setPermission(request.mandatoryParam(PARAM_PERMISSION)).setGroupId(request.param(PARAM_GROUP_ID)).setGroupName(request.param(PARAM_GROUP_NAME)).setTemplateId(request.param(PARAM_TEMPLATE_UUID)).setTemplateName(request.param(PARAM_TEMPLATE_NAME));
}
