{
  es.client().prepareIndex(INDEX,TYPE).setId(SourceLineIndexDefinition.docKey(fileUuid,line)).setSource(FileUtils.readFileToString(TestUtils.getResource(this.getClass(),projectUuid + "_" + fileUuid+ "_line"+ line+ ".json"))).setRefresh(true).get();
}
