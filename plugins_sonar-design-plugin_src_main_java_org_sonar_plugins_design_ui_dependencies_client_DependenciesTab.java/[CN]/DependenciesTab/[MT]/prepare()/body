{
  if (panel == null) {
    panel=new FlowPanel();
    panel.getElement().setId("deps");
    loading=new Loading();
    dependenciesTable=new DependenciesTable();
    panel.setWidth("100%");
  }
  panel.clear();
  panel.add(loading);
}
