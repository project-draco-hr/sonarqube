{
  thrown.expect(IllegalStateException.class);
  thrown.expectMessage("onFile or onProject can be called only once");
  new DefaultAnalyzerMeasureBuilder<Integer>().onProject().onFile(new DefaultInputFile("src/Foo.php")).withValue(3).build();
}
