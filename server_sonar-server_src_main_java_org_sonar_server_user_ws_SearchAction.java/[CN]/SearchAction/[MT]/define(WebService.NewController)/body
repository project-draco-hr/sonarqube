{
  WebService.NewAction action=controller.createAction("search2").setDescription("Get a list of active users.").setSince("3.6").setHandler(this);
  action.addFieldsParam(FIELDS);
  action.addPagingParams(50);
  action.createParam(Param.TEXT_QUERY).setDescription("Filter on login or name.");
}
