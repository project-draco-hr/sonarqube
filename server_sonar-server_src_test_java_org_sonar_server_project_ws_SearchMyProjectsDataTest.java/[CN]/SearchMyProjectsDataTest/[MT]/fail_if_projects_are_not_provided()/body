{
  expectedException.expect(NullPointerException.class);
  underTest.setProjects(null).setProjectLinks(emptyList()).setSnapshots(emptyList()).setQualityGates(emptyList()).setTotalNbOfProjects(0).build();
}
