{
  expectedException.expect(NullPointerException.class);
  underTest.setProjects(emptyList()).setProjectLinks(emptyList()).setSnapshots(null).setQualityGates(emptyList()).setTotalNbOfProjects(0).build();
}
