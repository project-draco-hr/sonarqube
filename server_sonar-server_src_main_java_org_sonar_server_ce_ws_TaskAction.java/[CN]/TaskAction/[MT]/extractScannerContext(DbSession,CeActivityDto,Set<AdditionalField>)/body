{
  if (additionalFields.contains(AdditionalField.SCANNER_CONTEXT)) {
    return dbClient.scannerContextDao().selectScannerContext(dbSession,activityDto.getAnalysisUuid()).orElse(null);
  }
  return null;
}
