{
  checkState(changeset.hasRevision(),"Changeset must have a revision");
  checkState(changeset.hasDate(),"Changeset must have a date");
  return Changeset.newChangesetBuilder().setRevision(changeset.getRevision()).setAuthor(changeset.hasAuthor() ? changeset.getAuthor() : null).setDate(changeset.getDate()).build();
}
