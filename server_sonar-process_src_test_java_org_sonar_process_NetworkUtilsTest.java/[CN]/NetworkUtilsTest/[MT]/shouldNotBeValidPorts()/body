{
  assertThat(RandomPortFinder.isValidPort(0)).isFalse();
  assertThat(RandomPortFinder.isValidPort(50)).isFalse();
  assertThat(RandomPortFinder.isValidPort(1023)).isFalse();
  assertThat(RandomPortFinder.isValidPort(2049)).isFalse();
  assertThat(RandomPortFinder.isValidPort(4045)).isFalse();
}
