{
  if (isMethod(ast)) {
    return AnnotationUtility.containsAnnotation(ast,OVERRIDE_ANNOTATION_KEYWORD) || AnnotationUtility.containsAnnotation(ast,"java.lang." + OVERRIDE_ANNOTATION_KEYWORD);
  }
  return false;
}
