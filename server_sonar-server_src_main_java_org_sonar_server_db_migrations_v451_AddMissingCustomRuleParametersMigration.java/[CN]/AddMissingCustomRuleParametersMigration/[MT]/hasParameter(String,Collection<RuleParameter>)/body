{
  return Iterables.any(customRuleParams,new Predicate<RuleParameter>(){
    @Override public boolean apply(    @Nullable RuleParameter input){
      return input != null && input.getName().equals(parameter);
    }
  }
);
}
