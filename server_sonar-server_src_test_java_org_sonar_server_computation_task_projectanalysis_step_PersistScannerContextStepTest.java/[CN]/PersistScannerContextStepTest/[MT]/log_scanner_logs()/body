{
  reportReader.setScannerLogs(asList("log1","log2"));
  underTest.execute();
  assertThat(dbClient.scannerContextDao().selectScannerContext(dbTester.getSession(),ANALYSIS_UUID)).contains("log1" + '\n' + "log2");
}
