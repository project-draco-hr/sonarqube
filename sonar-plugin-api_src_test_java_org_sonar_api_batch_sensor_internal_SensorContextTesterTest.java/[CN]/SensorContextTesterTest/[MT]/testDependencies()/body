{
  assertThat(tester.dependencies()).isEmpty();
  tester.newDependency().from(new DefaultInputFile("foo","src/Foo.java")).to(new DefaultInputFile("foo","src/Foo2.java")).weight(3).save();
  assertThat(tester.dependencies()).hasSize(1);
}
