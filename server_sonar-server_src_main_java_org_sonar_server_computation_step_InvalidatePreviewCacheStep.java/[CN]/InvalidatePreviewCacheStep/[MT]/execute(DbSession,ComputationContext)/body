{
  PropertyDto property=new PropertyDto().setKey(PreviewCache.SONAR_PREVIEW_CACHE_LAST_UPDATE_KEY).setResourceId(context.getProject().getId()).setValue(String.valueOf(System.currentTimeMillis()));
  propertiesDao.setProperty(property,session);
}
