{
  WsTester tester=new WsTester(ws,new MetricWebService());
  tester.newGetRequest("api/webservices","list").setParam("includeInternals","true").execute().assertJson(getClass(),"list_including_internals.json");
}
