{
  Violation violation=Violation.create(rule1,javaFile).setPriority(RulePriority.CRITICAL).setLineId(10).setMessage("old message");
  RuleFailureModel model=violationPersister.selectPreviousViolation(violation);
  assertThat(model,notNullValue());
}
