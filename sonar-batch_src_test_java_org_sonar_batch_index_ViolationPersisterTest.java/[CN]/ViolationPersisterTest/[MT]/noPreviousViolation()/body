{
  Violation violation=Violation.create(rule1,javaFile).setPriority(RulePriority.CRITICAL).setLineId(10).setMessage("new message");
  RuleFailureModel model=violationPersister.selectPreviousViolation(violation);
  assertThat(model,nullValue());
}
