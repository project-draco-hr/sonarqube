{
  Settings settings=new Settings();
  settings.setProperty(CoreProperties.HOURS_IN_DAY,8);
  decorator=new IssueTrackingDecorator(issueCache,initialOpenIssues,tracking,lastSnapshots,index,handlers,workflow,updater,mock(Project.class),perspectives,profile,ruleFinder,new WorkDurationFactory(settings));
}
