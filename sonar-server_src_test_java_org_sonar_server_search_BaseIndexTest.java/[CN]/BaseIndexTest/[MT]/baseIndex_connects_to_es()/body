{
  BaseIndex<?> searchIndex=getBaseIndex();
  searchIndex.connect();
  assertThat(node.client().admin().cluster().prepareClusterStats().get().getNodesStats().getCounts().getTotal()).isEqualTo(searchIndex.getNodesStats().getCounts().getTotal());
  searchIndex.stop();
}
