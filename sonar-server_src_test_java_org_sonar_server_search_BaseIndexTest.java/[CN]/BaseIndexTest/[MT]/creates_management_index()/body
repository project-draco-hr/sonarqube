{
  BaseIndex index=getIndex(this.node);
  IndicesExistsResponse indexExistsResponse=index.getClient().admin().indices().prepareExists(IndexDefinition.TEST.getManagementIndex()).execute().actionGet();
  assertThat(indexExistsResponse.isExists()).isTrue();
}
