{
  ComponentDto project=new ComponentDto().setId(1L).setKey("MyProject").setScope(Scopes.DIRECTORY).setQualifier(Qualifiers.DIRECTORY).setProjectId_unit_test_only(1L);
  db.componentDao().insert(session,project);
  session.commit();
  service.delete(project.getKey());
}
