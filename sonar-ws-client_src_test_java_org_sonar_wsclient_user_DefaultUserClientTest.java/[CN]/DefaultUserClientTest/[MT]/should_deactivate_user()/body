{
  httpServer.stubStatusCode(200);
  client.deactivate("daveloper");
  assertThat(httpServer.requestedPath()).isEqualTo("/api/users/deactivate");
  assertThat(httpServer.requestParams()).includes(entry("login","daveloper"));
}
