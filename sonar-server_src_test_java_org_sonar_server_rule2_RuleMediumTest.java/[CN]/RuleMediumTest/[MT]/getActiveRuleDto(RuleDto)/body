{
  return new ActiveRuleDto().setId(1).setNoteCreatedAt(new Date()).setNoteData("Note data, and some more note and here too.").setRuleId(dto.getId()).setProfileId(1).setParentId(0).setSeverity(3);
}
