{
  long now=system.now();
  return new PermissionTemplateDto().setName(name).setDescription(description).setKeyPattern(projectKeyPattern).setKee(Uuids.create()).setCreatedAt(new Date(now)).setUpdatedAt(new Date(now));
}
