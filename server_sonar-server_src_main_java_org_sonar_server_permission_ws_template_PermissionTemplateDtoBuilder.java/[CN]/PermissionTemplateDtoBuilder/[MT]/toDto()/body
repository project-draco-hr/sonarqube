{
  long now=system.now();
  return new PermissionTemplateDto().setName(name).setDescription(description).setKeyPattern(projectKeyPattern).setUuid(Uuids.create()).setCreatedAt(new Date(now)).setUpdatedAt(new Date(now));
}
