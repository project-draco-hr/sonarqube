{
  ModuleFileSystemProvider provider=new ModuleFileSystemProvider();
  ModuleFileSystem fs=provider.provide(newSimpleModule(),new TempDirectories(),new Settings(),mock(InputFileCache.class),mock(FileIndexer.class));
  assertThat(fs.sourceCharset()).isEqualTo(Charset.defaultCharset());
}
