{
  ModuleFileSystemProvider provider=new ModuleFileSystemProvider();
  ModuleFileSystem fs=provider.provide(newSimpleModule(),new PathResolver(),new TempDirectories(),mock(LanguageFilters.class),new Settings(),new FileSystemFilter[0]);
  assertThat(fs.sourceCharset()).isEqualTo(Charset.defaultCharset());
}
