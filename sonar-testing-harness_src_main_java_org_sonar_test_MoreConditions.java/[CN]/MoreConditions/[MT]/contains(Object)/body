{
  return new Condition<Collection<?>>(){
    @Override public boolean matches(    Collection<?> collection){
      for (      Object actual : collection) {
        if (EqualsBuilder.reflectionEquals(expected,actual)) {
          return true;
        }
      }
      return false;
    }
  }
;
}
