{
  try {
    if (connection == null) {
      connection=getDatabaseTester().getConnection().getConnection();
    }
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
  return connection;
}
