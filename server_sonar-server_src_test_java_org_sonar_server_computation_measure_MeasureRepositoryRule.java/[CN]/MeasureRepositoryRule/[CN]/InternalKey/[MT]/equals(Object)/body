{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  InternalKey that=(InternalKey)o;
  return Objects.equals(componentRef,that.componentRef) && Objects.equals(ruleId,that.ruleId) && Objects.equals(characteristicId,that.characteristicId)&& Objects.equals(metricKey,that.metricKey);
}
