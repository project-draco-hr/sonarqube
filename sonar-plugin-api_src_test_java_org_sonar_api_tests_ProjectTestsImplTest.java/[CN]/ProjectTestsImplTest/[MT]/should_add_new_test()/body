{
  org.sonar.api.tests.Test test=new org.sonar.api.tests.Test("test").setStatus("ok").setMessage("message").setStackTrace("stacktrace").setDurationMilliseconds(10);
  projectTests.addTest("key",test);
  assertThat(projectTests.getFileTests()).hasSize(1);
  assertThat(projectTests.getFileTests().get(0).getTests()).hasSize(1);
}
