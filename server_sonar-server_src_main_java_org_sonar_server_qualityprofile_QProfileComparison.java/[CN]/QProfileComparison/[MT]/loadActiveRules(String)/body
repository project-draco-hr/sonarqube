{
  return Maps.uniqueIndex(profileLoader.findActiveRulesByProfile(profileKey),new NonNullInputFunction<ActiveRuleDoc,RuleKey>(){
    @Override protected RuleKey doApply(    ActiveRuleDoc input){
      return input.key().ruleKey();
    }
  }
);
}
