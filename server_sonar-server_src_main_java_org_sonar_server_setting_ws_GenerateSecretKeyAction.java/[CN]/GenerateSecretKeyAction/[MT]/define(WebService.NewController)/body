{
  context.createAction("generate_secret_key").setDescription("Generate a secret key").setSince("6.1").setInternal(true).setResponseExample(getClass().getResource("generate_secret_key-example.json")).setHandler(this);
}
