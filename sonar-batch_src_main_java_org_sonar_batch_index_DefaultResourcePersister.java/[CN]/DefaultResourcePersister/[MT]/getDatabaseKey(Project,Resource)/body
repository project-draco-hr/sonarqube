{
  if (StringUtils.equals(Resource.SCOPE_SET,resource.getScope())) {
    return resource.getKey();
  }
  return new StringBuilder(ResourceModel.KEY_SIZE).append(project.getKey()).append(':').append(resource.getKey()).toString();
}
