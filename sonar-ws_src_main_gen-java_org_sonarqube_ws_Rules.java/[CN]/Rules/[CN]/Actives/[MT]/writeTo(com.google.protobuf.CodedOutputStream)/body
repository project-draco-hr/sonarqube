{
  for (  java.util.Map.Entry<java.lang.String,org.sonarqube.ws.Rules.ActiveList> entry : internalGetActives().getMap().entrySet()) {
    com.google.protobuf.MapEntry<java.lang.String,org.sonarqube.ws.Rules.ActiveList> actives=ActivesDefaultEntryHolder.defaultEntry.newBuilderForType().setKey(entry.getKey()).setValue(entry.getValue()).build();
    output.writeMessage(1,actives);
  }
  unknownFields.writeTo(output);
}
