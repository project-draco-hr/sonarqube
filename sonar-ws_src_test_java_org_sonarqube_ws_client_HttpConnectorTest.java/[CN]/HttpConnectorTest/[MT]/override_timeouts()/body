{
  HttpConnector underTest=new HttpConnector.Builder().url(serverUrl).readTimeoutMilliseconds(42).connectTimeoutMilliseconds(74).build();
  assertThat(underTest.okHttpClient().getReadTimeout()).isEqualTo(42);
  assertThat(underTest.okHttpClient().getConnectTimeout()).isEqualTo(74);
}
