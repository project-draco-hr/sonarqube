{
  return from(entry.getValue()).transform(new Function<TextBlock,DuplicateWithLocation>(){
    @Override @Nonnull public DuplicateWithLocation apply(    @Nonnull TextBlock input){
      return new DuplicateWithLocation(entry.getKey(),input);
    }
  }
);
}
