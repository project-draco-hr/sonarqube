{
  call(new PostRequest(path(ACTION_UPDATE)).setParam(PARAM_LOGIN,request.getLogin()).setParam(PARAM_NAME,request.getName()).setParam(PARAM_EMAIL,request.getEmail()).setParam(PARAM_SCM_ACCOUNT,request.getScmAccounts()));
}
