{
  Issuable issuable=perspectives.as(Issuable.class,file);
  Issue issue=issuable.newIssueBuilder().ruleKey(RULE_KEY_DEF).line(file.lines() + 10).message("This issue is generated on an invalid line using the old API").effortToFix(10.0).build();
  issuable.addIssue(issue);
}
