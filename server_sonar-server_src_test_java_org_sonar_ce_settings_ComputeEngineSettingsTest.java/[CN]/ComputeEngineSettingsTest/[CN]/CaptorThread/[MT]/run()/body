{
  if (callLoad) {
    try {
      rootProperties.setProperty(THREAD_PROPERTY,CAPTOR);
      underTest.load();
      doCapture();
      latch.countDown();
    }
  finally {
      underTest.unload();
    }
  }
 else {
    doCapture();
    latch.countDown();
  }
}
