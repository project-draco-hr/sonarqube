{
  underTest.load();
  expectedException.expect(IllegalStateException.class);
  expectedException.expectMessage("loadLocal called twice for Thread '" + Thread.currentThread().getName() + "' or state wasn't cleared last time it was used");
  underTest.load();
}
