{
  assertThat(underTest.getSettings()).isSameAs(underTest);
  underTest.load();
  assertThat(underTest.getSettings()).isSameAs(underTest);
  underTest.unload();
  assertThat(underTest.getSettings()).isSameAs(underTest);
}
