{
  insertUsers();
  String result=ws.newRequest().setParam("permission","scan").setParam(Param.SELECTED,SelectionMode.ALL.value()).execute().getInput();
  assertThat(result).contains("login-1","login-2","login-3");
}
