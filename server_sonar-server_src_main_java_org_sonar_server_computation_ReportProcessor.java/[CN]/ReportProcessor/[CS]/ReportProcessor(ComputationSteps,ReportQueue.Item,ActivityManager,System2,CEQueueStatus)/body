{
  this.executor=new ComputationStepExecutor(Loggers.get(ReportProcessor.class),new BatchReportStepsExecutorListener(item,system,activityManager,queueStatus),createDescription(item),queueStatus);
  this.steps=steps;
}
