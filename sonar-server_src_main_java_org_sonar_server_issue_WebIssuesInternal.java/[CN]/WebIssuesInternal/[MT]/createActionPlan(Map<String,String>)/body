{
  Integer projectId=toInteger(parameters.get("projectId"));
  DefaultActionPlan actionPlan=DefaultActionPlan.create(parameters.get("name")).setDescription(parameters.get("description")).setUserLogin(UserSession.get().login()).setDeadLine(toDate(parameters.get("deadLine")));
  return actionPlanManager.create(actionPlan,projectId);
}
