{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  Rule rule=(Rule)o;
  if (!name.equals(rule.name)) {
    return false;
  }
  if (!ruleKey.equals(rule.ruleKey)) {
    return false;
  }
  return true;
}
