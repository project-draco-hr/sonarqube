{
  json.name("user").beginObject().prop("login",user.login()).prop("name",user.name()).prop("email",user.email()).prop("active",user.active()).name("scmAccounts").beginArray().values(user.scmAccounts()).endArray().endObject();
}
