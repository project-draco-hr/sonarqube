{
  GroupPermissionDto groupRole=new GroupPermissionDto().setRole(permission).setGroupId(groupId).setResourceId(resourceId);
  updateProjectAuthorizationDate(session,resourceId);
  dbClient.roleDao().deleteGroupRole(groupRole,session);
}
