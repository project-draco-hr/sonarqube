{
  try {
    userCreator.create(NewUser.create().setLogin("").setName("").setEmail("marius@mail.com").setPassword("").setPasswordConfirmation(""));
    fail();
  }
 catch (  BadRequestException e) {
    assertThat(e.errors().messages()).hasSize(4);
  }
}
