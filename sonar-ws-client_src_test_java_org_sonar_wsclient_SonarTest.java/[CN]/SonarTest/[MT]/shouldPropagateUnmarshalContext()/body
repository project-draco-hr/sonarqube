{
  try {
    sonar.findAll(new RuleQuery("java"));
    fail();
  }
 catch (  UnmarshalException ue) {
    assertThat(ue.getMessage(),containsString("/api/rules"));
    assertThat(ue.getMessage(),containsString(BadRulesServlet.JSON));
  }
}
