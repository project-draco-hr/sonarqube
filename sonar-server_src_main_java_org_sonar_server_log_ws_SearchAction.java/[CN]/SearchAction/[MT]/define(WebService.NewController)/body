{
  WebService.NewAction action=controller.createAction(SEARCH_ACTION).setDescription("Search for a logs").setSince("4.4").setHandler(this);
  action.createParam(PARAM_TYPE).setDescription("Select types of log to search").setPossibleValues(Log.Type.values()).setDefaultValue(StringUtils.join(Log.Type.values(),","));
  SearchOptions.defineFieldsParam(action,mapping.supportedFields());
  SearchOptions.definePageParams(action);
}
