{
  return Lists.newArrayList(Iterables.filter(repository.getMetadata(),new Predicate<PluginMetadata>(){
    @Override public boolean apply(    PluginMetadata input){
      return !input.isCore();
    }
  }
));
}
