{
  thrown.expect(IllegalStateException.class);
  thrown.expectMessage("onProject already called");
  new DefaultMeasure<Integer>().onProject().onFile(new DefaultInputFile("foo","src/Foo.php")).withValue(3).save();
}
