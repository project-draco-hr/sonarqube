{
  setupData("insertRoles");
  RoleMapper mapper=session.getMapper(RoleMapper.class);
  mapper.insertGroupRole(new GroupRoleDto().setRole("admin").setGroupId(100L).setResourceId(123L));
  mapper.insertGroupRole(new GroupRoleDto().setRole("user").setResourceId(123L));
  mapper.insertUserRole(new UserRoleDto().setRole("codeviewer").setUserId(200L).setResourceId(123L));
  session.commit();
  checkTables("insertRoles","group_roles","user_roles");
}
