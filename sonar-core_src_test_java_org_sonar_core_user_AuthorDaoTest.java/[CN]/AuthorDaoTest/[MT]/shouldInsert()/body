{
  setupData("shouldInsert");
  AuthorDto authorDto=new AuthorDto().setLogin("godin").setPersonId(13L);
  dao.insert(authorDto);
  checkTables("shouldInsert",new String[]{"created_at","updated_at"},"authors");
}
