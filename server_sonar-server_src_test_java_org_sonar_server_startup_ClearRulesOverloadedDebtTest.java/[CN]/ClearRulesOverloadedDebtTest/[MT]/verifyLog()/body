{
  assertThat(logTester.logs(LoggerLevel.WARN)).containsOnly("The SQALE model has been cleaned to remove any redundant data left over from previous migrations.","=> As a result, the technical debt of existing issues in your projects may change slightly when those projects are reanalyzed.");
}
