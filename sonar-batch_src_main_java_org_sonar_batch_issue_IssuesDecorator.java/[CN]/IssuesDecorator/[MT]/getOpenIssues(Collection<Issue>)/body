{
  return newArrayList(Iterables.filter(issues,new Predicate<Issue>(){
    @Override public boolean apply(    final Issue issue){
      return !Issue.STATUS_CLOSED.equals(issue.status());
    }
  }
));
}
