{
  if (issues.isEmpty()) {
    return;
  }
  for (  DefaultIssue issue : issues) {
    if (issue.line() != null) {
      issue.setChecksum(sourceHashHolder.getHashedSource().getHash(issue.line()));
    }
  }
}
