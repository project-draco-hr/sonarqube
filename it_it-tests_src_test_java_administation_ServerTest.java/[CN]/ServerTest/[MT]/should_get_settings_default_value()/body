{
  orchestrator=Orchestrator.builderEnv().addPlugin(ItUtils.pluginArtifact("server-plugin")).build();
  orchestrator.start();
  Selenese selenese=Selenese.builder().setHtmlTestsInClasspath("settings-default-value","/server/ServerTest/settings-default-value.html").build();
  orchestrator.executeSelenese(selenese);
}
