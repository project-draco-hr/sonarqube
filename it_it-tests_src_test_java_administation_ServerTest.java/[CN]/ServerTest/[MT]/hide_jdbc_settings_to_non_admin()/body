{
  orchestrator=Orchestrator.createEnv();
  orchestrator.start();
  Selenese selenese=Selenese.builder().setHtmlTestsInClasspath("jdbc-settings","/administration/ServerTest/hide-jdbc-settings.html").build();
  orchestrator.executeSelenese(selenese);
}
