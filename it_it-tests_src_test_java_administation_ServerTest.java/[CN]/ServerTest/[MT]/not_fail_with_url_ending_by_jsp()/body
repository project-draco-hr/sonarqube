{
  orchestrator=Orchestrator.builderEnv().addPlugin(ItUtils.xooPlugin()).build();
  orchestrator.start();
  orchestrator.executeBuild(SonarRunner.create(ItUtils.projectDir("shared/xoo-sample")).setProperty("sonar.projectKey","myproject.jsp"));
  Selenese selenese=Selenese.builder().setHtmlTestsInClasspath("url_ending_by_jsp","/administration/ServerTest/url_ending_by_jsp.html").build();
  orchestrator.executeSelenese(selenese);
}
