{
  OrchestratorBuilder builder=Orchestrator.builderEnv();
  if (builder.getOrchestratorConfiguration().getString("sonar.jdbc.dialect").equals("h2")) {
    orchestrator=builder.build();
    orchestrator.start();
    Selenese selenese=Selenese.builder().setHtmlTestsInClasspath("derby-warnings","/administration/ServerTest/derby-warning.html").build();
    orchestrator.executeSelenese(selenese);
  }
}
