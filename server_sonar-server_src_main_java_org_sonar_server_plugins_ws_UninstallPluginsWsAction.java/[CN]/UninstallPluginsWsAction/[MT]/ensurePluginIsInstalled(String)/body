{
  if (find(pluginRepository.getMetadata(),new PluginKeyPredicate(key),null) == null) {
    throw new IllegalArgumentException(format("No plugin with key '%s' or plugin '%s' is not installed",key,key));
  }
}
