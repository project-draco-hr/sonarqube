{
  predicates=new DefaultFilePredicates(temp.newFolder());
  javaFile=new DefaultInputFile("foo","src/main/java/struts/Action.java").setFile(temp.newFile("Action.java")).setLanguage("java").setStatus(InputFile.Status.ADDED);
}
