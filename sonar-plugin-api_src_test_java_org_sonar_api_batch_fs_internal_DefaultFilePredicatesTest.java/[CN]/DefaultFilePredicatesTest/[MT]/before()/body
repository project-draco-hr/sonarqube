{
  predicates=new DefaultFilePredicates(temp.newFolder().toPath());
  javaFile=new DefaultInputFile("foo","src/main/java/struts/Action.java").setModuleBaseDir(temp.newFolder().toPath()).setLanguage("java").setStatus(InputFile.Status.ADDED);
}
