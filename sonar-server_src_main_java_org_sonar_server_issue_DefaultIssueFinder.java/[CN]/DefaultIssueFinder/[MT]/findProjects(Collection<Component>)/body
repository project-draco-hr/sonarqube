{
  return findComponents(newHashSet(Iterables.transform(components,new Function<Component,Long>(){
    @Override public Long apply(    @Nullable Component input){
      return input != null ? ((ComponentDto)input).projectId() : null;
    }
  }
)));
}
