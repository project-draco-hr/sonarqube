{
  NewController controller=context.newController("api/sources");
  controller.newAction("show").setDescription("Show source of a component").setSince("4.2").setInternal(true).setHandler(showHandler).newParam("key","Component key");
  controller.done();
}
