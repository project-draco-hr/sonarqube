{
  return Iterables.find(issues,new Predicate<Issue>(){
    public boolean apply(    Issue issue){
      return Objects.equals(issue.line(),line) && Objects.equals(issue.ruleKey(),repoKey + ":" + ruleKey);
    }
  }
);
}
