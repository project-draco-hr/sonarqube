{
  controller.createAction("list").setDescription("List of rules, excluding the manual rules and the rules with status REMOVED. JSON format is not supported for response.").setSince("5.2").setInternal(true).setHandler(this);
}
