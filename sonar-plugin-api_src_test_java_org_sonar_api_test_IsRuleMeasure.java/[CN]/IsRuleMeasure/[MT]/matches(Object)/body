{
  if (!(o instanceof RuleMeasure)) {
    return false;
  }
  RuleMeasure m=(RuleMeasure)o;
  return ObjectUtils.equals(metric,m.getMetric()) && ObjectUtils.equals(rule.ruleKey(),m.ruleKey()) && NumberUtils.compare(value,m.getValue()) == 0;
}
