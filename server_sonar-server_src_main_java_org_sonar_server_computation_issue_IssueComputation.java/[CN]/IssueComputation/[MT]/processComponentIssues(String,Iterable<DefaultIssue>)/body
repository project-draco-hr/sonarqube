{
  linesCache.init(componentUuid);
  for (  DefaultIssue issue : issues) {
    if (issue.isNew()) {
      guessAuthor(issue);
      autoAssign(issue);
      copyRuleTags(issue);
    }
    finalIssuesAppender.append(issue);
  }
  linesCache.clear();
}
