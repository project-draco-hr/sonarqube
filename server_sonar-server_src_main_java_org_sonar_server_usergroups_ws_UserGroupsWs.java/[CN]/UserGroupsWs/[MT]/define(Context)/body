{
  NewController controller=context.createController("api/user_groups").setDescription("User groups management").setSince("5.2");
  for (  UserGroupsWsAction action : actions) {
    action.define(controller);
  }
  controller.done();
}
