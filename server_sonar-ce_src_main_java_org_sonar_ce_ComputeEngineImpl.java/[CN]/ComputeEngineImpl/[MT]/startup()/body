{
  checkState(this.status == Status.INIT,"startup() can not be called multiple times");
  try {
    this.status=Status.STARTING;
    this.computeEngineContainer.configure(props).start();
  }
  finally {
    this.status=Status.STARTED;
  }
}
