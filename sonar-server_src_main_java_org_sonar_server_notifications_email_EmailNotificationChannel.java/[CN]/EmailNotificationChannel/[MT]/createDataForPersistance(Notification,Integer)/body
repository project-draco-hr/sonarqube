{
  for (  EmailMessageTemplate template : templates) {
    EmailMessage email=template.format(notification);
    if (email != null) {
      email.setTo(userId.toString());
      return email;
    }
  }
  return null;
}
