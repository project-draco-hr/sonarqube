{
  permissionService=mock(PermissionService.class);
  ws=new WsTester(new PermissionsWs(new RemoveGroupAction(permissionService)));
  userSession.login("admin").setGlobalPermissions(SYSTEM_ADMIN);
}
