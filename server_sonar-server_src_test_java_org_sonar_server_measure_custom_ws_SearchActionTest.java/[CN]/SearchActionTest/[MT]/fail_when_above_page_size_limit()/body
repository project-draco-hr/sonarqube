{
  expectedException.expect(IllegalArgumentException.class);
  newRequest().setParam(SearchAction.PARAM_PROJECT_ID,DEFAULT_PROJECT_UUID).setParam(WebService.Param.PAGE_SIZE,String.valueOf(MAX_LIMIT + 10)).execute();
}
