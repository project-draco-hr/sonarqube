{
  final int profileId=42;
  Map<String,Object> params=ImmutableMap.of("profileId",(Object)Integer.toString(profileId),"inheritance","bad value");
  try {
    ProfileRuleQuery.parse(params);
    fail();
  }
 catch (  Exception e) {
    assertThat(e).isInstanceOf(BadRequestException.class);
  }
}
