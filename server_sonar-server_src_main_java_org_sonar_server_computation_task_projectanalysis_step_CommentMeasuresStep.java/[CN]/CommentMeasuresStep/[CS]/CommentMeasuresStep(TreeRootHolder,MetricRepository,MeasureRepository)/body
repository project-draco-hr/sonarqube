{
  this.treeRootHolder=treeRootHolder;
  this.metricRepository=metricRepository;
  this.measureRepository=measureRepository;
  this.formulas=ImmutableList.<Formula>of(createIntSumFormula(COMMENTED_OUT_CODE_LINES_KEY),new DocumentationFormula(),new CommentDensityFormula());
}
