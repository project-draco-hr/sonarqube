{
  List<Rule> rules=rulesDao.getRules();
  List<Rule> userRules=Lists.newArrayList();
  for (  Rule rule : rules) {
    if (rule.getParent() != null) {
      userRules.add(rule);
    }
  }
  return userRules;
}
