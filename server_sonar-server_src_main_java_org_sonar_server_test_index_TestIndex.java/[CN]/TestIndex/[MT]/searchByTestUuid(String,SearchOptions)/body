{
  SearchRequestBuilder searchRequest=getClient().prepareSearch(TestIndexDefinition.INDEX).setTypes(TestIndexDefinition.TYPE).setSize(searchOptions.getLimit()).setFrom(searchOptions.getOffset()).setQuery(QueryBuilders.filteredQuery(QueryBuilders.matchAllQuery(),FilterBuilders.termFilter(FIELD_TEST_UUID,testUuid)));
  return new SearchResult<>(searchRequest.get(),CONVERTER);
}
