{
  for (  PurgeableAnalysisDto snapshot : snapshots) {
    LOG.debug("<- Delete snapshot: {} [{}]",DateUtils.formatDateTime(snapshot.getDate()),snapshot.getAnalysisUuid());
    purgeDao.deleteSnapshots(session,profiler,PurgeSnapshotQuery.create().setAnalysisUuid(snapshot.getAnalysisUuid()),PurgeSnapshotQuery.create().setSnapshotUuid(snapshot.getAnalysisUuid()));
  }
}
