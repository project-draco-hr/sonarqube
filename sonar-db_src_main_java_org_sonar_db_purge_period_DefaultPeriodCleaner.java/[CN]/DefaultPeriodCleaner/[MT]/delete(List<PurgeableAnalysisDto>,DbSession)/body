{
  for (  PurgeableAnalysisDto snapshot : snapshots) {
    LOG.debug("<- Delete snapshot: {} [{}]",DateUtils.formatDateTime(snapshot.getDate()),snapshot.getAnalysisUuid());
    purgeDao.deleteSnapshots(session,profiler,PurgeSnapshotQuery.create().setRootSnapshotId(snapshot.getAnalysisId()),PurgeSnapshotQuery.create().setId(snapshot.getAnalysisId()));
  }
}
