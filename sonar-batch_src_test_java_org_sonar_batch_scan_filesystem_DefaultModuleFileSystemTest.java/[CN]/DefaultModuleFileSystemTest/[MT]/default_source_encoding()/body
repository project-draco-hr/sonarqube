{
  File basedir=temp.newFolder("base");
  DefaultModuleFileSystem fileSystem=new DefaultModuleFileSystem().setBaseDir(basedir).setSettings(new Settings());
  assertThat(fileSystem.sourceCharset()).isEqualTo(Charset.defaultCharset());
}
