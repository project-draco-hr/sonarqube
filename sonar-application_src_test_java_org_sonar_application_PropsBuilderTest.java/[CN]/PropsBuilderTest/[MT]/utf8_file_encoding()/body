{
  FileUtils.write(new File(homeDir,"conf/sonar.properties"),"utf8prop=Th??nes");
  Props props=new PropsBuilder(new Properties(),jdbcSettings,homeDir).build();
  assertThat(props.value("utf8prop")).isEqualTo("Th??nes");
}
