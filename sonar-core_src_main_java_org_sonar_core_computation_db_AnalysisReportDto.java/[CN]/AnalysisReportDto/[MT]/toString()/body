{
  Objects.toStringHelper(this).add("id",getId()).add("projectKey",getProjectKey()).add("projectName",getProjectName()).add("status",getStatus());
  return String.format("analysis report {id:%s;project:'%s'}",getId(),getProjectKey());
}
