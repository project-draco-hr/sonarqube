{
  return Objects.toStringHelper(this).add("id",getId()).add("projectKey",getProjectKey()).add("status",getStatus()).add("createdAt",getCreatedAt()).add("startedAt",getStartedAt()).add("finishedAt",getFinishedAt()).toString();
}
