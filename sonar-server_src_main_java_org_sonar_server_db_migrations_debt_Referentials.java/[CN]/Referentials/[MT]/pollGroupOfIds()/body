{
  long[] longs=groupsOfIds.poll();
  if (longs == null) {
    return new Long[0];
  }
  Long[] objects=new Long[longs.length];
  for (int i=0; i < longs.length; i++) {
    objects[i]=longs[i];
  }
  return objects;
}
