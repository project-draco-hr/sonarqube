{
  json.name("comments").beginArray();
  for (  IssueComment comment : issue.comments()) {
    json.beginObject().prop("key",comment.key()).prop("userLogin",comment.userLogin()).prop("userName",queryResult.user(comment.userLogin()).name()).endObject();
  }
  json.endArray();
}
