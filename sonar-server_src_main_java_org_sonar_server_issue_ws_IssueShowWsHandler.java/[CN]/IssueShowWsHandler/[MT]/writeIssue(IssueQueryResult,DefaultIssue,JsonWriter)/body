{
  json.prop("key",issue.key()).prop("component",issue.componentKey()).prop("project",result.project(issue).key()).prop("rule",issue.ruleKey().toString()).prop("line",issue.line()).prop("message",issue.message()).prop("resolution",issue.resolution()).prop("status",issue.status()).prop("severity",issue.severity()).prop("actionPlan",issue.actionPlanKey());
  addUserField(result,issue.assignee(),"assignee",json);
  addUserField(result,issue.reporter(),"reporter",json);
  addUserField(result,issue.authorLogin(),"author",json);
}
