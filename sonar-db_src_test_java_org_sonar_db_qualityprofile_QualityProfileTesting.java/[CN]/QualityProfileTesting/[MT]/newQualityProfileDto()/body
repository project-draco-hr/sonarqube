{
  String uuid=Uuids.createFast();
  QualityProfileDto dto=QualityProfileDto.createFor(uuid).setName(uuid).setLanguage(randomAlphanumeric(20)).setLastUsed(nextLong());
  dto.setCreatedAt(new Date()).setUpdatedAt(new Date());
  return dto;
}
