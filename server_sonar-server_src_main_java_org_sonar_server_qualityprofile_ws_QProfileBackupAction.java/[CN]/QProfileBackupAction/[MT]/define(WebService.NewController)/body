{
  controller.createAction("backup").setSince("5.2").setDescription("Backup a quality profile in XML form.").setHandler(this).createParam(PARAM_KEY).setDescription("Key of the profile to backup.");
}
