{
  return new ReferencesBuilder(){
    @Override public ReferencesBuilder addReference(    int startOffset){
      symbolDataRepository.registerSymbolReference(symbol,startOffset);
      return this;
    }
  }
;
}
