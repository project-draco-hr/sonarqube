{
  NewController controller=context.createController("api/server").setDescription("Get system properties and upgrade db").setSince("2.10");
  defineIndexAction(controller);
  defineSystemAction(controller);
  defineSetupAction(controller);
  controller.done();
}
