{
  permissionService.synchronizePermissions(session,oldKey);
  permissionService.synchronizePermissions(session,newKey);
  dbClient.issueDao().synchronizeAfter(session,new Date(0),ImmutableMap.of("project",newKey));
}
