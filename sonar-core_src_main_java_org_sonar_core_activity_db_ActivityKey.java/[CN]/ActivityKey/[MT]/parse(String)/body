{
  String[] split=s.split(":");
  Preconditions.checkArgument(split.length == 3,"Invalid log key: " + s);
  return ActivityKey.of(new Date(Long.getLong(split[0])),Activity.Type.valueOf(split[1]),split[2]);
}
