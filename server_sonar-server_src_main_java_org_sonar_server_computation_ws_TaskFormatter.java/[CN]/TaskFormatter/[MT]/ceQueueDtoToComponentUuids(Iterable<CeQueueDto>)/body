{
  return from(dtos).transform(new Function<CeQueueDto,String>(){
    @Override @Nullable public String apply(    @Nonnull CeQueueDto input){
      return input.getComponentUuid();
    }
  }
).filter(notNull()).toSet();
}
