{
  return from(dtos).transform(new Function<CeActivityDto,String>(){
    @Override @Nullable public String apply(    @Nonnull CeActivityDto input){
      return input.getComponentUuid();
    }
  }
).filter(notNull()).toSet();
}
