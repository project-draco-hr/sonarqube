{
  DefaultCharacteristic requirement=new DefaultCharacteristic().setId(1).setRuleKey(RuleKey.of("repo","rule")).setFunction("linear_offset").setFactor(WorkUnit.create(2.0,"mn")).setOffset(WorkUnit.create(1.0,"h")).setRootId(3).setParentId(2);
  assertThat(requirement.isRequirement()).isTrue();
}
