{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  MeasureKey that=(MeasureKey)o;
  return metricKey.equals(that.metricKey) && ruleId == that.ruleId && characteristicId == that.characteristicId && developer == that.developer;
}
