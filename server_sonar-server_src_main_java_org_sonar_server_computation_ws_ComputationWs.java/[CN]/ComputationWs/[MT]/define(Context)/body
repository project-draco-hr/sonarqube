{
  NewController controller=context.createController(ENDPOINT).setDescription("Analysis reports processed");
  for (  ComputationWsAction action : actions) {
    action.define(controller);
  }
  controller.done();
}
