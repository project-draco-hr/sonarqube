{
  dbTester.getDbClient().customMeasureDao().insert(dbTester.getSession(),CustomMeasureTesting.newCustomMeasureDto().setComponentUuid(componentUuid).setMetricId(metric.getId()).setTextValue(value));
  dbTester.getSession().commit();
}
