{
  RuleDto rule=RuleDto.createFor(RuleKey.of("blah",id)).setLanguage(lang).setSeverity(Severity.BLOCKER).setStatus(RuleStatus.READY.name());
  dbClient.ruleDao().insert(session,rule);
  return rule;
}
