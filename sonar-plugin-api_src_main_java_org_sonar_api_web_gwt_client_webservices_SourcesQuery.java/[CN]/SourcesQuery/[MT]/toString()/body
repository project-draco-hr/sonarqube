{
  String url=Utils.getServerApiUrl() + "/sources?resource=" + getResourceKey()+ "&";
  if (length > 0) {
    url+="from=" + from + "&to="+ (from + length)+ "&";
  }
  if (color) {
    url+="color=true&";
  }
  return url;
}
