{
  JsonUtils.requestJson(this.toString(),new JSONHandlerDispatcher<FileSource>(callback){
    @Override public FileSource parseResponse(    JavaScriptObject obj){
      return parseLines(obj);
    }
  }
);
}
