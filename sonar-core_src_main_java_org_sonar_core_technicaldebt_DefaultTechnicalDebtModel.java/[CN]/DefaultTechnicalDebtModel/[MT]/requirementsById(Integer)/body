{
  return Iterables.find(requirements(),new Predicate<DefaultRequirement>(){
    @Override public boolean apply(    DefaultRequirement input){
      return input.id().equals(id);
    }
  }
,null);
}
