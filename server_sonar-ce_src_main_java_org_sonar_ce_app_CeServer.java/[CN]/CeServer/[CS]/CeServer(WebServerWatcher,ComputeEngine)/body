{
  this.webServerWatcher=webServerWatcher;
  this.computeEngine=computeEngine;
  new MinimumViableSystem().checkJavaVersion().checkWritableTempDir().checkRequiredJavaOptions(ImmutableMap.of("file.encoding","UTF-8"));
}
