{
  ListDefinitionsRequest request=ListDefinitionsRequest.builder().setComponentKey("KEY").build();
  underTest.listDefinitions(request);
  GetRequest getRequest=serviceTester.getGetRequest();
  assertThat(serviceTester.getGetParser()).isSameAs(Settings.ListDefinitionsWsResponse.parser());
  serviceTester.assertThat(getRequest).hasParam(PARAM_COMPONENT_KEY,"KEY").andNoOtherParam();
}
