{
  expectedException.expect(BadRequestException.class);
  newRequest(UserRole.USER,template1.getUuid()).setParam(org.sonarqube.ws.client.permission.PermissionsWsParameters.PARAM_TEMPLATE_NAME,template1.getName()).execute();
}
