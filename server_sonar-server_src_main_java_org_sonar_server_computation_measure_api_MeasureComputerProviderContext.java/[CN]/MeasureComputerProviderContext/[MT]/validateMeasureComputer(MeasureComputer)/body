{
  if (measureComputer instanceof MeasureComputerImpl) {
    return measureComputer;
  }
  Set<String> inputMetrics=measureComputer.getInputMetrics();
  Set<String> outputMetrics=measureComputer.getOutputMetrics();
  return newMeasureComputerBuilder().setInputMetrics(inputMetrics.toArray(new String[inputMetrics.size()])).setOutputMetrics(outputMetrics.toArray(new String[outputMetrics.size()])).setImplementation(measureComputer.getImplementation()).build();
}
