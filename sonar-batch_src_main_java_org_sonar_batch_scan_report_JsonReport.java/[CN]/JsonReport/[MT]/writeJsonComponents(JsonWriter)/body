{
  json.name("components").beginArray();
  for (  String componentKey : componentSelector.componentKeys()) {
    json.beginObject().name("key").value(componentSelector.getDeprecatedKey(componentKey)).endObject();
  }
  json.endArray();
}
