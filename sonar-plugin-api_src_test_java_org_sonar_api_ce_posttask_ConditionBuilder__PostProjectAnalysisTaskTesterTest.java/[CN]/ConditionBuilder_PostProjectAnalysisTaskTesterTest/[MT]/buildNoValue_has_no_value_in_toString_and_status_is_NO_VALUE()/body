{
  initValidBuilder();
  assertThat(underTest.buildNoValue().toString()).isEqualTo("Condition{status=NO_VALUE, metricKey='some metric key', operator=GREATER_THAN, " + "errorThreshold='some error threshold', warningThreshold='some warning threshold', onLeakPeriod=false}");
}
