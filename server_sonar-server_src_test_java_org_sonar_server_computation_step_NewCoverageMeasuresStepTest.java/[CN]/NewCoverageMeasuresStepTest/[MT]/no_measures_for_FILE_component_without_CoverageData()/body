{
  DumbComponent fileComponent=DumbComponent.builder(Component.Type.FILE,FILE_1_REF).setFileAttributes(new FileAttributes(false,null)).build();
  treeRootHolder.setRoot(fileComponent);
  reportReader.putChangesets(Changesets.newBuilder().setComponentRef(fileComponent.getReportAttributes().getRef()).addChangeset(Changesets.Changeset.newBuilder().setDate(parseDate("2008-05-18").getTime()).build()).addChangesetIndexByLine(0).build());
  underTest.execute();
  assertThat(measureRepository.isEmpty()).isTrue();
}
