{
  try {
    if (!pico.getLifecycleState().isStopped()) {
      pico.stop();
    }
    removeChild();
    if (parent != null) {
      parent.removeChild();
    }
  }
 catch (  RuntimeException e) {
    if (!swallowException) {
      throw PicoUtils.propagate(e);
    }
  }
  return this;
}
