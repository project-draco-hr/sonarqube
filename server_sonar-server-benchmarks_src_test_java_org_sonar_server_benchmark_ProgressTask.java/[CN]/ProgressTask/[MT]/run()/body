{
  long currentCount=counter.get();
  long now=System.currentTimeMillis();
  logger.info("{} {} indexed ({} docs/second)",currentCount,label,documentsPerSecond(currentCount - previousCount,now - previousTime),label);
  this.previousCount=currentCount;
  this.previousTime=now;
}
