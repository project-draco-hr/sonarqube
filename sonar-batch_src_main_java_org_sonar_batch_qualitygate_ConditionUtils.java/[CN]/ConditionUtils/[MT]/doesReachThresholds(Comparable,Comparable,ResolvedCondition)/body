{
  int comparison=measureValue.compareTo(criteriaValue);
  return !(isNotEquals(comparison,condition) || isGreater(comparison,condition) || isSmaller(comparison,condition)|| isEquals(comparison,condition));
}
