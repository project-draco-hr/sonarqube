{
  SqlSession sqlSession=mybatis.openSession();
  try {
    ResourceDto resource=new ResourceDto().setId(resourceId).setQualifier(qualifier).setName(resourceName).setRootId(rootProjectId);
    index(resource,sqlSession,true);
  }
  finally {
    sqlSession.close();
  }
  return this;
}
