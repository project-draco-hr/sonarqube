{
  WsTester tester=new WsTester(new UsersWs(new CreateAction(mock(UserService.class),mock(I18n.class)),new UpdateAction(mock(UserService.class)),new CurrentUserAction()));
  controller=tester.controller("api/users");
}
