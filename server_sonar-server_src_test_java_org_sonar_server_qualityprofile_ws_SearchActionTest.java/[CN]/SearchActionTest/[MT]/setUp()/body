{
  db.truncateTables();
  dbClient=db.getDbClient();
  qualityProfileDao=dbClient.qualityProfileDao();
  dbSession=db.getSession();
  xoo1=LanguageTesting.newLanguage("xoo1");
  xoo2=LanguageTesting.newLanguage("xoo2");
  ws=new WsActionTester(new SearchAction(new Languages(xoo1,xoo2),new QProfileLookup(dbClient),profileLoader,qualityProfileDao));
}
