{
  dbTester.prepareDbUnit(getClass(),"users_with_permissions.xml");
  assertThat(dao.selectUsers(dbSession,OldPermissionQuery.builder().permission("user").membership(OldPermissionQuery.OUT).build(),TEMPLATE_ID,0,10)).hasSize(1);
}
