{
  boolean destroyed=false;
  if (isAlive(process)) {
    try {
      process.destroy();
      destroyed=true;
    }
 catch (    Exception e) {
      LoggerFactory.getLogger(ProcessUtils.class).error("Fail to destroy " + process);
    }
  }
  return destroyed;
}
