{
  QualityProfileDto profile=profileDao.selectById(profileId,session);
  RuleDto rule=ruleDao.getById(ruleId,session);
  return activeRuleDao.getByKey(ActiveRuleKey.of(QualityProfileKey.of(profile.getName(),profile.getLanguage()),rule.getKey()),session);
}
