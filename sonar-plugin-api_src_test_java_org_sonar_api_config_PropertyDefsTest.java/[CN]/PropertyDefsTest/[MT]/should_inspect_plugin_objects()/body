{
  PropertyDefs def=new PropertyDefs(PropertyDef.builder("foo").name("Foo").build(),PropertyDef.builder("one").name("One").build(),PropertyDef.builder("two").name("Two").defaultValue("2").build());
  assertProperties(def);
}
