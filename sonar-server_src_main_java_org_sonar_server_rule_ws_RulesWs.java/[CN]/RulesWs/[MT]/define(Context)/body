{
  NewController controller=context.newController("api/rules").setDescription("Coding rules");
  controller.newAction("search").setDescription("Search for coding rules").setSince("4.2").setHandler(new RequestHandler(){
    @Override public void handle(    Request request,    Response response) throws Exception {
      search(request,response);
    }
  }
);
  controller.done();
}
