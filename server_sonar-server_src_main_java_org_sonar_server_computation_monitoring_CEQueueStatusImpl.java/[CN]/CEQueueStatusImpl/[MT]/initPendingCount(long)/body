{
  checkArgument(initialPendingCount >= 0,"Initial pending count must be >= 0");
  checkState(pending.compareAndSet(PENDING_INITIAL_VALUE,initialPendingCount),"Method initPendingCount must be used before any other method and can not be called twice");
  return initialPendingCount;
}
