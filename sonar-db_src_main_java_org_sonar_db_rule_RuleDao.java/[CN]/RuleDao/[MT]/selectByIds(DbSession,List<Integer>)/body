{
  if (ids.isEmpty()) {
    return emptyList();
  }
  return DatabaseUtils.executeLargeInputs(ids,new Function<List<Integer>,List<RuleDto>>(){
    @Override public List<RuleDto> apply(    @Nullable List<Integer> input){
      return mapper(dbSession).selectByIds(input);
    }
  }
);
}
