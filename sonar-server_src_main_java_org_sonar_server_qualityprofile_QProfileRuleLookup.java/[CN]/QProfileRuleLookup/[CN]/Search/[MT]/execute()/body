{
  int currentPage=1;
  boolean hasNextPage=true;
  while (hasNextPage) {
    int resultSize=search(currentPage);
    if (resultSize < pageSize) {
      hasNextPage=false;
    }
 else {
      currentPage++;
    }
  }
}
