{
  return index.executeCount(index.client().prepareCount(INDEX_RULES).setTypes(ESActiveRule.TYPE_ACTIVE_RULE).setQuery(QueryBuilders.filteredQuery(QueryBuilders.matchAllQuery(),boolFilter().must(hasParentFilter(TYPE_RULE,boolFilter().must(termFilter(RuleDocument.FIELD_ID,ruleId)))))));
}
