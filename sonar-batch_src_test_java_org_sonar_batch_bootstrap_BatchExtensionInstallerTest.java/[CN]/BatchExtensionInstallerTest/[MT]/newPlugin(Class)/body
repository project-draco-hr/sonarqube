{
  Map<PluginMetadata,Plugin> result=Maps.newHashMap();
  result.put(METADATA,new SonarPlugin(){
    public List<Class> getExtensions(){
      return Arrays.asList(classes);
    }
  }
);
  return result;
}
