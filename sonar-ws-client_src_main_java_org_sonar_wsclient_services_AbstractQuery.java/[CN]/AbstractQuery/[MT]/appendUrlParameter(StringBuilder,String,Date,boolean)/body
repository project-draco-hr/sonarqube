{
  if (paramValue != null) {
    String format=(includeTime ? "yyyy-MM-dd'T'HH:mm:ssZ" : "yyyy-MM-dd");
    url.append(paramKey).append('=').append(WSUtils.getINSTANCE().encodeUrl(WSUtils.getINSTANCE().format(paramValue,format))).append('&');
  }
}
