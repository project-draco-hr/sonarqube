{
  long now=System.currentTimeMillis();
  LOGGER.debug("Monitor::processIsValid() -- Time since last ping for '{}': {}ms",process.getName(),(now - pings.get(process.getName())));
  return (now - pings.get(process.getName())) < MAX_TIME;
}
