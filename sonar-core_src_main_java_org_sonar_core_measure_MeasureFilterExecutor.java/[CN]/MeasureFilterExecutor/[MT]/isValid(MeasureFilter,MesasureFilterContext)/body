{
  return !(filter.resourceQualifiers().isEmpty() && !filter.userFavourites()) && !(filter.isOnBaseResourceChildren() && context.getBaseSnapshot() == null) && !(filter.userFavourites() && context.getUserId() == null);
}
