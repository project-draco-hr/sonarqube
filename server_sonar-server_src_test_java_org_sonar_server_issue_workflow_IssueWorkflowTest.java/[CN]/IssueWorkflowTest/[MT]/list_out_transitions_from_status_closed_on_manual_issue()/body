{
  workflow.start();
  DefaultIssue issue=new DefaultIssue().setKey("ABCDE").setStatus(STATUS_CLOSED).setRuleKey(RuleKey.of("manual","Performance")).setReporter("simon");
  List<Transition> transitions=workflow.outTransitions(issue);
  assertThat(keys(transitions)).containsOnly("reopen");
}
