{
  Notification notification=createReviewNotification(review,project,resource).setFieldValue("old.status",review.getStatus()).setFieldValue("new.status",ReviewDto.STATUS_CLOSED);
  notificationManager.scheduleForSending(notification);
}
