{
  Notification notification=createReviewNotification(review,project,resource).setFieldValue("old.status",review.getStatus()).setFieldValue("new.status",ReviewDto.STATUS_REOPENED).setFieldValue("old.resolution",review.getResolution()).setFieldValue("new.resolution",null);
  notificationManager.scheduleForSending(notification);
}
