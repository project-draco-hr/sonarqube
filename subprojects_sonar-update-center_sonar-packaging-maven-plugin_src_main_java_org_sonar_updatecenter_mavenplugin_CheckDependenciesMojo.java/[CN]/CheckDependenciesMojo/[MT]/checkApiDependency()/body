{
  Artifact sonarApi=getSonarPluginApiArtifact();
  if (sonarApi == null) {
    throw new MojoExecutionException(SONAR_GROUPID + ":" + SONAR_PLUGIN_API_ARTIFACTID+ " should be declared in dependencies");
  }
}
