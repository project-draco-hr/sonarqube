{
  dbClient.widgetDao().update(session,widget.setWidgetKey(PROJECT_ISSUE_FILTER_WIDGET_KEY).setUpdatedAt(system.newDate()).setConfigured(true));
  widgetIdsWithPropertiesToDelete.add(widget.getId());
}
