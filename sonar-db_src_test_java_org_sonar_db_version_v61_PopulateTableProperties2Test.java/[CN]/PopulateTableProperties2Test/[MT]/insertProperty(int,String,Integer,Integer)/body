{
  dbTester.executeInsert("PROPERTIES","id",valueOf(idAndKey),"prop_key",valueOf(idAndKey),"text_value",value,"resource_id",resourceId == null ? null : valueOf(resourceId),"user_id",userId == null ? null : valueOf(userId));
  dbTester.commit();
}
