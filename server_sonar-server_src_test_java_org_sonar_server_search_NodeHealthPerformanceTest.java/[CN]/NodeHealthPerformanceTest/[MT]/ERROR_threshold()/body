{
  NodeHealth.Performance OK=new NodeHealth.Performance("test").setWarnThreshold(Integer.MIN_VALUE).setErrorThreshold(Integer.MIN_VALUE).setValue(1);
  assertThat(OK.getStatus()).isEqualTo(NodeHealth.Performance.Status.ERROR);
}
