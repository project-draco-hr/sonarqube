{
  return DaoUtils.partitionSelect(uuids,new DaoUtils.Function<ComponentDto,String>(){
    @Override public List<ComponentDto> execute(    List<String> partition){
      return mapper(session).findByUuids(partition);
    }
  }
);
}
