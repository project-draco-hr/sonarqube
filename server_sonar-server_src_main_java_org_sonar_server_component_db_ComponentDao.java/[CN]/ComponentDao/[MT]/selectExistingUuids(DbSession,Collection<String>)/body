{
  return DatabaseUtils.executeLargeInputs(uuids,new Function<List<String>,List<String>>(){
    @Override public List<String> apply(    @Nonnull List<String> partition){
      return mapper(session).selectExistingUuids(partition);
    }
  }
);
}
