{
  return DaoUtils.partitionSelect(uuids,new DaoUtils.Function<String,String>(){
    @Override public List<String> execute(    List<String> partition){
      return mapper(session).selectUuidsByUuids(partition);
    }
  }
);
}
