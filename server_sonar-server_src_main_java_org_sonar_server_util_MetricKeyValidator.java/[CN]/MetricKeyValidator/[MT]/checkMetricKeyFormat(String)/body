{
  if (!isMetricKeyValid(candidateKey)) {
    throw new IllegalArgumentException(String.format("Malformed metric key '%s'. Allowed characters are alphanumeric, '-', '_', '.' and ':', with at least one non-digit.",candidateKey));
  }
  return candidateKey;
}
