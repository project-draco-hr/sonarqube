{
  for (int i=0; i < 9; i++) {
    db.executeInsert("project_measures","METRIC_ID",valueOf(i),"VALUE",valueOf(i),"SNAPSHOT_ID",valueOf(i));
  }
  db.commit();
  underTest.execute();
  verifyAddedColumns();
}
