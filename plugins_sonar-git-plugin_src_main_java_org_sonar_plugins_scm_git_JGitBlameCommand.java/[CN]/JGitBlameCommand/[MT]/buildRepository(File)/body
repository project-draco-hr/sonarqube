{
  try {
    return new RepositoryBuilder().findGitDir(basedir).setMustExist(true).build();
  }
 catch (  IOException e) {
    throw new IllegalStateException("Unable to open Git repository",e);
  }
}
