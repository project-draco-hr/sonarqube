{
  return fs.hasFiles(fs.predicates().and(fs.predicates().hasType(Type.MAIN),fs.predicates().hasLanguage(Xoo.KEY))) && activeRules.find(RuleKey.of(XooRulesDefinition.XOO_REPOSITORY,RULE_KEY)) != null;
}
