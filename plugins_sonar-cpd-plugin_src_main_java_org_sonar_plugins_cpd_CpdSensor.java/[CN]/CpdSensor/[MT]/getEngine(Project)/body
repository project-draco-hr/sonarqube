{
  if (isSonarEngineEnabled(project)) {
    if (sonarEngine.isLanguageSupported(project.getLanguage())) {
      return sonarEngine;
    }
 else {
      return pmdEngine;
    }
  }
 else {
    return pmdEngine;
  }
}
