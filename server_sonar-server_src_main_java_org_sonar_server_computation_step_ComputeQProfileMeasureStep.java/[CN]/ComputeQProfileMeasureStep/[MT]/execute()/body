{
  Metric qProfilesMetric=metricRepository.getByKey(CoreMetrics.QUALITY_PROFILES_KEY);
  new PathAwareCrawler<>(new NewCoverageAggregationComponentCrawler(qProfilesMetric)).visit(treeRootHolder.getRoot());
}
