{
  try {
    barrier.await();
    for (int i=0; i < 100; i++) {
      if (dao.lock("my-lock",60 * 5)) {
        locks.incrementAndGet();
      }
    }
    latch.countDown();
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}
