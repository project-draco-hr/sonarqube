{
  QueryContext context=new QueryContext();
  return context.addFieldsToReturn(request.paramAsStrings(PARAM_FIELDS)).setFacet(request.paramAsBoolean(PARAM_FACETS)).setPage(request.mandatoryParamAsInt(PARAM_PAGE),request.mandatoryParamAsInt(PARAM_PAGE_SIZE));
}
