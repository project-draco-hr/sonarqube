{
  StringBuilder errorMsg=new StringBuilder();
  for (  String duplicationMetric : duplicated) {
    MeasureComputer otherComputer=computerByOutputMetrics.get(duplicationMetric);
    errorMsg.append(String.format("The output metric '%s' is already declared by another computer. This computer has these input metrics '%s' and these output metrics '%s'. ",duplicationMetric,otherComputer.getInputMetrics(),otherComputer.getOutputMetrics()));
  }
  return errorMsg.toString();
}
