{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  Match match=(Match)o;
  return dbKey.equals(match.dbKey) && reportKey.equals(match.reportKey);
}
