{
  DatabaseMigration migration=newMigration(System2.INSTANCE);
  migration.execute();
  int count=db.countSql("select count(*) from project_measures where " + "measure_date_ms is not null");
  assertThat(count).isEqualTo(2);
}
