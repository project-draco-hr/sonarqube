{
  StringBuilder sb=new StringBuilder();
  sb.append(pluginKey).append(",").append(sonarLintSupported).append(",").append(file.getFilename()).append("|").append(file.getHash());
  return sb.toString();
}
