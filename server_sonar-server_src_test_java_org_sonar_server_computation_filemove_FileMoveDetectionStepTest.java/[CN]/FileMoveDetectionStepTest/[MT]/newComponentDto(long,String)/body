{
  ComponentDtoWithSnapshotId res=new ComponentDtoWithSnapshotId();
  res.setSnapshotId(snapshotId).setId(dbIdGenerator).setKey(key).setUuid(componentUuidOf(key)).setPath("path_" + key);
  dbIdGenerator++;
  return res;
}
