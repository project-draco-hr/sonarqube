{
  List<Class<? extends Extension>> list=new ArrayList<Class<? extends Extension>>();
  if (sensor != null) {
    list.add(sensor);
  }
  if (rulesClass != null) {
    list.add(rulesClass);
  }
  return list;
}
