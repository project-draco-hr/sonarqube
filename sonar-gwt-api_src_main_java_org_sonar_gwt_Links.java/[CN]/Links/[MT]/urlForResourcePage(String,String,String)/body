{
  String url=baseUrl() + "/plugins/resource/";
  if (resourceIdOrKey != null) {
    url+=resourceIdOrKey;
  }
  url+="?page=";
  url+=pageId;
  if (query != null) {
    url+="&";
    url+=query;
  }
  return url;
}
