{
  UserSession.set(new UserSession(){
    @Override public boolean hasGlobalPermission(    String globalPermission){
      return true;
    }
    @Override public boolean hasProjectPermission(    String permission,    String projectKey){
      return true;
    }
  }
.setLocale(Locale.getDefault()).setLogin(SYSTEM_LOGIN).setName(SYSTEM_NAME));
}
