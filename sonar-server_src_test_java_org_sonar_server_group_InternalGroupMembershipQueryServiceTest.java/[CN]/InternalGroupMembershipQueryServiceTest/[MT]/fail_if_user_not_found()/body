{
  setupData("shared");
  try {
    service.find(ImmutableMap.<String,Object>of("login","user_not_existing","selected","all"));
    fail();
  }
 catch (  Exception e) {
    assertThat(e).isInstanceOf(NotFoundException.class).hasMessage("User 'user_not_existing' does not exists.");
  }
}
