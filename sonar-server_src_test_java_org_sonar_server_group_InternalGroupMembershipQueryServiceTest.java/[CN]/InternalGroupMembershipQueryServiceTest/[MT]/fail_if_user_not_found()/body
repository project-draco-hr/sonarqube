{
  setupData("shared");
  try {
    service.find(ImmutableMap.of("user","user_not_existing","selected","all"));
    fail();
  }
 catch (  Exception e) {
    assertThat(e).isInstanceOf(NotFoundException.class).hasMessage("User 'user_not_existing' does not exists.");
  }
}
