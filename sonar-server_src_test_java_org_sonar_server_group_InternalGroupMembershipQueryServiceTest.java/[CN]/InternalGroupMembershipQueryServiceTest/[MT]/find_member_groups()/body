{
  setupData("shared");
  List<GroupMembership> result=service.find(ImmutableMap.of("user","user1","selected","selected"));
  assertThat(result).hasSize(1);
  check(result.get(0),"sonar-users",true);
}
