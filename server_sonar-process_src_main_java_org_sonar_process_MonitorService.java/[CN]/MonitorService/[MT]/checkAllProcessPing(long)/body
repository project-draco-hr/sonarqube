{
  for (  Thread thread : processes.values()) {
    if (thread.isInterrupted()) {
      return false;
    }
  }
  for (  Long ping : processesPing.values()) {
    if ((now - ping) > 5000) {
      return false;
    }
  }
  return true;
}
