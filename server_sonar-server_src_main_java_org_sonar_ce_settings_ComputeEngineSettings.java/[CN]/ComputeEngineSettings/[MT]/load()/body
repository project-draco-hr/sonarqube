{
  checkState(this.threadLocalDelegate.get() == null,"loadLocal called twice for Thread '%' or state wasn't cleared last time it was used",Thread.currentThread().getName());
  this.threadLocalDelegate.set(loadServerSettings());
}
