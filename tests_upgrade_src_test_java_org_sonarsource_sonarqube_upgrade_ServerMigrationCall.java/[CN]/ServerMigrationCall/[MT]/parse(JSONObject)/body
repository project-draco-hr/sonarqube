{
  try {
    return new ServerMigrationResponse(ServerMigrationResponse.Status.valueOf((String)jsonObject.get("state")),(String)jsonObject.get("message"),parseDate((String)jsonObject.get("createdAt")));
  }
 catch (  Exception e) {
    throw new IllegalStateException("Failed to parse JSON response",e);
  }
}
