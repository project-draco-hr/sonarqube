{
  return newArrayList(Iterables.transform(snapshotDtos,new Function<PurgeableSnapshotDto,Long>(){
    @Override public Long apply(    @Nullable PurgeableSnapshotDto input){
      return input != null ? input.getSnapshotId() : null;
    }
  }
));
}
