{
  NewController controller=context.createController(API_ENDPOINT).setDescription("Analysis reports processed");
  NewAction action=controller.createAction("digest").setDescription("Digest report - experimental feature only").setInternal(true).setHandler(this.action);
  action.createParam("path").setRequired(true).setDescription("full path");
  controller.done();
}
