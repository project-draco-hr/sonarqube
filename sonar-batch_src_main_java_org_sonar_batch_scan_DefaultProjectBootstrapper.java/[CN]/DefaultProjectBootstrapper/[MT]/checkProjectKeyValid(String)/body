{
  if (!ComponentKeys.isValidModuleKey(projectKey)) {
    throw new IllegalStateException(String.format("Invalid project key '%s'.\n" + "Allowed characters are alphanumeric, '-', '_', '.' and ':', with at least one non-digit.",projectKey));
  }
}
