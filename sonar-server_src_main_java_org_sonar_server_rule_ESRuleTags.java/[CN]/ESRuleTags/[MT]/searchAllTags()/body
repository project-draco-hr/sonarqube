{
  final int scrollSize=50;
  return index.findDocumentIds(index.client().prepareSearch(RuleRegistry.INDEX_RULES).setTypes(TYPE_TAG).addSort(RuleTagDocument.FIELD_VALUE,SortOrder.ASC),scrollSize);
}
