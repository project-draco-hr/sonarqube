{
  for (int retry=0; retry < 16; retry++) {
    try {
      serverControl.ping();
      sleep(250);
    }
 catch (    Exception ex) {
      return;
    }
  }
  throw new SonarException("Embedded database is not stopped");
}
