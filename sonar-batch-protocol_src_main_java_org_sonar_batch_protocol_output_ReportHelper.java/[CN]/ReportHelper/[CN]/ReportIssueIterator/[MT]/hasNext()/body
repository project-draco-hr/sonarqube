{
  try {
    if (reader.hasNext()) {
      return true;
    }
    reader.endArray();
    reader.close();
    return false;
  }
 catch (  IOException e) {
    IOUtils.closeQuietly(reader);
    throw new IllegalStateException("Unable to iterate over JSON file ",e);
  }
}
