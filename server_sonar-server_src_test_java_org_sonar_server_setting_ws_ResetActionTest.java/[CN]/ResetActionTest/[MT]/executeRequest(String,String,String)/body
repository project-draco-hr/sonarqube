{
  TestRequest request=ws.newRequest().setMediaType(MediaTypes.PROTOBUF).setParam("key",key);
  if (componentId != null) {
    request.setParam("componentId",componentId);
  }
  if (componentKey != null) {
    request.setParam("componentKey",componentKey);
  }
  request.execute();
}
