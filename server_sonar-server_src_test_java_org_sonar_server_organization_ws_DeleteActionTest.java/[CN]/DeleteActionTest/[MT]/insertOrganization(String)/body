{
  OrganizationDto dto=new OrganizationDto().setUuid(uuid).setKey(uuid + "_key").setName(uuid + "_name").setDescription(uuid + "_description").setUrl(uuid + "_url").setAvatarUrl(uuid + "_avatar_url").setCreatedAt((long)uuid.hashCode()).setUpdatedAt((long)uuid.hashCode());
  dbTester.getDbClient().organizationDao().insert(dbTester.getSession(),dto);
  dbTester.commit();
  return dto;
}
