{
  List<Metric> result=Lists.newLinkedList();
  for (  String metricKey : metricKeys) {
    Metric metric=find(metricKey);
    if (metric != null) {
      result.add(metric);
    }
  }
  return result;
}
