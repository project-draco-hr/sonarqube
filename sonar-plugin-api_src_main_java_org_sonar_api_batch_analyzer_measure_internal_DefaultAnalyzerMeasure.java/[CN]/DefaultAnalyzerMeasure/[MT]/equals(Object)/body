{
  if (!(obj instanceof DefaultAnalyzerMeasure)) {
    return false;
  }
  DefaultAnalyzerMeasure<?> other=(DefaultAnalyzerMeasure<?>)obj;
  return metric.equals(other.metric) && value.equals(other.value) && (inputFile == null ? other.inputFile == null : inputFile.equals(other.inputFile));
}
