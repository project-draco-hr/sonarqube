{
  if (!(obj instanceof DefaultAnalyzerMeasure)) {
    return false;
  }
  DefaultAnalyzerMeasure<?> other=(DefaultAnalyzerMeasure<?>)obj;
  return metricKey.equals(other.metricKey) && value.equals(other.value) && (inputFile == null ? other.inputFile == null : inputFile.equals(other.inputFile));
}
