{
  NewAction action=controller.createAction("delete").setDescription("Delete a quality profile and all its descendants. The default quality profile cannot be deleted.").setSince("5.2").setPost(true).setHandler(this);
  QProfileIdentificationParamUtils.defineProfileParams(action,languages);
}
