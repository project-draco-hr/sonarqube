{
  if (edge.getTargetAsmClass() != asmClass && edge.getUsage() == SourceCodeEdgeUsage.CALLS_METHOD && !((AsmMethod)edge.getTo()).isAccessor()) {
    distinctCallToExternalMethods.add((AsmMethod)edge.getTo());
  }
}
