{
  return Maps.uniqueIndex(activeRules,new Function<ActiveRule,RuleKey>(){
    @Override public RuleKey apply(    @Nullable ActiveRule input){
      return input != null ? input.key().ruleKey() : null;
    }
  }
);
}
