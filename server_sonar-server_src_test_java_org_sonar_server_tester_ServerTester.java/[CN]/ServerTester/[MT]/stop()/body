{
  try {
    if (platform != null) {
      platform.doStop();
    }
  }
 catch (  Exception e) {
    LOG.error("Fail to stop web server",e);
  }
  platform=null;
  FileUtils.deleteQuietly(homeDir);
}
