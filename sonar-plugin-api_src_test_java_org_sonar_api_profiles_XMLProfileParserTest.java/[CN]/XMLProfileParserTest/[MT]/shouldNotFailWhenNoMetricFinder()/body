{
  ValidationMessages validation=ValidationMessages.create();
  RulesProfile profile=new XMLProfileParser(newRuleFinder(),null).parseResource(getClass().getClassLoader(),getResourcePath("importProfileWithAlerts.xml"),validation);
  assertThat(profile.getAlerts().size(),is(0));
}
