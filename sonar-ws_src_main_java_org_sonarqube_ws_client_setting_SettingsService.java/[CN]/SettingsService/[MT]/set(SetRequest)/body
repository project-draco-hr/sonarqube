{
  call(new PostRequest(path(ACTION_SET)).setParam(PARAM_KEY,request.getKey()).setParam(PARAM_VALUE,request.getValue()).setParam(PARAM_COMPONENT_ID,request.getComponentId()).setParam(PARAM_COMPONENT_KEY,request.getComponentKey()));
}
