{
  HighlightingLineReader highlightingLineReader=new HighlightingLineReader(newArrayList(BatchReport.SyntaxHighlighting.newBuilder().setRange(BatchReport.Range.newBuilder().setStartLine(1).setEndLine(1).setStartOffset(0).setEndOffset(4).build()).setType(Constants.HighlightingType.CONSTANT).build(),BatchReport.SyntaxHighlighting.newBuilder().setRange(BatchReport.Range.newBuilder().setStartLine(1).setEndLine(1).setStartOffset(2).setEndOffset(3).build()).setType(Constants.HighlightingType.KEYWORD).build()).iterator());
  highlightingLineReader.read(line1);
  assertThat(line1.getHighlighting()).isEqualTo("0,4,c;2,3,k");
}
