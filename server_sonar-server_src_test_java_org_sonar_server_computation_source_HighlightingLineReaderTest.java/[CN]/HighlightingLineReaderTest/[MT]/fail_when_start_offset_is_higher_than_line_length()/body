{
  HighlightingLineReader highlightingLineReader=new HighlightingLineReader(newArrayList(BatchReport.SyntaxHighlighting.newBuilder().setRange(BatchReport.TextRange.newBuilder().setStartLine(1).setEndLine(1).setStartOffset(10).setEndOffset(11).build()).setType(Constants.HighlightingType.ANNOTATION).build()).iterator());
  try {
    highlightingLineReader.read(line1);
    failBecauseExceptionWasNotThrown(IllegalArgumentException.class);
  }
 catch (  IllegalArgumentException e) {
    assertThat(e).hasMessage("Start offset 10 is defined outside the length (5) of the line 1");
  }
}
