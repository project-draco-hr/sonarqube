{
  setupData("shared");
  List<JpaPlugin> plugins=createManyPlugins();
  dao.register(plugins);
  assertThat(dao.getPlugins().size(),is(150));
  assertThat(dao.getPluginFiles().size(),is(150 * 20));
}
