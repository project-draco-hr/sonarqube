{
  DefaultCoverage coverage=new DefaultCoverage().onFile(main).ofType(CoverageType.UNIT).lineHits(1,2).lineHits(2,5).conditions(1,2,1);
  assertThat(coverage.file()).isEqualTo(main);
  assertThat(coverage.type()).isEqualTo(CoverageType.UNIT);
}
