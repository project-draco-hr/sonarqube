{
  thrown.expect(IllegalArgumentException.class);
  thrown.expectMessage("Hits already saved on line [src/Foo.php:1]");
  new DefaultCoverage().onFile(main).ofType(CoverageType.UNIT).lineHits(1,2).lineHits(1,1);
}
