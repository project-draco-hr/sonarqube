{
  thrown.expect(IllegalArgumentException.class);
  thrown.expectMessage("Number of conditions should be positive [src/Foo.php:1]");
  new DefaultCoverage().onFile(main).ofType(CoverageType.UNIT).conditions(1,-1,0);
}
