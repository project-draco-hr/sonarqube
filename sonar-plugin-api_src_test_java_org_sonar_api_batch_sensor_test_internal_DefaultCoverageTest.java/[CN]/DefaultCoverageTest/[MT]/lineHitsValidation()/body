{
  thrown.expect(IllegalArgumentException.class);
  thrown.expectMessage("Line number should be positive and non zero [src/Foo.php:0]");
  new DefaultCoverage().onFile(main).ofType(CoverageType.UNIT).lineHits(0,2);
}
