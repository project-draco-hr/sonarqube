{
  new DepthTraversalTypeAwareCrawler(new TypeAwareVisitorAdapter(Component.Type.PROJECT,POST_ORDER){
    @Override public void visitProject(    Component tree){
      executeForProject(tree);
    }
  }
).visit(treeRootHolder.getRoot());
}
