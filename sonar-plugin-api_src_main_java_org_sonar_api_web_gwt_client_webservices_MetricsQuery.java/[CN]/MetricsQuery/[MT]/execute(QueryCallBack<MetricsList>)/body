{
  JsonUtils.requestJson(this.toString(),new JSONHandlerDispatcher<MetricsList>(callback){
    @Override public MetricsList parseResponse(    JavaScriptObject obj){
      return parseMetrics(obj);
    }
  }
);
}
