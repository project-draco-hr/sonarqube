{
  isNotNull();
  if (actual.getValueType() != Measure.ValueType.NO_VALUE) {
    failWithMessage("Expected Measure to have no value and therefore its ValueType to be <%s> but was <%s>",Measure.ValueType.DOUBLE,actual.getValueType());
  }
  return this;
}
