{
  List<String> encoding=ViolationPersisterDecorator.getChecksums("???????????? ??????");
  assertThat(encoding.size(),is(1));
  assertThat(encoding.get(0),is("5ba3a45e1299ede07f56e5531351be52"));
}
