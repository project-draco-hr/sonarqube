{
  db.executeInsert(TABLE_EVENTS,"ID",valueOf(id),"NAME","name_" + id,"ANALYSIS_UUID",hasAnalysisUuid ? "uuid_" + id : null,"SNAPSHOT_ID",hasSnapshotId ? valueOf(id + 100) : null,"EVENT_DATE",valueOf(1 + 100),"CREATED_AT",valueOf(1 + 300));
}
