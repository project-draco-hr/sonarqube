{
  if (rule.ruleNote() != null && !Strings.isNullOrEmpty(rule.ruleNote().data())) {
    json.prop("noteRaw",rule.ruleNote().data()).prop("noteHtml",Markdown.convertToHtml(rule.ruleNote().data()));
  }
}
