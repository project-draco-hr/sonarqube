{
  return asList(PropertyDefinition.builder(CeLogging.MAX_LOGS_PROPERTY).name("Compute Engine Log Retention").description("Number of tasks to keep logs for a given project. Once the number of logs exceeds this limit, oldest logs are purged.").type(PropertyType.INTEGER).defaultValue("10").category(CoreProperties.CATEGORY_GENERAL).build());
}
