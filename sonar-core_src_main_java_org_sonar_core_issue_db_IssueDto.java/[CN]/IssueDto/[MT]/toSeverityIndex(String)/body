{
  if (severity != null) {
    return Iterables.indexOf(Severity.ALL,new Predicate<String>(){
      @Override public boolean apply(      String currentSeverity){
        return currentSeverity.equals(severity);
      }
    }
) + 1;
  }
  return null;
}
