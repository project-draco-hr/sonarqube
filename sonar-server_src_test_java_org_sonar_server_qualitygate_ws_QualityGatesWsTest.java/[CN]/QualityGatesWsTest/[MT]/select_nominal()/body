{
  long gateId=42L;
  long projectId=666L;
  tester.newGetRequest("api/qualitygates","select").setParam("gateId",Long.toString(gateId)).setParam("projectId",Long.toString(projectId)).execute().assertNoContent();
  verify(qGates).associateProject(gateId,projectId);
}
