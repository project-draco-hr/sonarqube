{
  long gateId=42L;
  long projectId=666L;
  tester.newRequest("deselect").setParam("gateId",Long.toString(gateId)).setParam("projectId",Long.toString(projectId)).execute().assertNoContent();
  verify(qGates).dissociateProject(gateId,projectId);
}
