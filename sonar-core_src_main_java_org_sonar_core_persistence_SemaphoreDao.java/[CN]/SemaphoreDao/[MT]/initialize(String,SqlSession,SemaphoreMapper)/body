{
  try {
    SemaphoreDto semaphore=new SemaphoreDto().setName(name).setLockedAt(org.sonar.api.utils.DateUtils.parseDate("2001-01-01"));
    mapper.initialize(semaphore);
    session.commit();
  }
 catch (  Exception e) {
  }
}
