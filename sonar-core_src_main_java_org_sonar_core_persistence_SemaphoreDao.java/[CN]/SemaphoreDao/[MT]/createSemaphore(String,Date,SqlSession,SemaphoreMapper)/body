{
  try {
    SemaphoreDto semaphore=new SemaphoreDto().setName(name).setLockedAt(lockedAt);
    mapper.initialize(semaphore);
    session.commit();
    return semaphore;
  }
 catch (  Exception e) {
    session.rollback();
    return null;
  }
}
