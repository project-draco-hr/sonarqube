{
  return new MetricFilter<Measure>(metric){
    public Measure filter(    Collection<Measure> measures){
      if (measures == null) {
        return null;
      }
      for (      Measure measure : measures) {
        Characteristic measureCharacteristic=measure.getCharacteristic();
        if (measure.getClass().equals(Measure.class) && measure.getMetric().equals(metric) && measure.getPersonId() == null && measureCharacteristic != null && measureCharacteristic.equals(characteristic)) {
          return measure;
        }
      }
      return null;
    }
  }
;
}
