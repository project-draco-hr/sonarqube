{
  tester.get(SearchClient.class).prepareDeleteByQuery(tester.get(SearchClient.class).admin().cluster().prepareState().get().getState().getMetaData().concreteIndices(new String[]{IndexDefinition.ISSUES.getIndexName()})).setQuery(QueryBuilders.matchAllQuery()).get();
}
