{
  GroupWithPermission group=new GroupWithPermissionDto().setName("users").setPermission(null).toGroupWithPermission();
  assertThat(group.name()).isEqualTo("users");
  assertThat(group.hasPermission()).isFalse();
}
