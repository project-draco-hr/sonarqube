{
  List<String> items=RubyUtils.toStrings(jRubyArray);
  String[] empty=new String[0];
  if (items == null) {
    return empty;
  }
 else {
    items.remove("");
    return items.toArray(empty);
  }
}
