{
  expectedException.expect(ForbiddenException.class);
  userSession.anonymous().login().addProjectUuidPermissions(UserRole.CODEVIEWER,"project-uuid");
  componentDb.insertComponent(newProjectDto("project-uuid"));
  db.commit();
  ws.newRequest().setParam(PARAM_BASE_COMPONENT_ID,"project-uuid").execute();
}
