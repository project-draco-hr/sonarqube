{
  thrown.expect(IllegalArgumentException.class);
  thrown.expectMessage("line starts at 1, invalid value 0.");
  new DefaultIssue().onFile(new DefaultInputFile("foo","src/Foo.php").setLines(3)).forRule(RuleKey.of("repo","rule")).atLine(0);
}
