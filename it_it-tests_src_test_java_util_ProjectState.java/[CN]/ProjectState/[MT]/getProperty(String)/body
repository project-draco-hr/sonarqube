{
  String value=this.properties.getProperty(propertyName);
  checkState(value != null,"Property %s is missing in %s file in project directory %s",propertyName,SONAR_PROJECT_PROPERTIES_FILE_NAME,projectDir.getAbsolutePath());
  return value;
}
