{
  RuleKey ruleKey=RuleKey.of(XooRulesDefinition.XOO_REPOSITORY,RULE_KEY);
  NewIssue newIssue=context.newIssue();
  newIssue.forRule(ruleKey).addLocation(newIssue.newLocation().onFile(file).at(file.selectLine(1)).message("This issue is generated on each file")).save();
}
