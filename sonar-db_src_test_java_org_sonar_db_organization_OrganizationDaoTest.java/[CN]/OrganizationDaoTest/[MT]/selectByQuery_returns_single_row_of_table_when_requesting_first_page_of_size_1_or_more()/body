{
  insertOrganization(ORGANIZATION_DTO);
  assertThat(underTest.selectByQuery(dbSession,0,1)).hasSize(1).extracting("uuid").containsOnly(ORGANIZATION_DTO.getUuid());
  assertThat(underTest.selectByQuery(dbSession,0,10)).hasSize(1).extracting("uuid").containsOnly(ORGANIZATION_DTO.getUuid());
}
