{
  IndexStat stat=new IndexStat();
  stat.setDocumentCount(getClient().prepareCount(this.getIndexName()).setQuery(QueryBuilders.matchAllQuery()).get().getCount());
  stat.setLastUpdate(getLastSynchronization());
  return stat;
}
