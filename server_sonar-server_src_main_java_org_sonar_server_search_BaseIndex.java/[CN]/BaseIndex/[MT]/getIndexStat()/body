{
  CountRequestBuilder countRequest=client.prepareCount(this.getIndexName()).setTypes(this.getIndexType()).setQuery(QueryBuilders.matchAllQuery());
  CountResponse response=client.execute(countRequest);
  return new IndexStat(getLastSynchronization(),response.getCount());
}
