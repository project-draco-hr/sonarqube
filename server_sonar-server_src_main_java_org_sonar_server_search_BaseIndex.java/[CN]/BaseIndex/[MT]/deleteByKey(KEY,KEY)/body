{
  try {
    this.deleteDocument(key);
    for (    KEY additionalKey : keys) {
      this.deleteDocument(additionalKey);
    }
  }
 catch (  Exception e) {
    throw new IllegalStateException(String.format("Could not DELETE _id = '%s' for index '%s",getKeyValue(key),getIndexName()),e);
  }
}
