{
  GroupDto group=createGroup();
  session.commit();
  tester.newGetRequest("api/usergroups","users").setParam("login","john").setParam("id",group.getId().toString()).execute().assertJson(getClass(),"empty.json");
}
