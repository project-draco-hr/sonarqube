{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  SqlActiveRuleKey activeRuleKey=(SqlActiveRuleKey)o;
  return qProfile.equals(activeRuleKey.qProfile) && rule.equals(activeRuleKey.rule) && repository.equals(activeRuleKey.repository);
}
