{
  if (!isSortByMetric(wsRequest)) {
    return components;
  }
  Paging paging=Paging.forPageIndex(wsRequest.getPage()).withPageSize(wsRequest.getPageSize()).andTotal(componentCount);
  return from(components).skip(paging.offset()).limit(paging.pageSize()).toList();
}
