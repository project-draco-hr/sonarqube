{
  this.metric=metric;
  MeasureDto measure=new MeasureDto().setMetricId(metric.getId()).setMetricKey(metric.getKey()).setValue(metric.getBestValue());
  for (  Integer periodIndex : periodIndexes) {
    measure.setVariation(periodIndex,0.0d);
  }
  this.bestValue=measure;
}
