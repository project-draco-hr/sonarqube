{
  String uid=resource.getKey();
  if (!StringUtils.equals(Resource.SCOPE_SET,resource.getScope())) {
    uid=new StringBuilder(ResourceModel.KEY_SIZE).append(project.getKey()).append(':').append(resource.getKey()).toString();
  }
  return uid;
}
