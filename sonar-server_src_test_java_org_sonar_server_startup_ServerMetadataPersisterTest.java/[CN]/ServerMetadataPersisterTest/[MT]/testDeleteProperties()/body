{
  setupData("testDeleteProperties");
  Server server=mock(Server.class);
  when(server.getStartedAt()).thenReturn(new SimpleDateFormat("yyyy-MM-dd HH:mm").parse("2010-05-18 17:59"));
  persist(server);
  checkTables("testDeleteProperties","properties");
}
