{
  if (this.latestChange == null) {
    this.latestChange=newChangeSet;
  }
 else   if (newChangeSet.hasDate() && this.latestChange.hasDate() && newChangeSet.getDate() > this.latestChange.getDate()) {
    this.latestChange=newChangeSet;
  }
}
