{
  String[] split=s.split(":");
  Preconditions.checkArgument(split.length == 4,"Bad format of activeRule key: " + s);
  return ActiveRuleKey.of(QProfileKey.of(split[0],split[1]),RuleKey.of(split[2],split[3]));
}
