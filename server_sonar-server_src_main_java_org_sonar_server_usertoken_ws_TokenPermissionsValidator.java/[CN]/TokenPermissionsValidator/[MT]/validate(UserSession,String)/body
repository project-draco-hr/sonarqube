{
  if (userSession.hasPermission(GlobalPermissions.SYSTEM_ADMIN) || (requestLogin != null && requestLogin.equals(userSession.getLogin()))) {
    return;
  }
  throw insufficientPrivilegesException();
}
