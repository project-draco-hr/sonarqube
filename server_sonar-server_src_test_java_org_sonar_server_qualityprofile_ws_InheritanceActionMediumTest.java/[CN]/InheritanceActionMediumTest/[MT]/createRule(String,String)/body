{
  long now=new Date().getTime();
  RuleDto rule=RuleDto.createFor(RuleKey.of("blah",id)).setLanguage(lang).setSeverity(Severity.BLOCKER).setStatus(RuleStatus.READY).setUpdatedAtInMs(now).setCreatedAtInMs(now);
  db.ruleDao().insert(session,rule);
  return rule;
}
