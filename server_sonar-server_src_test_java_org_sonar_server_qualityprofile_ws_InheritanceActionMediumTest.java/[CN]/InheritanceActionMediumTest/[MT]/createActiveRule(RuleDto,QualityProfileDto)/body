{
  long now=new Date().getTime();
  ActiveRuleDto activeRule=ActiveRuleDto.createFor(profile,rule).setSeverity(rule.getSeverityString()).setUpdatedAtInMs(now).setCreatedAtInMs(now);
  db.activeRuleDao().insert(session,activeRule);
  return activeRule;
}
