{
  try {
    NewRule.createForManualRule("").setName("My manual").setHtmlDescription("Some description");
    fail();
  }
 catch (  Exception e) {
    assertThat(e).isInstanceOf(IllegalArgumentException.class).hasMessage("Manual key should be set");
  }
}
