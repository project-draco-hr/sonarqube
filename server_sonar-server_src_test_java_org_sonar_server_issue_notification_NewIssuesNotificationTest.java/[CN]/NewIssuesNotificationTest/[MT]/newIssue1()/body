{
  return new DefaultIssue().setAssignee("maynard").setComponentUuid("file-uuid").setSeverity(Severity.INFO).setTags(Lists.newArrayList("bug","owasp")).setRuleKey(RuleKey.of("SonarQube","rule-the-world")).setDebt(Duration.create(5L));
}
