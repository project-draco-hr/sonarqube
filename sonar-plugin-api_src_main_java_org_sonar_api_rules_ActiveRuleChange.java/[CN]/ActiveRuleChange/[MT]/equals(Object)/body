{
  if (obj == null) {
    return false;
  }
  if (obj == this) {
    return true;
  }
  if (obj.getClass() != getClass()) {
    return false;
  }
  ActiveRuleChange rhs=(ActiveRuleChange)obj;
  return new EqualsBuilder().appendSuper(super.equals(obj)).append(modifierLogin,rhs.modifierLogin).append(rulesProfile,rhs.rulesProfile).append(rule,rhs.rule).append(date,rhs.date).append(enabled,rhs.enabled).append(newSeverity,rhs.newSeverity).isEquals();
}
