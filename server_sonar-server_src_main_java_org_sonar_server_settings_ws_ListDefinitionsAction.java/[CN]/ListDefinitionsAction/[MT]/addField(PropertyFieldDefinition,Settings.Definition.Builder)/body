{
  builder.addFieldsBuilder().setKey(fieldDefinition.key()).setName(fieldDefinition.name()).setType(Settings.Type.valueOf(fieldDefinition.type().name())).setIndicativeSize(fieldDefinition.indicativeSize()).addAllOptions(fieldDefinition.options()).build();
}
