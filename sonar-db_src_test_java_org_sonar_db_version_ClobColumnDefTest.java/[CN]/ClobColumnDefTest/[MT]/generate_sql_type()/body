{
  ClobColumnDef def=new ClobColumnDef.Builder().setColumnName("issues").setIsNullable(true).build();
  assertThat(def.generateSqlType(new MsSql())).isEqualTo("NVARCHAR (MAX) COLLATE Latin1_General_CS_AS");
}
