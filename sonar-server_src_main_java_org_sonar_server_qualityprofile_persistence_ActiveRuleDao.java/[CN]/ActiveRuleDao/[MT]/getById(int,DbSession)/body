{
  ActiveRuleDto rule=mapper(session).selectById(activeRuleId);
  rule.setKey(ActiveRuleKey.of(profileDao.selectById(rule.getProfileId(),session).getKey(),ruleDao.getById(rule.getRulId(),session).getKey()));
  return rule;
}
