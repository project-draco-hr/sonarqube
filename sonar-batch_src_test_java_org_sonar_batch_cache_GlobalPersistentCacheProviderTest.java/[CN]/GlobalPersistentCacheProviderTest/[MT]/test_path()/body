{
  PersistentCache cache=provider.provide(globalProperties);
  assertThat(cache.getDirectory()).isEqualTo(temp.getRoot().toPath().resolve("ws_cache").resolve("http%3A%2F%2Flocalhost%3A9000-" + BatchUtils.getServerVersion()).resolve("global"));
}
