{
  if (resourceId != null) {
    List<Property> properties=session.createQuery("from " + Property.class.getSimpleName() + " p where p.resourceId=:resourceId").setParameter("resourceId",resourceId).getResultList();
    registerProperties(properties);
  }
}
