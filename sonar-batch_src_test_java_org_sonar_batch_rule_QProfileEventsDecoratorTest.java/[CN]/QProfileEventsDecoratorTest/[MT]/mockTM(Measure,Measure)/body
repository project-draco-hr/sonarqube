{
  when(timeMachine.getMeasures(any(TimeMachineQuery.class))).thenReturn(Collections.<Measure>emptyList()).thenReturn(result1 == null ? Collections.<Measure>emptyList() : Arrays.asList(result1)).thenReturn(result2 == null ? Collections.<Measure>emptyList() : Arrays.asList(result2));
}
