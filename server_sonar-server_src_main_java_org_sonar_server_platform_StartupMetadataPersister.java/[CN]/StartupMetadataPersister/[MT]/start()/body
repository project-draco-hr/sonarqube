{
  String startedAt=DateUtils.formatDateTime(new Date(metadata.getStartedAt()));
  persistentSettings.saveProperties(ImmutableMap.of(CoreProperties.SERVER_ID,metadata.getStartupId(),CoreProperties.SERVER_STARTTIME,startedAt));
}
