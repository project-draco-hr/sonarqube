{
  return new UpdateTemplateWsRequest().setId(request.mandatoryParam(PARAM_ID)).setName(request.param(PARAM_NAME)).setDescription(request.param(PARAM_DESCRIPTION)).setProjectKeyPattern(request.param(PARAM_PROJECT_KEY_PATTERN));
}
