{
  JsonUtils.requestJson(this.toString(),new JSONHandlerDispatcher<Violations>(callback){
    @Override public Violations parseResponse(    JavaScriptObject obj){
      return parseJSON(obj);
    }
  }
);
}
