{
  json.name("groups").beginArray();
  for (  GroupMembershipDto group : groups) {
    json.beginObject().prop("name",group.getName()).prop("description",group.getDescription()).prop("selected",group.getUserId() != null).endObject();
  }
  json.endArray();
}
