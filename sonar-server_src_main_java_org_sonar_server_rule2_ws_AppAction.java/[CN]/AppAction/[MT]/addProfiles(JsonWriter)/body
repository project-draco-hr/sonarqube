{
  json.name("qualityprofiles").beginArray();
  for (  QProfile profile : qProfiles.allProfiles()) {
    json.beginObject().prop("name",profile.name()).prop("lang",profile.language()).prop("parent",profile.parent()).endObject();
  }
  json.endArray();
}
