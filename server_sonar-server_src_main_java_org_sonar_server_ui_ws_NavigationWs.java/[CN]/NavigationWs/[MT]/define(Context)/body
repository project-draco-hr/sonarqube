{
  NewController navigation=context.createController("api/navigation").setDescription("Get information required to build navigation UI components").setSince("5.2");
  for (  NavigationAction action : actions) {
    action.define(navigation);
  }
  navigation.done();
}
