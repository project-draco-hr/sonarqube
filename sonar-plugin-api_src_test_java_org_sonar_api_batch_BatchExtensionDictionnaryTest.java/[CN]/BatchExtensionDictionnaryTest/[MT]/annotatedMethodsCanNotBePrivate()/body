{
  BatchExtensionDictionnary selector=newSelector();
  BatchExtension wrong=new BatchExtension(){
    @DependsUpon @SuppressWarnings("unused") private Object foo(){
      return "foo";
    }
  }
;
  selector.evaluateAnnotatedClasses(wrong,DependsUpon.class);
}
