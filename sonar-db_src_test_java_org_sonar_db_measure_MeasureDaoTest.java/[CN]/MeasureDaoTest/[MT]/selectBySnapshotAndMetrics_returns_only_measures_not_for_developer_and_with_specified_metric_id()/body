{
  db.prepareDbUnit(getClass(),"with_some_measures_for_developer.xml");
  List<MeasureDto> measureDtos=underTest.selectBySnapshotAndMetrics(dbSession,SNAPSHOT_ID,ImmutableList.of(NCLOC_METRIC_ID));
  assertThat(measureDtos).extracting("id").containsOnly(22L);
}
