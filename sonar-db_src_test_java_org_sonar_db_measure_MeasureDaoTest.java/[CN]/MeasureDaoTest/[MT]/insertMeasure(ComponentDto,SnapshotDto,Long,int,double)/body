{
  MeasureDto measureDto=new MeasureDto().setMetricId(metricId).setValue(value).setComponentUuid(componentDto.uuid()).setSnapshotId(snapshotDto.getId()).setAnalysisUuid("u1").setDeveloperId(developerId);
  dbClient.measureDao().insert(dbSession,measureDto);
  dbSession.commit();
  return measureDto;
}
