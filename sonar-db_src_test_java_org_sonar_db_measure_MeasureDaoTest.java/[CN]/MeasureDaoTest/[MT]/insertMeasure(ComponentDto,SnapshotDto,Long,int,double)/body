{
  MeasureDto measureDto=new MeasureDto().setMetricId(metricId).setValue(value).setComponentUuid(componentDto.uuid()).setSnapshotId(snapshotDto.getId()).setDeveloperId(developerId);
  dbClient.measureDao().insert(dbSession,measureDto);
  dbSession.commit();
  return measureDto;
}
