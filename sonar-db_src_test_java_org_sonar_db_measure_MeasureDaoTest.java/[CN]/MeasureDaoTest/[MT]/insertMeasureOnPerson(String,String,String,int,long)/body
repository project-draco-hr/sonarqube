{
  MeasureDto measure=MeasureTesting.newMeasure().setAnalysisUuid(analysisUuid).setComponentUuid(componentUuid).setMetricId(metricId).setDeveloperId(personId).setData(id);
  db.getDbClient().measureDao().insert(db.getSession(),measure);
}
