{
  db.prepareDbUnit(getClass(),"with_some_measures_for_developer.xml");
  List<MeasureDto> measureDtos=underTest.selectByDeveloperForSnapshotAndMetrics(db.getSession(),DEVELOPER_ID,SNAPSHOT_ID,ImmutableList.of(NCLOC_METRIC_ID));
  assertThat(measureDtos).extracting("id").containsOnly(32L);
}
