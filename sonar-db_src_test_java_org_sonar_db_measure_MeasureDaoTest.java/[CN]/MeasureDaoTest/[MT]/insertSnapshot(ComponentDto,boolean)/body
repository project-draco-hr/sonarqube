{
  SnapshotDto snapshotDto=new SnapshotDto().setComponentUuid(componentDto.uuid()).setRootComponentUuid(componentDto.projectUuid()).setLast(last).setQualifier(componentDto.qualifier()).setScope(componentDto.scope());
  dbClient.snapshotDao().insert(dbSession,snapshotDto);
  dbSession.commit();
  return snapshotDto;
}
