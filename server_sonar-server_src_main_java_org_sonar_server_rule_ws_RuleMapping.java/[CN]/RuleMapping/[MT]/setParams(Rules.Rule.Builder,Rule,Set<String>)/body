{
  if (shouldReturnField(fieldsToReturn,RuleNormalizer.RuleField.PARAMS)) {
    ruleResponse.getParamsBuilder().addAllParams(from(ruleDoc.params()).transform(RuleParamToResponseRuleParam.INSTANCE).toList());
  }
}
