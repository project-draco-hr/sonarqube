{
  WSUtils utils=WSUtils.getINSTANCE();
  Object map=utils.parse(json);
  ServerSetup server=new ServerSetup().setStatus(utils.getString(map,"status")).setMessage(utils.getString(map,"msg"));
  return server;
}
