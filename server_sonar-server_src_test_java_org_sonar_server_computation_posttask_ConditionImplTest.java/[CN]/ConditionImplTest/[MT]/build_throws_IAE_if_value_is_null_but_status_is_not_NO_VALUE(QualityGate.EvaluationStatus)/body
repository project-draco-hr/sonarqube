{
  builder.setStatus(status).setValue(null);
  expectedException.expect(IllegalArgumentException.class);
  expectedException.expectMessage("value can not be null when status is not NO_VALUE");
  builder.build();
}
