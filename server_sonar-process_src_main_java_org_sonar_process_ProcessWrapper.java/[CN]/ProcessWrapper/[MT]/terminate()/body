{
  if (processMXBean != null) {
    processMXBean.terminate();
    try {
      Thread.sleep(10000L);
    }
 catch (    InterruptedException e) {
      LOGGER.warn("Could bnit",e);
    }
    this.interrupt();
  }
}
