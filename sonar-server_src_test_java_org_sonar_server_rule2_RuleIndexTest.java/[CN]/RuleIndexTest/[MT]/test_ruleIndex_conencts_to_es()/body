{
  RuleIndex ruleIndex=getRuleIndex();
  ruleIndex.connect();
  assertThat(node.client().admin().cluster().prepareClusterStats().get().getNodesStats().getCounts().getTotal()).isEqualTo(ruleIndex.getNodesStats().getCounts().getTotal());
  ruleIndex.stop();
}
