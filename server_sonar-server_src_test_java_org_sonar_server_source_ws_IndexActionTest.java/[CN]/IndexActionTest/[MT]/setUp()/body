{
  when(dbClient.componentDao()).thenReturn(componentDao);
  when(dbClient.openSession(false)).thenReturn(session);
  tester=new WsTester(new SourcesWs(mock(ShowAction.class),mock(RawAction.class),mock(LinesAction.class),mock(HashAction.class),new IndexAction(dbClient,sourceService)));
}
