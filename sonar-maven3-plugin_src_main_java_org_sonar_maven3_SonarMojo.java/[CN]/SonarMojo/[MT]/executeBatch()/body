{
  Batch batch=new Batch(getInitialConfiguration(),session,project,getLog(),lifecycleExecutor,artifactFactory,localRepository,artifactMetadataSource,artifactCollector,dependencyTreeBuilder,projectBuilder,Environment.MAVEN3,Maven3PluginExecutor.class);
  batch.execute();
}
