{
  NewController controller=context.createController("api/users").setSince("3.6").setDescription("Users management");
  for (  UsersWsAction action : actions) {
    action.define(controller);
  }
  controller.done();
}
