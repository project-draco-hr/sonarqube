{
  List<MeasureDto> measures=dbClient.measureDao().selectBySnapshotIdAndMetricKeys(snapshotDto.getId(),Collections.singleton(CoreMetrics.QUALITY_GATE_DETAILS_KEY),dbSession);
  return measures.isEmpty() ? null : measures.get(0).getData();
}
