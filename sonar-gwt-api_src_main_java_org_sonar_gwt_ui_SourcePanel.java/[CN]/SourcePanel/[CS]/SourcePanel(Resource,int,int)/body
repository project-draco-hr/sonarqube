{
  this.from=from;
  this.length=length;
  this.resource=resource;
  panel.add(loading);
  panel.getElement().setId("sourcePanel");
  initWidget(panel);
  setStyleName("gwt-SourcePanel");
  loadSources();
}
