{
  if (action.responseExample() != null) {
    response.newJsonWriter().beginObject().prop("format",action.responseExampleFormat()).prop("example",IOUtils.toString(action.responseExample(),Charsets.UTF_8)).endObject().close();
  }
 else {
    response.noContent();
  }
}
