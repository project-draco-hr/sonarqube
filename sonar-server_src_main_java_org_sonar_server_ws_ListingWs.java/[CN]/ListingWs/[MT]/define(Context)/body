{
  NewController controller=context.createController("api/webservices").setDescription("List web services");
  defineList(context,controller);
  defineResponseExample(context,controller);
  controller.done();
}
