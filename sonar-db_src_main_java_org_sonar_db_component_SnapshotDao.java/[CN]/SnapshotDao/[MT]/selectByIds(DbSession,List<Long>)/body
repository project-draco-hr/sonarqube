{
  return executeLargeInputs(snapshotIds,new Function<List<Long>,List<SnapshotDto>>(){
    @Nonnull @Override public List<SnapshotDto> apply(    @Nonnull List<Long> input){
      return mapper(dbSession).selectByIds(input);
    }
  }
);
}
