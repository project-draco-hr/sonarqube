{
  checkNotNull(metricDto.getId());
  checkNotNull(metricDto.getKey());
  checkNotNull(component.uuid());
  checkNotNull(snapshot.getUuid());
  return new MeasureDto().setMetricId(metricDto.getId()).setComponentUuid(component.uuid()).setAnalysisUuid(snapshot.getUuid());
}
