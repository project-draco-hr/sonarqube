{
  checkNotNull(metricDto.getId());
  checkNotNull(metricDto.getKey());
  checkNotNull(component.uuid());
  checkNotNull(analysis.getUuid());
  return new MeasureDto().setMetricId(metricDto.getId()).setComponentUuid(component.uuid()).setAnalysisUuid(analysis.getUuid());
}
