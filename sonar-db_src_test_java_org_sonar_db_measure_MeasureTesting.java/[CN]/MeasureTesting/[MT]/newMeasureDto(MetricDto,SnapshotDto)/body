{
  checkNotNull(metricDto.getId());
  checkNotNull(metricDto.getKey());
  checkNotNull(snapshot.getComponentUuid());
  return new MeasureDto().setMetricId(metricDto.getId()).setComponentUuid(snapshot.getComponentUuid()).setAnalysisUuid(snapshot.getUuid());
}
