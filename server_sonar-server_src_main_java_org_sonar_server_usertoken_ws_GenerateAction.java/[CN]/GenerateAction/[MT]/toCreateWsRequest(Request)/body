{
  GenerateWsRequest generateWsRequest=new GenerateWsRequest().setLogin(request.param(PARAM_LOGIN)).setName(request.mandatoryParam(PARAM_NAME));
  if (generateWsRequest.getLogin() == null) {
    generateWsRequest.setLogin(userSession.getLogin());
  }
  return generateWsRequest;
}
