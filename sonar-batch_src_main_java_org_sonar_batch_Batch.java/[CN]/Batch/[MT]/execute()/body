{
  MutablePicoContainer container=null;
  try {
    container=buildPicoContainer();
    container.start();
    analyzeModules(container);
  }
  finally {
    if (container != null) {
      container.stop();
    }
  }
}
