{
  try {
    return jsonBuilder().startObject().startObject(this.getType()).endObject().endObject();
  }
 catch (  IOException e) {
    LOG.error("Could not create mapping for {}",this.getIndexName());
    return null;
  }
}
