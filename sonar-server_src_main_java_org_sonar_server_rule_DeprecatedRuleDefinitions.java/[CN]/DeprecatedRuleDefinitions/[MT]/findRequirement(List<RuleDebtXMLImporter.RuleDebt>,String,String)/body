{
  return Iterables.find(requirements,new Predicate<RuleDebtXMLImporter.RuleDebt>(){
    @Override public boolean apply(    RuleDebtXMLImporter.RuleDebt input){
      return input.ruleKey().equals(RuleKey.of(repoKey,ruleKey));
    }
  }
,null);
}
