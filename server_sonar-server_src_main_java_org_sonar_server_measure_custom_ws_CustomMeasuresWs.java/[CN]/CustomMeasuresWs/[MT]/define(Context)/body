{
  NewController controller=context.createController(ENDPOINT).setDescription("Manage custom measures for a project. See also api/metrics.").setSince("5.2");
  for (  CustomMeasuresWsAction action : actions) {
    action.define(controller);
  }
  controller.done();
}
