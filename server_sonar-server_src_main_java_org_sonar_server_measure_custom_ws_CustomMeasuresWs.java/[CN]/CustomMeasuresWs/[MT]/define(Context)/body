{
  NewController controller=context.createController(ENDPOINT).setDescription("Custom measures management").setSince("5.2");
  for (  CustomMeasuresWsAction action : actions) {
    action.define(controller);
  }
  controller.done();
}
