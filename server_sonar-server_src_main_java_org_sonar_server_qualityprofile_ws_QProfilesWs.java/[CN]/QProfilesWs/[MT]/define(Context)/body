{
  NewController controller=context.createController(API_ENDPOINT).setDescription("Manage quality profiles.").setSince("4.4");
  ruleActivationActions.define(controller);
  bulkRuleActivationActions.define(controller);
  for (  QProfileWsAction action : actions) {
    action.define(controller);
  }
  controller.done();
}
