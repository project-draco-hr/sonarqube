{
  Notification notification=new Notification(MyNewIssuesNotification.TYPE).setFieldValue(SEVERITY + ".count","32").setFieldValue("projectName","Struts");
  EmailMessage message=sut.format(notification);
  assertThat(message.getMessage()).doesNotContain("See it");
}
