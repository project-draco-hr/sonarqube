{
  writeQualityGateCondition(qualityGates.createCondition(parseId(request,PARAM_GATE_ID),request.requiredParam(PARAM_METRIC),request.requiredParam(PARAM_OPERATOR),request.param(PARAM_WARNING),request.param(PARAM_ERROR),request.intParam(PARAM_PERIOD)),response.newJsonWriter()).close();
  ;
}
