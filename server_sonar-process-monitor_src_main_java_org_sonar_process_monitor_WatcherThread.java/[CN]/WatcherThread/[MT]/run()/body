{
  boolean stopped=false;
  while (!stopped) {
    try {
      processRef.getProcess().waitFor();
      processRef.stop();
      monitor.stopAsync();
      stopped=true;
    }
 catch (    InterruptedException ignored) {
    }
  }
}
