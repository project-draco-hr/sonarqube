{
  JsonUtils.requestJson(this.toString(),new JSONHandlerDispatcher<Resources>(callback){
    @Override public Resources parseResponse(    JavaScriptObject obj){
      return new Resources(parseResources(obj));
    }
  }
);
}
