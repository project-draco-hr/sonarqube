{
  return index.get(RuleIndex.class).search(new RuleQuery().setQProfileKey(key.toString()).setActivation(true).setStatuses(newArrayList(RuleStatus.DEPRECATED)),new QueryOptions().setLimit(0)).getTotal();
}
