{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  FileTest fileTest=(FileTest)o;
  if (!key.equals(fileTest.key)) {
    return false;
  }
  return true;
}
