{
  return newChildComponent(uuid,viewOrSubView).setKey(!path.equals("/") ? viewOrSubView.getKey() + ":" + path : viewOrSubView.getKey() + ":/").setName(path).setLongName(path).setPath(path).setScope(Scopes.PROJECT).setQualifier(Qualifiers.SUBVIEW);
}
