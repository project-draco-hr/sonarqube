{
  try {
    barrier.await();
    for (int i=0; i < 100; i++) {
      if (dao.acquire("my-lock",60 * 60 * 24).isLocked()) {
        locks.incrementAndGet();
      }
    }
    latch.countDown();
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}
