{
  DefaultCharacteristic characteristic=new DefaultCharacteristic().setId(1).setKey("NETWORK_USE").setName("Network use").setOrder(5).setParent(null).setRoot(null);
  assertThat(characteristic.isRoot()).isTrue();
}
