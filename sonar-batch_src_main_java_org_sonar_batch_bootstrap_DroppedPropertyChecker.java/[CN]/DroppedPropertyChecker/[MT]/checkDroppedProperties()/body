{
  for (  Map.Entry<String,String> entry : properties.entrySet()) {
    if (settings.hasKey(entry.getKey())) {
      logger.warn("Property '{}' (which value is '{}') is not supported any more. {}",entry.getKey(),settings.getString(entry.getKey()),entry.getValue());
    }
  }
}
