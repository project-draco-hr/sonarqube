{
  if (!onlyEnabled) {
    return children;
  }
  List<Characteristic> result=Lists.newArrayList();
  for (  Characteristic child : children) {
    if (child.getEnabled()) {
      result.add(child);
    }
  }
  return result;
}
