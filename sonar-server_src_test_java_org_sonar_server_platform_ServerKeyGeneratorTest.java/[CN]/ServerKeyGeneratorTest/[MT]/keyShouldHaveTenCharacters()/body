{
  ServerKeyGenerator.ServerKey key=new ServerKeyGenerator.ServerKey("SonarSource",localhost);
  assertThat(key.getKey().length(),Is.is(10));
  assertThat(StringUtils.isBlank(key.getKey()),Is.is(false));
}
