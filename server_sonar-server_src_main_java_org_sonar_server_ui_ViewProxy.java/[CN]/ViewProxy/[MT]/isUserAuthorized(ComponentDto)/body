{
  boolean authorized=userRoles.length == 0;
  for (  String userRole : getUserRoles()) {
    authorized|=(UserRole.VIEWER.equals(userRole) || UserRole.USER.equals(userRole) || UserSession.get().hasProjectPermissionByUuid(userRole,component.uuid()));
  }
  return authorized;
}
