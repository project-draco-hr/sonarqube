{
  DecimalColumnDef def=new DecimalColumnDef.Builder().setColumnName("issues").build();
  assertThat(def.getPrecision()).isEqualTo(38);
  assertThat(def.getScale()).isEqualTo(20);
  assertThat(def.isNullable()).isTrue();
}
