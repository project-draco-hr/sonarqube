{
  if (!connected) {
    try {
      startDatabaseConnectors(conf);
      connected=true;
    }
 catch (    Exception e) {
      LoggerFactory.getLogger(getClass()).error("Can not start Sonar",e);
    }
  }
}
