{
  if (!connected) {
    try {
      startDatabaseConnectors(servletContext);
      connected=true;
    }
 catch (    Exception e) {
      LoggerFactory.getLogger(getClass()).error("Can not start Sonar",e);
    }
  }
}
