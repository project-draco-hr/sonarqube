{
  useMssql();
  new FixMsSqlCollation.UpdateTableCollation(context,db,"rules").addClobColumn("description").execute();
  verify(context).execute(singletonList("ALTER TABLE rules ALTER COLUMN description NVARCHAR (MAX) COLLATE Latin1_General_CS_AS"));
}
