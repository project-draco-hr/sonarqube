{
  return newContext(new ComponentTreeBuilder(){
    @Override public Component build(    ComputationContext context){
      return new EventAndMeasureRepoComponent(context,Component.Type.PROJECT,1);
    }
  }
);
}
