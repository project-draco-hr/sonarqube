{
  SnapshotSourceDto dto=new SnapshotSourceDto().setId(1L).setSnapshotId(10L).setData("data");
  assertThat(dto.getId()).isEqualTo(1L);
  assertThat(dto.getSnapshotId()).isEqualTo(10L);
  assertThat(dto.getData()).isEqualTo("data");
}
