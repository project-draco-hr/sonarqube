{
  return wsClient.execute(newGetRequest(action("search")).setParam("qualifiers",request.getQualifiers()).setParam("p",request.getPage()).setParam("ps",request.getPageSize()).setParam("q",request.getQuery()),SearchWsResponse.parser());
}
