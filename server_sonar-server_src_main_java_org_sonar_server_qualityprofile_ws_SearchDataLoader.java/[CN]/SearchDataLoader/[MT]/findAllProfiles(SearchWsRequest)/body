{
  String language=request.getLanguage();
  List<QProfile> profiles=language != null ? profileLookup.profiles(language) : profileLookup.allProfiles();
  return from(profiles).filter(new IsLanguageKnown()).toList();
}
