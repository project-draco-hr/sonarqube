{
  File file=new File(baseDir,path);
  FileUtils.write(file,content);
  return new DefaultInputFile(path).setFile(file).setPathRelativeToSourceDir(sourceRelativePath).setLanguage(languageKey).setType(unitTest ? InputFile.Type.TEST : InputFile.Type.MAIN);
}
