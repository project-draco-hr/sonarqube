{
  File file=new File(baseDir,path);
  FileUtils.write(file,content);
  return new DeprecatedDefaultInputFile(path).setPathRelativeToSourceDir(sourceRelativePath).setFile(file).setLanguage(languageKey).setType(unitTest ? InputFile.Type.TEST : InputFile.Type.MAIN);
}
