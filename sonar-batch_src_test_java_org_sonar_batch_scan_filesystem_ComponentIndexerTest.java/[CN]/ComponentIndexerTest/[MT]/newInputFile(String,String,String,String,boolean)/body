{
  File file=new File(baseDir,path);
  FileUtils.write(file,content);
  return new DeprecatedDefaultInputFile("foo",path).setLanguage(languageKey).setType(unitTest ? InputFile.Type.TEST : InputFile.Type.MAIN);
}
