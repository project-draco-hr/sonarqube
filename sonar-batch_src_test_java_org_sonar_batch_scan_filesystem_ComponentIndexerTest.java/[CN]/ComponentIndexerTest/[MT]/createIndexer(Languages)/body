{
  SnapshotCache snapshotCache=new SnapshotCache();
  snapshotCache.put("myProject",mock(Snapshot.class));
  ComponentIndexer indexer=new ComponentIndexer(project,languages,sonarIndex,mock(ResourceKeyMigration.class),sourcePersister,new ProjectReferentials(),snapshotCache);
  return indexer;
}
