{
  String uuid1=insertSnapshot(40);
  insertSnapshot(50);
  insertDuplicationIndex(1,40);
  underTest.execute();
  verifyDuplicationsIndex(1,40,uuid1);
  underTest.execute();
  verifyDuplicationsIndex(1,40,uuid1);
}
