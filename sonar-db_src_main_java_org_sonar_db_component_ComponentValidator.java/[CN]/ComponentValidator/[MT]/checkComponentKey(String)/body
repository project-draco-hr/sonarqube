{
  checkArgument(key.length() <= MAX_COMPONENT_KEY_LENGTH,"Component key length (%s) is longer than the maximum authorized (%s). '%s' was provided.",key.length(),MAX_COMPONENT_KEY_LENGTH,key);
  return key;
}
