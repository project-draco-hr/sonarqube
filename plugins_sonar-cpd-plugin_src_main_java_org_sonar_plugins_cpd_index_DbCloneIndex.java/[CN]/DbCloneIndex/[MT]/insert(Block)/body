{
  CloneBlock dbBlock=new CloneBlock(currentSnapshotId,block.getBlockHash().toString(),block.getResourceId(),block.getIndexInFile(),block.getFirstLineNumber(),block.getLastLineNumber());
  session.save(dbBlock);
}
