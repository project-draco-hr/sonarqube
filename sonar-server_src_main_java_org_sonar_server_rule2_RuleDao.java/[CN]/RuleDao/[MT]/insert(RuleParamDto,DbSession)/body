{
  getMapper(session).insertParameter(param);
  session.enqueue(new EmbeddedIndexAction<RuleKey>(this.getIndexName(),IndexAction.Method.INSERT,param,this.selectById(param.getRuleId(),session).getKey()));
}
