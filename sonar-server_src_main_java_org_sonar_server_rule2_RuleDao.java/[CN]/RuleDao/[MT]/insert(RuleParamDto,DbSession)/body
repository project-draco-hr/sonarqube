{
  getMapper(session).insertParameter(param);
  RuleDto dto=this.selectById(param.getRuleId(),session);
  if (dto != null) {
    session.enqueue(new EmbeddedIndexAction<RuleKey>(this.getIndexType(),IndexAction.Method.INSERT,param,dto.getKey()));
  }
}
