{
  getMapper(session).deleteTag(tagToDelete.getId().intValue());
  session.enqueue(new EmbeddedIndexAction<RuleKey>(this.getIndexType(),IndexAction.Method.DELETE,tagToDelete,this.selectById(tagToDelete.getRuleId(),session).getKey()));
}
