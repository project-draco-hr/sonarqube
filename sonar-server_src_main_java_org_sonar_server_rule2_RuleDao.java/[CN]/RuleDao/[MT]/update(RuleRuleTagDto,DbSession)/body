{
  getMapper(session).updateTag(existingTag);
  session.enqueue(new EmbeddedIndexAction<RuleKey>(this.getIndexType(),IndexAction.Method.UPDATE,existingTag,this.selectById(existingTag.getRuleId(),session).getKey()));
}
