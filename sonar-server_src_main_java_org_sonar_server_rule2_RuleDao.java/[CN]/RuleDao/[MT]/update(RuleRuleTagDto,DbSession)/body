{
  getMapper(session).updateTag(existingTag);
  session.enqueue(new EmbeddedIndexAction<RuleKey>(this.getIndexName(),IndexAction.Method.UPDATE,existingTag,this.selectById(existingTag.getRuleId(),session).getKey()));
}
