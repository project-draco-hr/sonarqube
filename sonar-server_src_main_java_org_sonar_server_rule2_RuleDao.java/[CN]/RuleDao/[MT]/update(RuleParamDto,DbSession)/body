{
  getMapper(session).updateParameter(param);
  RuleDto dto=this.selectById(param.getRuleId(),session);
  if (dto != null) {
    session.enqueue(new EmbeddedIndexAction<RuleKey>(this.getIndexType(),IndexAction.Method.UPDATE,param,dto.getKey()));
  }
}
