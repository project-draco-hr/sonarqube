{
  getMapper(session).updateParameter(param);
  session.enqueue(new EmbeddedIndexAction<RuleKey>(this.getIndexName(),IndexAction.Method.UPDATE,param,this.selectById(param.getRuleId(),session).getKey()));
}
