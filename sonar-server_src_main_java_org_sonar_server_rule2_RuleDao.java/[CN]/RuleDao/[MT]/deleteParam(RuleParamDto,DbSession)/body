{
  getMapper(session).deleteParameter(persistedParam.getId());
  session.enqueue(new EmbeddedIndexAction<RuleKey>(this.getIndexName(),IndexAction.Method.DELETE,persistedParam,this.selectById(persistedParam.getRuleId(),session).getKey()));
}
