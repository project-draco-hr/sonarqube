{
  setupData("shared");
  ActiveRuleDto dto=new ActiveRuleDto().setId(1).setProfileId(1).setRuleId(10L).setSeverity(4).setInheritance(null).setNoteData("text");
  dao.update(dto);
  checkTables("update","active_rules");
}
