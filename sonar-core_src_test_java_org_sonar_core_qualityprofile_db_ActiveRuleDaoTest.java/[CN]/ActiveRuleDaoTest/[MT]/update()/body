{
  setupData("shared");
  ActiveRuleDto dto=new ActiveRuleDto().setId(1).setProfileId(1).setRuleId(10).setSeverity(Severity.BLOCKER).setInheritance(null);
  dao.update(dto);
  checkTables("update","active_rules");
}
