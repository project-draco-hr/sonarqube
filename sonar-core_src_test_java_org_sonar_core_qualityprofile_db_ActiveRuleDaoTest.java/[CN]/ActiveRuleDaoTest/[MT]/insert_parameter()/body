{
  setupData("empty");
  DbSession session=getMyBatis().openSession(false);
  ActiveRuleParamDto dto=new ActiveRuleParamDto().setActiveRuleId(1).setRulesParameterId(1).setKey("max").setValue("20");
  dao.insert(dto,session);
  session.commit();
  session.close();
  checkTables("insert_parameter","active_rule_parameters");
}
