{
  File file=new File(baseDir,path);
  String deprecatedEffectiveKey=module.getKey() + ":" + deprecatedKey;
  return new DeprecatedDefaultInputFile(module.getKey(),path).setDeprecatedKey(deprecatedEffectiveKey).setFile(file).setType(isTest ? InputFile.Type.TEST : InputFile.Type.MAIN);
}
