{
  String result=ws.newRequest().setParam("permission","scan").setParam("selected",SelectionMode.ALL.value()).execute().getInput();
  assertThat(result).containsSequence(DefaultGroups.ANYONE,"group-1","group-2","group-3");
}
