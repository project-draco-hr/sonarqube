{
  measureClasses.setVariation4(40d);
  when(profile.getAlerts()).thenReturn(Arrays.asList(new Alert(null,CoreMetrics.CLASSES,Alert.OPERATOR_GREATER,null,"30",4)));
  decorator.decorate(project,context);
}
