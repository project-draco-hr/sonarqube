{
  measureClasses.setVariation4(40d);
  when(projectAlerts.all()).thenReturn(Arrays.asList(new Alert(null,CoreMetrics.CLASSES,Alert.OPERATOR_GREATER,null,"30",4)));
  decorator.decorate(project,context);
}
