{
  try {
    jarsInstaller.install();
    fail();
  }
 catch (  MessageException e) {
    assertThat(e.getMessage()).contains("Found two files for the same plugin 'foo'").contains("foo-plugin1.jar").contains("foo-plugin2.jar");
  }
}
