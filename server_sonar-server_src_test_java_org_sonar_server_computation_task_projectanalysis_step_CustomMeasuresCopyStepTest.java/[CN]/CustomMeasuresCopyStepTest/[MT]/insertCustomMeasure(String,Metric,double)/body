{
  dbTester.getDbClient().customMeasureDao().insert(dbTester.getSession(),CustomMeasureTesting.newCustomMeasureDto().setComponentUuid(componentUuid).setMetricId(metric.getId()).setValue(value));
  dbTester.getSession().commit();
}
