{
  ServletFilter.UrlPattern pattern=ServletFilter.UrlPattern.builder().setExcludePatterns("/*").build();
  assertThat(pattern.matches("/")).isFalse();
  assertThat(pattern.matches("/foo/ooo")).isFalse();
}
