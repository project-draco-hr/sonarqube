{
  db.prepareDbUnit(getClass(),"snapshots.xml");
  ComputationContext context=new ComputationContext(AnalysisReportDto.newForTests(1L).setSnapshotId(1L),ComponentTesting.newProjectDto());
  sut.execute(context);
  db.assertDbUnit(getClass(),"snapshots-result.xml","snapshots");
}
