{
  db.prepareDbUnit(getClass(),"empty.xml");
  ComputationContext context=new ComputationContext(AnalysisReportDto.newForTests(1L).setSnapshotId(1L),ComponentTesting.newProjectDto(),mock(BatchOutputReader.class));
  sut.execute(context);
}
