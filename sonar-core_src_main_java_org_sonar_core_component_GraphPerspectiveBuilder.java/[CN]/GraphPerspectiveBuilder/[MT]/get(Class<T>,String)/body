{
  ComponentVertex vertex=graph.getComponent(componentKey);
  if (vertex != null) {
    T perspective=perspectiveLoader.load(vertex);
    if (perspective != null) {
      return perspective;
    }
  }
  return null;
}
