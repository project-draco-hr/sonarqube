{
  MeasureComputer measureComputer=new MeasureComputerImpl.MeasureComputerBuilderImpl().setInputMetrics().setOutputMetrics("comment_density_1","comment_density_2").setImplementation(DEFAULT_MEASURE_COMPUTER_IMPLEMENTATION).build();
  assertThat(measureComputer.getInputMetrics()).isEmpty();
}
