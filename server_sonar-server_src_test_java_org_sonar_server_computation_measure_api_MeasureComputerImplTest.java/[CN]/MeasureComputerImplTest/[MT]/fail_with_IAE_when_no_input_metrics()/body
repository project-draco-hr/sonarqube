{
  thrown.expect(IllegalArgumentException.class);
  thrown.expectMessage("At least one input metric must be defined");
  new MeasureComputerImpl.MeasureComputerBuilderImpl().setOutputMetrics("comment_density_1","comment_density_2").setImplementation(DEFAULT_MEASURE_COMPUTER_IMPLEMENTATION).build();
}
