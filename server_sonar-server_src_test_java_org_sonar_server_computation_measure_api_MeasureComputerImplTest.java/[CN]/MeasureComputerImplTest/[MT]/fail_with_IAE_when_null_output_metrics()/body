{
  thrown.expect(IllegalArgumentException.class);
  thrown.expectMessage("At least one output metric must be defined");
  new MeasureComputerImpl.MeasureComputerBuilderImpl().setInputMetrics("ncloc","comment").setOutputMetrics(null).setImplementation(DEFAULT_MEASURE_COMPUTER_IMPLEMENTATION);
}
