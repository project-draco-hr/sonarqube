{
  return Iterables.find(characteristicsByKey.values(),new Predicate<DebtCharacteristic>(){
    @Override public boolean apply(    DebtCharacteristic input){
      return id.equals(((DefaultDebtCharacteristic)input).id());
    }
  }
);
}
