{
  for (int i=0; i < 9; i++) {
    db.executeInsert("resource_index","KEE","key_" + i,"POSITION",valueOf(i),"NAME_SIZE",valueOf(i + 1),"RESOURCE_ID",valueOf(i + 10),"ROOT_PROJECT_ID",valueOf(i + 20),"QUALIFIER",(i % 2 == 0 ? "FILE" : "PROJECT"));
  }
  db.commit();
  underTest.execute();
  verifyAddedColumns();
}
