{
  json.name("dashboards").beginArray();
  for (  DashboardDto dashboard : dashboards) {
    json.beginObject().prop("key",dashboard.getId()).prop("name",dashboard.getName()).endObject();
  }
  json.endArray();
}
