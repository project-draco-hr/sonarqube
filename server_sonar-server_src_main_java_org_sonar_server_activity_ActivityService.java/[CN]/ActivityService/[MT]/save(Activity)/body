{
  ActivityDto dto=new ActivityDto().setKey(Uuids.create()).setAuthor(userSession.getLogin()).setAction(activity.getAction()).setMessage(activity.getMessage()).setData(KeyValueFormat.format(activity.getData())).setType(activity.getType().name());
  dbClient.activityDao().insert(dto);
  indexer.index();
}
