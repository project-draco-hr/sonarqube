{
  when(system2.now()).thenReturn(1418215735482L);
  createDefaultGroup();
  userUpdater.create(NewUser.create().setLogin("user").setName("User").setPassword("password").setPasswordConfirmation("password"));
  assertThat(userDao.selectNullableByLogin(session,"user")).isNotNull();
}
