{
  request.acceptGzipEncoding().uncompress(true).acceptJson().acceptCharset(HttpRequest.CHARSET_UTF8).connectTimeout(CONNECT_TIMEOUT_MILLISECONDS).readTimeout(READ_TIMEOUT_MILLISECONDS).trustAllCerts().trustAllCerts();
  if (login != null) {
    request.basic(login,password);
  }
  return request;
}
