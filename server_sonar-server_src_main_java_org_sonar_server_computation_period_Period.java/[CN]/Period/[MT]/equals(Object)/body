{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  Period period=(Period)o;
  return index == period.index && snapshotDate == period.snapshotDate && snapshotId == period.snapshotId && mode.equals(period.mode) && java.util.Objects.equals(modeParameter,period.modeParameter);
}
