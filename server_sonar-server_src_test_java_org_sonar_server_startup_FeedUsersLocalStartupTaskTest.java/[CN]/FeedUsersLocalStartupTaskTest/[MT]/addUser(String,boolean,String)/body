{
  UserDto user=UserTesting.newUserDto().setLogin(login).setActive(true).setLocal(local).setExternalIdentityProvider(externalIdentityProvider).setExternalIdentity(login).setCreatedAt(PAST).setUpdatedAt(PAST);
  userDao.insert(dbSession,user);
  dbSession.commit();
  return user;
}
