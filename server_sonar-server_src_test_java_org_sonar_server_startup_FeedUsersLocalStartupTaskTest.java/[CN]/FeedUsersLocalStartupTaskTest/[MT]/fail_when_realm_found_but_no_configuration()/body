{
  initTask(createRealm("REALM1"),createRealm("REALM2"));
  expectedException.expect(MessageException.class);
  expectedException.expectMessage("External authentication plugin [REALM1, REALM2] has been found, but no related configuration has been set. " + "Either update your configuration or remove the plugin");
  underTest.start();
}
