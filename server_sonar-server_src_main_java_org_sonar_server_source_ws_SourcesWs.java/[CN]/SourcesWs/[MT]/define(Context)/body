{
  NewController controller=context.createController("api/sources").setSince("4.2").setDescription("Display sources information");
  showAction.define(controller);
  show2Action.define(controller);
  rawAction.define(controller);
  scmAction.define(controller);
  controller.done();
}
