{
  loadBasicDataInDatabase();
  sut.delete(session,new ComponentDto().setId(1L).setKey("org.struts:struts-core:src/org/struts/RequestContext.java"));
  session.commit();
  db.assertDbUnit(getClass(),"delete-result.xml","projects");
}
