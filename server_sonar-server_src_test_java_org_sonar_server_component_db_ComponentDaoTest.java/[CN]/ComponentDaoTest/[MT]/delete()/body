{
  setupData("shared");
  dao.delete(session,new ComponentDto().setId(1L).setKey("org.struts:struts-core:src/org/struts/RequestContext.java"));
  session.commit();
  checkTable("delete","projects");
}
