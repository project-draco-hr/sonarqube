{
  return Iterables.find(params,new Predicate<NewRuleParam>(){
    @Override public boolean apply(    @Nullable NewRuleParam input){
      return input != null && input.key().equals(paramKey);
    }
  }
,null);
}
