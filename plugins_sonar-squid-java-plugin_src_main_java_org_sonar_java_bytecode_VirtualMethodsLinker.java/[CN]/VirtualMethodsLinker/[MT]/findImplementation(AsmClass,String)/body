{
  AsmMethod method=asmClass.getMethod(methodKey);
  if (method != null && method.isBodyLoaded()) {
    return method;
  }
  if (asmClass.getSuperClass() != null) {
    return findImplementation(asmClass.getSuperClass(),methodKey);
  }
  return null;
}
