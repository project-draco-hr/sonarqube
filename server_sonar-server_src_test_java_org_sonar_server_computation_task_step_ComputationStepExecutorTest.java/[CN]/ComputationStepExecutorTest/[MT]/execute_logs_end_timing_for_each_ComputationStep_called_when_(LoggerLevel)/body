{
  try (ChangeLogLevel executor=new ChangeLogLevel(ComputationStepExecutor.class,level);ChangeLogLevel step1=new ChangeLogLevel(computationStep1.getClass(),level);ChangeLogLevel step2=new ChangeLogLevel(computationStep2.getClass(),level)){
    new ComputationStepExecutor(mockComputationSteps(computationStep1,computationStep2)).execute();
    return logTester.logs(LoggerLevel.DEBUG);
  }
 }
