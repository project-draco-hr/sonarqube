{
  return ActionPlanStats.create(name).setKey(kee).setDescription(description).setStatus(status).setDeadLine(deadLine).setUserLogin(userLogin).setCreationDate(createdAt).setUpdateDate(updatedAt).setTotalIssues(totalIssues).setOpenIssues(openIssues);
}
