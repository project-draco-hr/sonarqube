{
  return ActionPlanStats.create(name).setKey(kee).setProjectKey(projectKey).setDescription(description).setStatus(status).setDeadLine(deadLine).setUserLogin(userLogin).setCreatedAt(createdAt).setUpdatedAt(updatedAt).setTotalIssues(totalIssues).setOpenIssues(openIssues);
}
