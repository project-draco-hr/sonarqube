{
  Settings.Value.Builder valueBuilder=valuesWsBuilder.addValuesBuilder().setKey(definition.key()).setIsDefault(true);
  setValue(valueBuilder,definition.defaultValue());
  valueBuilderByKey.put(definition.key(),valueBuilder);
}
