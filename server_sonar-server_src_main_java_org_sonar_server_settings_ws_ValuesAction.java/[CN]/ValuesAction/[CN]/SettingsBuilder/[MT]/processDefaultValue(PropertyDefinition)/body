{
  Settings.Setting.Builder settingBuilder=valuesWsBuilder.addSettingsBuilder().setKey(definition.key()).setInherited(false).setDefault(true);
  setValue(settingBuilder,definition.defaultValue());
  settingsBuilderByKey.put(definition.key(),settingBuilder);
}
