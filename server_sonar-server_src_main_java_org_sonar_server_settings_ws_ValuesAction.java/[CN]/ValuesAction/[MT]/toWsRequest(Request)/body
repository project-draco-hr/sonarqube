{
  return ValuesRequest.builder().setKeys(request.mandatoryParamAsStrings(PARAM_KEYS)).setComponentId(request.param(PARAM_COMPONENT_ID)).setComponentKey(request.param(PARAM_COMPONENT_KEY)).build();
}
