{
  List<FileSourceDb.Test> tests=Arrays.asList(FileSourceDb.Test.newBuilder().setName("name#1").build(),FileSourceDb.Test.newBuilder().setName("name#2").build());
  FileSourceDto sut=new FileSourceDto().setTestData(tests);
  assertThat(sut.getTestData()).hasSize(2);
  assertThat(sut.getTestData().get(0).getName()).isEqualTo("name#1");
}
