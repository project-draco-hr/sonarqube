{
  Graph sub=SubGraph.extract(a,"uses",Direction.IN,"implements",Direction.OUT);
  assertThat(sub.getVertices()).hasSize(1);
  assertThat(sub.getVertex(a.getId())).isNotNull();
  assertThat(sub.getEdges()).isEmpty();
}
