{
  LoggerFactory.getLogger(getClass()).debug("Purging snapshots");
  PurgeSnapshotQuery query=PurgeSnapshotQuery.create().setBeforeBuildDate(context.getBeforeBuildDate()).setRootProjectId(context.getRootProjectId());
  purgeDao.purgeSnapshots(query);
}
