{
  if (update.isEmpty()) {
    return false;
  }
  DbSession dbSession=dbClient.openSession(false);
  try {
    return update(dbSession,update,userSession);
  }
  finally {
    dbSession.close();
  }
}
