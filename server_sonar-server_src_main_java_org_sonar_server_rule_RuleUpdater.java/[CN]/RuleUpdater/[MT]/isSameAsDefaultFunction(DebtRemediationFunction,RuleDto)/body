{
  return new EqualsBuilder().append(fn.type().name(),rule.getDefaultRemediationFunction()).append(fn.coefficient(),rule.getDefaultRemediationGapMultiplier()).append(fn.offset(),rule.getDefaultRemediationBaseEffort()).isEquals();
}
