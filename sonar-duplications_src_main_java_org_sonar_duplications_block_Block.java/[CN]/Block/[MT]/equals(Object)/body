{
  if (!(obj instanceof Block)) {
    return false;
  }
  Block other=(Block)obj;
  return resourceId.equals(other.resourceId) && blockHash.equals(other.blockHash) && indexInFile == other.indexInFile && firstLineNumber == other.firstLineNumber && lastLineNumber == other.lastLineNumber;
}
