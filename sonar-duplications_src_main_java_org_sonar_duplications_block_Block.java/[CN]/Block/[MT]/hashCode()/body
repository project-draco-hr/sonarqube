{
  int h=hash;
  if (h == 0) {
    h=resourceId.hashCode();
    h=31 * h + blockHash.hashCode();
    h=31 * h + indexInFile;
    h=31 * h + firstLineNumber;
    h=31 * h + lastLineNumber;
    hash=h;
  }
  return h;
}
