{
  int port=settings.getInt(IndexProperties.NODE_PORT);
  client=new TransportClient(esSettings).addTransportAddress(new InetSocketTransportAddress("localhost",port));
  LOG.info("Elasticsearch port: " + port);
}
