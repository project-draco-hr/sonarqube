{
  Notification notification=new Notification("review-changed").setFieldValue("reviewId",String.valueOf(reviewId)).setFieldValue("author",author).setFieldValue("creator",creator).setFieldValue("assignee",assignee).setFieldValue("old.comment",oldComment).setFieldValue("new.comment",newComment);
  notificationManager.scheduleForSending(notification);
}
