{
  if (ExtensionUtils.supportsEnvironment(extension,env) && matcher.accept(extension)) {
    container.addExtension(pluginInfo,extension);
  }
 else {
    container.declareExtension(pluginInfo,extension);
  }
}
