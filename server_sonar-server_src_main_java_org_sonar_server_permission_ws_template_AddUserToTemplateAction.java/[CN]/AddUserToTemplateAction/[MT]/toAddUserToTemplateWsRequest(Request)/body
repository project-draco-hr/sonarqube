{
  return new AddUserToTemplateWsRequest().setLogin(request.mandatoryParam(PARAM_USER_LOGIN)).setPermission(request.mandatoryParam(PARAM_PERMISSION)).setTemplateId(request.param(PARAM_TEMPLATE_ID)).setTemplateName(request.param(PARAM_TEMPLATE_NAME));
}
