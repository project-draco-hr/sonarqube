{
  return ImmutableList.of(GitScmProvider.class,GitBlameCommand.class,JGitBlameCommand.class,PropertyDefinition.builder(GIT_IMPLEMENTATION_PROP_KEY).name("Git implementation").description("By default pure Java implementation is used. You can force use of command line git executable in case of issue.").defaultValue(JGIT).type(PropertyType.SINGLE_SELECT_LIST).options(EXE,JGIT).category(CoreProperties.CATEGORY_SCM).subCategory(CATEGORY_GIT).build());
}
