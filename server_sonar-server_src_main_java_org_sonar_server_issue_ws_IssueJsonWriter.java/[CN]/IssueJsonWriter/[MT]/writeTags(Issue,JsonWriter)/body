{
  Collection<String> tags=issue.tags();
  if (tags != null && !tags.isEmpty()) {
    json.name(FIELD_TAGS).beginArray();
    for (    String tag : tags) {
      json.value(tag);
    }
    json.endArray();
  }
}
