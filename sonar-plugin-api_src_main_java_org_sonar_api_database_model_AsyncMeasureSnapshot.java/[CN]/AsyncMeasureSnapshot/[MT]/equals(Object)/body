{
  if (!(obj instanceof AsyncMeasureSnapshot)) {
    return false;
  }
  if (this == obj) {
    return true;
  }
  AsyncMeasureSnapshot other=(AsyncMeasureSnapshot)obj;
  return new EqualsBuilder().append(measureId,other.getMeasureId()).append(measureDate,other.getMeasureDate()).append(snapshotId,other.getSnapshotId()).append(snapshotDate,other.getSnapshotDate()).isEquals();
}
