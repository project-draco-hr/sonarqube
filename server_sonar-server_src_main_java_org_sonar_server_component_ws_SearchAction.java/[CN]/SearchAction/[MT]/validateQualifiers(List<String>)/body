{
  Set<String> possibleQualifiers=allQualifiers();
  for (  String qualifier : qualifiers) {
    checkRequest(possibleQualifiers.contains(qualifier),format("The '%s' parameter must be one of %s. '%s' was passed.",PARAM_QUALIFIER,possibleQualifiers,qualifier));
  }
}
