{
  int total=dbClient.componentDao().countByQuery(dbSession,query);
  return Paging.forPageIndex(wsRequest.mandatoryParamAsInt(Param.PAGE)).withPageSize(wsRequest.mandatoryParamAsInt(Param.PAGE_SIZE)).andTotal(total);
}
