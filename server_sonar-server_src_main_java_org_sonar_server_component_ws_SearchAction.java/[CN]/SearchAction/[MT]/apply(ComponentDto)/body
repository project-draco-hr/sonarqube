{
  WsComponents.Component.Builder builder=WsComponents.Component.newBuilder().setId(dto.uuid()).setKey(dto.key()).setName(dto.name()).setQualifier(dto.qualifier());
  if (dto.language() != null) {
    builder.setLanguage(dto.language());
  }
  return builder.build();
}
