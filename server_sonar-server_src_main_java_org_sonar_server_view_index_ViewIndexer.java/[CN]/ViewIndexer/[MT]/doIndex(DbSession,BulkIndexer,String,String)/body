{
  List<String> projects=dbClient.componentDao().selectProjectsFromView(dbSession,projectUuid,uuid);
  bulk.add(newUpsertRequest(new ViewDoc(Maps.<String,Object>newHashMap()).setUuid(uuid).setProjects(projects)));
}
