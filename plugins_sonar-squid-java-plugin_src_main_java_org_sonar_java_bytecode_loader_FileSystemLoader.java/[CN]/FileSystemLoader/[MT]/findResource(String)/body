{
  File file=new File(baseDir,name);
  if (file.exists() && file.isFile()) {
    try {
      return file.toURL();
    }
 catch (    MalformedURLException e) {
      return null;
    }
  }
  return null;
}
