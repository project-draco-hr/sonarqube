{
  ReportQueue.Item item=null;
  try {
    item=getComponentByType(ReportQueue.Item.class);
    stopComponents();
  }
 catch (  Throwable t) {
    Loggers.get(ComputeEngineContainerImpl.class).error(String.format("Cleanup of container for item '%s' failed",item == null ? null : item.dto),t);
  }
}
