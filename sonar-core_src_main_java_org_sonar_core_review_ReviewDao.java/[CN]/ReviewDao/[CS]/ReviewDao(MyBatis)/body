{
  this.mybatis=mybatis;
  this.cacheByResource=CacheBuilder.newBuilder().weakValues().build(new CacheLoader<Long,Collection<ReviewDto>>(){
    @Override public Collection<ReviewDto> load(    Long resourceId){
      return doSelectOpenByResourceId(resourceId);
    }
  }
);
}
