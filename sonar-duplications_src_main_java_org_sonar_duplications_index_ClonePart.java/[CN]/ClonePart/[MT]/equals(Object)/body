{
  if (obj instanceof ClonePart) {
    ClonePart another=(ClonePart)obj;
    return another.resourceId.equals(resourceId) && another.lineStart == lineStart && another.lineEnd == lineEnd && another.unitStart == unitStart;
  }
  return false;
}
