{
  Component view=ViewsComponent.builder(VIEW,ROOT_REF).addChildren(ViewsComponent.builder(SUBVIEW,INTERMEDIATE_1_REF).addChildren(ViewsComponent.builder(SUBVIEW,INTERMEDIATE_2_REF).addChildren(ViewsComponent.builder(PROJECT_VIEW,LEAF_REF).build()).build()).build()).build();
  treeRootHolder.setRoot(view);
  setupDbIds();
}
