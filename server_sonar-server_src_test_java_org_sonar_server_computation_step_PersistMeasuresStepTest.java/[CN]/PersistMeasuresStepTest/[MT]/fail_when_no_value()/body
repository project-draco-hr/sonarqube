{
  BatchReport.Measure measure=BatchReport.Measure.newBuilder().setMetricKey("repo:metric-key").build();
  BatchReport.Component component=defaultComponent().build();
  sut.toMeasureDto(measure,component);
}
