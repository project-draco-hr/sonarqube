{
  Component project=ReportComponent.builder(PROJECT,ROOT_REF).addChildren(ReportComponent.builder(MODULE,INTERMEDIATE_1_REF).addChildren(ReportComponent.builder(DIRECTORY,INTERMEDIATE_2_REF).addChildren(ReportComponent.builder(FILE,LEAF_REF).build()).build()).build()).build();
  treeRootHolder.setRoot(project);
  setupDbIds();
}
