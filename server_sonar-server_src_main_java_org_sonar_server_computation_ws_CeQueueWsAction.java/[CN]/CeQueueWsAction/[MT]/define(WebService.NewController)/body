{
  WebService.NewAction action=controller.createAction("queue").setDescription("Gets the tasks of the Compute Engine queue").setInternal(true).setResponseExample(getClass().getResource("CeQueueWsAction/example.json")).setHandler(this);
  action.createParam(PARAM_COMPONENT_ID);
}
