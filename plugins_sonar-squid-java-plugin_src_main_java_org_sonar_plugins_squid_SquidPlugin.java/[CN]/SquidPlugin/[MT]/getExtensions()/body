{
  List list=new ArrayList();
  list.add(SquidSearchProxy.class);
  list.add(SquidSensor.class);
  list.add(CheckTemplateRepository.createFromAnnotatedClasses(CoreProperties.SQUID_PLUGIN,Java.INSTANCE,BytecodeChecks.getCheckClasses()));
  return list;
}
