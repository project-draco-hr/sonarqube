{
  Metric metric=new Metric.Builder("foo",Metric.ValueType.INT).setDomain("my domain").setName("Foo").create();
  assertThat(metric.getKey(),is("foo"));
  assertThat(metric.getName(),is("Foo"));
  assertThat(metric.getDomain(),is("my domain"));
}
