{
  Batch batch=new Batch(getInitialConfiguration(),session,project,getLog(),lifecycleExecutor,pluginManager,artifactFactory,localRepository,artifactMetadataSource,artifactCollector,dependencyTreeBuilder,projectBuilder,Environment.MAVEN2,Maven2PluginExecutor.class);
  batch.execute();
}
