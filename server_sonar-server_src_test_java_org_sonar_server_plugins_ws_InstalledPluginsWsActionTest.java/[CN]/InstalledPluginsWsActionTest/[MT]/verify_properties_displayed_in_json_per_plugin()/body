{
  when(pluginRepository.getMetadata()).thenReturn(of(plugin("plugKey","plugName",10)));
  underTest.handle(request,response);
  assertJson(response.outputAsString()).isSimilarTo("[" + "{" + "  \"key\": \"plugKey\","+ "  \"name\": \"plugName\","+ "  \"version\": \"10\""+ "}"+ "]");
}
