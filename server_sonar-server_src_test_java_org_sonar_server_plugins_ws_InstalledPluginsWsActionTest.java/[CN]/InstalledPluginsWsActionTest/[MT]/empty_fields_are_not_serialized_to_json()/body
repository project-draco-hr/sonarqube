{
  when(pluginRepository.getMetadata()).thenReturn(of((PluginMetadata)DefaultPluginMetadata.create("").setName("").setCore(false)));
  underTest.handle(request,response);
  assertThat(response.outputAsString()).doesNotContain("name").doesNotContain("key");
}
