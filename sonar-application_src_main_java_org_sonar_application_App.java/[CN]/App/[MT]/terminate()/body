{
  if (monitor != null) {
    monitor.interrupt();
    monitor=null;
    if (elasticsearch != null) {
      elasticsearch.terminate();
    }
    if (server != null) {
      server.terminate();
    }
    LOGGER.info("Stopping SonarQube main process");
  }
}
