{
  if (monitor != null && monitor.isAlive()) {
    monitor.terminate();
    monitor=null;
  }
  if (server != null) {
    server.terminate();
    server=null;
  }
  if (elasticsearch != null) {
    elasticsearch.terminate();
    elasticsearch=null;
  }
}
