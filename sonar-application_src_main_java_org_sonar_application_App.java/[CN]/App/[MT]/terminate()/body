{
  LoggerFactory.getLogger(App.class).info("Stopping");
  if (monitor.isAlive()) {
    monitor.terminate();
    monitor.interrupt();
  }
  if (server != null) {
    server.terminate();
  }
  if (elasticsearch != null) {
    elasticsearch.terminate();
  }
}
