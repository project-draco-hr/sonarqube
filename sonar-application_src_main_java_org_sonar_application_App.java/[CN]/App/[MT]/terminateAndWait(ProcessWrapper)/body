{
  if (process != null) {
    process.terminate();
    try {
      process.join();
    }
 catch (    InterruptedException e) {
      LOGGER.warn("Process '{}' did not gracefully shutdown.",process.getName());
    }
  }
}
