{
  int columnPosition=code.getColumnPosition();
  if (code.peek() == '\n') {
    return false;
  }
  if (columnPosition < 2 || columnPosition > 7) {
    code.pop();
    return true;
  }
  return false;
}
