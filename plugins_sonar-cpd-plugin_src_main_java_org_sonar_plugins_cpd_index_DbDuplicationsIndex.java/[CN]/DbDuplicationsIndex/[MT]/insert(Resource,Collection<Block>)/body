{
  int resourceSnapshotId=getSnapshotIdFor(resource);
  for (  Block block : blocks) {
    DuplicationBlock dbBlock=new DuplicationBlock(currentProjectSnapshotId,resourceSnapshotId,block.getBlockHash().toString(),block.getIndexInFile(),block.getFirstLineNumber(),block.getLastLineNumber());
    session.save(dbBlock);
  }
  session.commit();
}
