{
  User user=new User().setEmail("super@m.an").setLogin("superman").setName("Superman");
  assertThat(user.toString(),containsString("super@m.an"));
  assertThat(user.toString(),containsString("superman"));
  assertThat(user.toString(),containsString("Superman"));
}
