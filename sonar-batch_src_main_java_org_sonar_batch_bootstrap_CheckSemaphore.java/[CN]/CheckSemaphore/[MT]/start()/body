{
  if (!isInDryRunMode()) {
    Lock lock=acquire();
    if (!lock.isAcquired()) {
      LOG.error(getErrorMessage(lock));
      throw new SonarException("The project is already been analysing.");
    }
  }
}
