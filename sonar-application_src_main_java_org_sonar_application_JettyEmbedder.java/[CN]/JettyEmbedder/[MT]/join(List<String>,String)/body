{
  StringBuilder sb=new StringBuilder();
  boolean first=true;
  for (  String path : paths) {
    if (!first) {
      sb.append(separator);
    }
    sb.append(path);
    first=false;
  }
  return sb.toString();
}
