{
  return new PersisterExecutionHandler.PersisterExecutionEvent(){
    @Override public boolean isStart(){
      return start;
    }
    @Override public boolean isEnd(){
      return !start;
    }
    @Override public ScanPersister getPersister(){
      return persister;
    }
  }
;
}
