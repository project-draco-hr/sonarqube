{
  return new PersistersPhaseHandler.PersistersPhaseEvent(){
    @Override public boolean isStart(){
      return start;
    }
    @Override public boolean isEnd(){
      return !start;
    }
    @Override public List<ScanPersister> getPersisters(){
      return null;
    }
  }
;
}
