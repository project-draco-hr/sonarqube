{
  List<PropertyFieldDef> definitions=newArrayList();
  for (  PropertyField field : fields) {
    definitions.add(PropertyFieldDef.build(field.key()).name(field.name()).description(field.description()).indicativeSize(field.indicativeSize()).type(field.type()).options(field.options()).build());
  }
  return definitions;
}
