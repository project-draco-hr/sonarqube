{
  GetRequestBuilder request=esClient.prepareGet().setType(UserIndexDefinition.INDEX).setIndex(UserIndexDefinition.TYPE_USER).setId(login).setFetchSource(true).setRouting(login);
  GetResponse response=request.get();
  if (response.isExists()) {
    return new UserDoc(response.getSource());
  }
  return null;
}
