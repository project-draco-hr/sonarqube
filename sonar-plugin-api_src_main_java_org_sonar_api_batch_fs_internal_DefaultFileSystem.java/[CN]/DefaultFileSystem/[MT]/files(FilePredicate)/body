{
  doPreloadFiles();
  return Iterables.transform(inputFiles(predicate),new Function<InputFile,File>(){
    @Override public File apply(    @Nullable InputFile input){
      return input == null ? null : input.file();
    }
  }
);
}
