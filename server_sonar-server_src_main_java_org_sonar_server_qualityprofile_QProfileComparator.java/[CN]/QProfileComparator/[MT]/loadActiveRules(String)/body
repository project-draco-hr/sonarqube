{
  return Maps.uniqueIndex(profileLoader.findActiveRulesByProfile(profileKey),new NonNullInputFunction<ActiveRule,RuleKey>(){
    @Override protected RuleKey doApply(    ActiveRule input){
      return input.key().ruleKey();
    }
  }
);
}
