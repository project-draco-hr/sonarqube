{
  List<String> stmts=new CreateTableBuilder(H2,"SOmE_TABLe_NamE").addColumn(newBooleanColumnDefBuilder().setColumnName("bool_col").build()).build();
  assertThat(stmts).hasSize(1);
  assertThat(stmts.iterator().next()).startsWith("CREATE TABLE some_table_name (").endsWith(")");
}
