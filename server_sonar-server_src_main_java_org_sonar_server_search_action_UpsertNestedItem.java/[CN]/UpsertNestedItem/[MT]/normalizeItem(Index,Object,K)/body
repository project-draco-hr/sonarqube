{
  List<UpdateRequest> updates=index.getNormalizer().normalizeNested(item,key);
  for (  UpdateRequest update : updates) {
    update.index(index.getIndexName()).type(index.getIndexType()).refresh(needsRefresh());
  }
  return updates;
}
