{
  Dashboard dashboard=Dashboard.create().setLayout(DashboardLayout.TWO_COLUMNS_30_70).setDescription("Fake dashboard for integration tests");
  dashboard.addWidget("lcom4",1);
  dashboard.addWidget("description",1);
  dashboard.addWidget("documentation_comments",2);
  dashboard.addWidget("complexity",3);
  return dashboard;
}
