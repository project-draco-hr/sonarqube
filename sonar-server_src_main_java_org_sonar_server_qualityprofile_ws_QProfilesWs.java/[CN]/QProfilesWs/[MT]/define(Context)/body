{
  NewController controller=context.createController("api/qprofiles").setDescription("Quality profiles");
  controller.createAction("restore_default").setDescription("Restore default profiles").setSince("4.4").setHandler(qProfileBackupWsHandler).createParam("language","Restore default profiles for this language");
  controller.done();
}
