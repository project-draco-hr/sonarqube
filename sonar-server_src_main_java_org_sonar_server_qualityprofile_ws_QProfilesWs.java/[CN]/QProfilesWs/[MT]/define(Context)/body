{
  NewController controller=context.createController("api/qualityprofiles").setDescription("Quality profiles management");
  restoreDefaultAction.define(controller);
  activateRuleAction.define(controller);
  controller.done();
}
