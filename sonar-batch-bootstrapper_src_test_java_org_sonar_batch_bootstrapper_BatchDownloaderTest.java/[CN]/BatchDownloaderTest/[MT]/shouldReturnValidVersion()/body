{
  BatchDownloader bootstrapper=new BatchDownloader("http://test"){
    @Override String remoteContent(    String path) throws IOException {
      return "2.6";
    }
  }
;
  assertThat(bootstrapper.getServerVersion(),is("2.6"));
}
