{
  setupData("grantGroupRole");
  DefaultResourcePermissions permissions=new DefaultResourcePermissions(new Settings(),getMyBatis(),new RoleDao(getMyBatis()),new UserDao(getMyBatis()));
  permissions.grantGroupRole(project,"sonar-administrators","admin");
  permissions.grantGroupRole(project,"sonar-administrators","admin");
  checkTables("grantGroupRole",new String[]{"id"},"group_roles");
}
