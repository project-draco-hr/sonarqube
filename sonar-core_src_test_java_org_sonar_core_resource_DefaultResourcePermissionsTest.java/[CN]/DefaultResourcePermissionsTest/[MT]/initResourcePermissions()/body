{
  settings=new Settings();
  ComponentPermissionFacade permissionFacade=new ComponentPermissionFacade(getMyBatis(),new RoleDao(getMyBatis()),new UserDao(getMyBatis()),new PermissionDao(getMyBatis()));
  permissions=new DefaultResourcePermissions(settings,getMyBatis(),permissionFacade);
}
