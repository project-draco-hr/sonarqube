{
  return new StringBuilder().append(indexHealth.getDocumentCount()).append("/").append(formatDateTimeNullSafe(indexHealth.getLastSynchronization())).append("/").append(formatIndexHealthOptimisation(indexHealth)).toString();
}
