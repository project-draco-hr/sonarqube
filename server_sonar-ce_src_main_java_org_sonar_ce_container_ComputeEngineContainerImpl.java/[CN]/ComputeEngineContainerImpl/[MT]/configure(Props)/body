{
  this.componentContainer.add(props.rawProperties()).add(LEVEL_1_COMPONENTS).add(toArray(CorePropertyDefinitions.all())).add(toArray(CePropertyDefinitions.all())).add(LEVEL_2_COMPONENTS);
  configureFromModules();
  return this;
}
