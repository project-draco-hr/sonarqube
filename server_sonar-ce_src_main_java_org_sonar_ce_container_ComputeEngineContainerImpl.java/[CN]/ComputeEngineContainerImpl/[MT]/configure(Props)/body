{
  this.componentContainer.add(props.rawProperties()).add(LEVEL_1_COMPONENTS).add(toArray(CorePropertyDefinitions.all())).add(toArray(CePropertyDefinitions.all()));
  configureFromModules();
  return this;
}
