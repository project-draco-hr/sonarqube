{
  expectedException.expect(IllegalStateException.class);
  SearchProjectPermissionsData.newBuilder().rootComponents(Collections.<ComponentDto>emptyList()).userCountByProjectIdAndPermission(HashBasedTable.<Long,String,Integer>create()).build();
}
