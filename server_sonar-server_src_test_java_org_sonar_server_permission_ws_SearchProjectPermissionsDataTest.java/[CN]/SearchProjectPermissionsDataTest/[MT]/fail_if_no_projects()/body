{
  expectedException.expect(IllegalStateException.class);
  SearchProjectPermissionsData.newBuilder().groupCountByProjectIdAndPermission(HashBasedTable.<Long,String,Integer>create()).userCountByProjectIdAndPermission(HashBasedTable.<Long,String,Integer>create()).build();
}
