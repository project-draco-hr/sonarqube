{
  onChanged();
  ;
  if (moneys_ == null) {
    moneys_=com.google.protobuf.MapField.newMapField(MoneysDefaultEntryHolder.defaultEntry);
  }
  if (!moneys_.isMutable()) {
    moneys_=moneys_.copy();
  }
  return moneys_;
}
