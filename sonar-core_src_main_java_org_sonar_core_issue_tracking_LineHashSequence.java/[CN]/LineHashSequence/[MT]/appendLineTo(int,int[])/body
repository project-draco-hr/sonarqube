{
  int[] result;
  if (to == null) {
    result=new int[]{line};
  }
 else {
    result=new int[to.length + 1];
    System.arraycopy(to,0,result,0,to.length);
    result[result.length - 1]=line;
  }
  return result;
}
