{
  JsonWriter jsonWriter=response.newJsonWriter();
  jsonWriter.beginObject().prop(PROPERTY_STATE,statusToJson(databaseMigration.status())).prop(PROPERTY_MESSAGE,buildMessage(databaseMigration)).propDateTime(PROPERTY_STARTED_AT,databaseMigration.startedAt()).endObject();
  jsonWriter.close();
}
