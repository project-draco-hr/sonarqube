{
  Query query=session.createQuery("SELECT s FROM " + Snapshot.class.getSimpleName() + " s "+ "WHERE s.last=true AND s.resourceId=:resourceId");
  query.setParameter("resourceId",snapshot.getResourceId());
  return session.getSingleResult(query,null);
}
