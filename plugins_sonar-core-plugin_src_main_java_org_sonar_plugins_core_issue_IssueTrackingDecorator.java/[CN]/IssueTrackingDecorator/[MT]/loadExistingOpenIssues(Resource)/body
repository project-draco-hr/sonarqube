{
  IssueQuery query=IssueQuery.builder().components(Lists.newArrayList(resource.getEffectiveKey())).statuses(Lists.newArrayList(Issue.STATUS_OPEN,Issue.STATUS_REOPENED,Issue.STATUS_RESOLVED)).build();
  return issueDao.select(query);
}
