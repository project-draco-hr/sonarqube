{
  if (!exclusionFilters.accept(inputFile,fs)) {
    return false;
  }
  for (  InputFileFilter filter : filters) {
    if (!filter.accept(inputFile)) {
      return false;
    }
  }
  return true;
}
