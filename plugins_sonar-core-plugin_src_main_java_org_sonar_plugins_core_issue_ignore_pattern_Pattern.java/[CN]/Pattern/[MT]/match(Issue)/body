{
  boolean match=violation.componentKey() != null && matchRule(violation.ruleKey());
  if (checkLines && violation.line() != null) {
    match=match && matchLine(violation.line());
  }
  return match;
}
