{
  try {
    return (UpdateRequest)this.getClass().getMethod("normalize",object.getClass(),key.getClass()).invoke(this,object,key);
  }
 catch (  Exception e) {
    throw new IllegalStateException("Could not invoke Normalizer Method",e);
  }
}
