{
  return new AbstractSumChildrenDecorator(){
    @Override @DependedUpon public List<Metric> generatesMetrics(){
      return Arrays.asList(CoreMetrics.LINES);
    }
    @Override protected boolean shouldSaveZeroIfNoChildMeasures(){
      return zeroIfNoChildMeasures;
    }
  }
;
}
