{
  WsProjectResponse.Builder response=WsProjectResponse.newBuilder();
  setLastAnalysisDate(response,data);
  response.setTimestamp(data.timestamp());
  response.getMutableFileDataByModuleAndPatch().putAll(buildFileDataByModuleAndPatch(data));
  response.getMutableSettingsByModule().putAll(buildSettingsByModule(data));
  return response.build();
}
