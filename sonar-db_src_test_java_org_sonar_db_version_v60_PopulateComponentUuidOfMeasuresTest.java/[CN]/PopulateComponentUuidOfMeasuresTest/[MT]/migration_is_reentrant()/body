{
  String uuid1=insertSnapshot(40);
  String uuid2=insertSnapshot(50);
  insertMeasure(1,40);
  underTest.execute();
  verifyMeasure(1,40,uuid1);
  underTest.execute();
  verifyMeasure(1,40,uuid1);
}
