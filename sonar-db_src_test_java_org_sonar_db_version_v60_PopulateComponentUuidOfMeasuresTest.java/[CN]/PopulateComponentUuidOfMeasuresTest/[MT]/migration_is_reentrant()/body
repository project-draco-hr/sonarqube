{
  String uuid1=insertComponent(40);
  String uuid2=insertComponent(50);
  insertMeasure(1,40);
  underTest.execute();
  verifyMeasure(1,40,uuid1);
  underTest.execute();
  verifyMeasure(1,40,uuid1);
}
