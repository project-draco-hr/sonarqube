{
  return Lists.transform(getChildrenQualifiers(qualifier),new Function<String,ResourceType>(){
    @Override public ResourceType apply(    String s){
      return typeByQualifier.get(s);
    }
  }
);
}
