{
  Request request=new Request.Builder().get().url(url).build();
  try {
    return new OkHttpClient.Builder().connectTimeout(30,TimeUnit.SECONDS).readTimeout(30,TimeUnit.SECONDS).build().newCall(request).execute();
  }
 catch (  IOException e) {
    throw Throwables.propagate(e);
  }
}
