{
  checkState(pluginMetricKeys.contains(metric) || CORE_METRIC_KEYS.contains(metric),String.format("Metric '%s' cannot be used as an input metric as it's not a core metric and no plugin declare this metric",metric));
  return true;
}
