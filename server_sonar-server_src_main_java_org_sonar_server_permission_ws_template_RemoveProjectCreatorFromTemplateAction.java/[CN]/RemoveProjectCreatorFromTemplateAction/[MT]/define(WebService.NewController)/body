{
  WebService.NewAction action=context.createAction("remove_project_creator_to_template").setDescription("Remove a project creator from a permission template.<br>" + "Requires the 'Administer' permission.").setSince("6.0").setPost(true).setHandler(this);
  createTemplateParameters(action);
  createProjectPermissionParameter(action);
}
