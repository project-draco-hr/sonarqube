{
  if (!monitor.isShutdown()) {
    monitor.shutdownNow();
  }
  if (!watch.isCancelled()) {
    watch.cancel(true);
  }
  for (int i=processes.size() - 1; i >= 0; i--) {
    processes.get(i).terminate();
  }
  processes.clear();
  interrupt();
}
