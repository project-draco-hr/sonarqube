{
  if (!isLatestAnalysis(project.getKey(),analysisDate)) {
    throw new IllegalArgumentException("The value '" + settings.getString(CoreProperties.PROJECT_DATE_PROPERTY) + "' of the sonar.projectDate property can't be older than the date of last known quality snapshot "+ "on this project. This property must be used to replay the past in a chronological order.");
  }
}
