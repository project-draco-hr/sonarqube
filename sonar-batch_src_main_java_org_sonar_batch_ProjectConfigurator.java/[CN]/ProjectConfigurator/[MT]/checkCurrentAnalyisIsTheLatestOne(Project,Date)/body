{
  if (!isLatestAnalysis(project.getKey(),analysisDate)) {
    throw new IllegalArgumentException("The value '" + settings.getString(CoreProperties.PROJECT_DATE_PROPERTY) + "' of the sonar.projectDate property "+ "can't be older than the date of last known quality snapshot "+ "on this project. This property must only be used to rebuild the past in a chronological order.");
  }
}
