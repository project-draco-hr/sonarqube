{
  InputStream responseStream=ws.newRequest().setMediaType(MediaTypes.PROTOBUF).setParam(PARAM_LOGIN,login).setParam(PARAM_NAME,name).execute().getInputStream();
  try {
    return GenerateWsResponse.parseFrom(responseStream);
  }
 catch (  IOException e) {
    throw propagate(e);
  }
}
