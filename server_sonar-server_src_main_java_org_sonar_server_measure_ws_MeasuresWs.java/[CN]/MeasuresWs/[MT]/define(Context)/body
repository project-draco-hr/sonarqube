{
  NewController controller=context.createController(MeasuresWsParameters.CONTROLLER_MEASURES).setSince("5.4").setDescription("Measures search");
  for (  MeasuresWsAction action : actions) {
    action.define(controller);
  }
  controller.done();
}
