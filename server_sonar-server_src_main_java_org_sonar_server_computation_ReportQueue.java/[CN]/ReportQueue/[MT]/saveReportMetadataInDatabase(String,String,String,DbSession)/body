{
  AnalysisReportDto dto=new AnalysisReportDto().setProjectKey(projectKey).setProjectName(projectName).setStatus(PENDING).setUuid(uuid);
  dao().insert(session,dto);
  session.commit();
  return dto;
}
