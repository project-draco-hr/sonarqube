{
  when(qProfiles.searchProfiles()).thenReturn(newArrayList(new QProfile().setId(1).setName("Sonar Way with Findbugs").setLanguage("java").setParent("Sonar Way").setVersion(1).setUsed(false)));
  assertThat(service.searchProfiles()).hasSize(1);
}
