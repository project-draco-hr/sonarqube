{
  return Collections2.transform(ImmutableList.copyOf(issues),new Function<TrackedIssue,String>(){
    @Override public String apply(    @Nullable TrackedIssue issue){
      return issue.key();
    }
  }
);
}
