{
  db.prepareDbUnit(getClass(),"empty.xml");
  sut.insert(session,new SnapshotDto().setComponentId(1L).setLast(false),new SnapshotDto().setComponentId(2L).setLast(false));
  session.commit();
  assertThat(db.countRowsOfTable("snapshots")).isEqualTo(2);
}
