{
  dbClient.userDao().insert(session,new UserDto().setEmail("john@email.com").setLogin("john").setName("John").setScmAccounts(newArrayList("jn")).setActive(true)).setExternalIdentity("jo").setExternalIdentityProvider("sonarqube");
  session.commit();
  userIndexer.index();
}
