{
  return Iterables.transform(Iterables.filter(cache.all(),new ResolvedPredicate(false)),new Function<DefaultIssue,Issue>(){
    @Override public Issue apply(    DefaultIssue input){
      return new DefaultIssueWrapper(input);
    }
  }
);
}
