{
  tester.newRequest().setParam("issue","ABC").setParam("plan","P1").execute();
  verify(issueService).plan("ABC","P1");
  verify(responseWriter).write(eq("ABC"),any(Request.class),any(Response.class));
}
