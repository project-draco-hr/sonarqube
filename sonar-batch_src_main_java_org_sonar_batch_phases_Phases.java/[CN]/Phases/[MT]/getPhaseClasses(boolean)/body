{
  List<Class> classes=Lists.<Class>newArrayList(DecoratorsExecutor.class,MavenPhaseExecutor.class,MavenPluginsConfigurator.class,PostJobsExecutor.class,SensorsExecutor.class,InitializersExecutor.class);
  if (!dryRun) {
    classes.add(UpdateStatusJob.class);
  }
  return classes;
}
