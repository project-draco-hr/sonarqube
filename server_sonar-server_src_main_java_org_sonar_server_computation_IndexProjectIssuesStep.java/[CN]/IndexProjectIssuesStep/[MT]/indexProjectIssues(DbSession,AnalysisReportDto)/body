{
  dbClient.issueDao().synchronizeAfter(session,index.get(IssueIndex.class).getLastSynchronization(ImmutableMap.of(IssueNormalizer.IssueField.PROJECT.field(),report.getProject().uuid())),ImmutableMap.of(IssueNormalizer.IssueField.PROJECT.field(),report.getProject().uuid()));
  session.commit();
}
