{
  return ComponentTreeQuery.builder().setPage(1).setPageSize(500).setBaseSnapshot(baseSnapshot).setSortFields(singletonList("name")).setAsc(true).setQualifiers(newArrayList(Qualifiers.FILE,Qualifiers.MODULE,Qualifiers.DIRECTORY,Qualifiers.PROJECT));
}
