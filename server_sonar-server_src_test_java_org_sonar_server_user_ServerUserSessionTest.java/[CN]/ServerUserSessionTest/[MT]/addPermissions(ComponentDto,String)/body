{
  for (  String permission : permissions) {
    dbClient.roleDao().insertUserRole(dbSession,new UserPermissionDto().setPermission(permission).setComponentId(component == null ? null : component.getId()).setUserId(userDto.getId()));
  }
  dbSession.commit();
}
