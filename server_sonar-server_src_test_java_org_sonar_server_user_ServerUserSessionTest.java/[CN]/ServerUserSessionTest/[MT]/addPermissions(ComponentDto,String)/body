{
  for (  String permission : permissions) {
    dbClient.roleDao().insertUserRole(dbSession,new UserRoleDto().setRole(permission).setResourceId(component == null ? null : component.getId()).setUserId(userDto.getId()));
  }
  dbSession.commit();
}
