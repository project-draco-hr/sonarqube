{
  ProjectPropertyCondition condition=new ProjectPropertyCondition("foo"){
    @Override public boolean doVerify(    Review review,    WorkflowContext context){
      return false;
    }
  }
;
  assertThat(condition.getPropertyKey()).isEqualTo("foo");
}
