{
  thrown.expect(IllegalArgumentException.class);
  new ProjectPropertyCondition(""){
    @Override public boolean doVerify(    @Nullable Review review,    WorkflowContext context){
      return false;
    }
  }
;
}
