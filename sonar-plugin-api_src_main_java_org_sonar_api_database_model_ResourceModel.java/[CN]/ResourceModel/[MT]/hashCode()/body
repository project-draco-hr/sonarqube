{
  if (StringUtils.isNotBlank(path)) {
    return new HashCodeBuilder(17,37).append(path).append(enabled).append(rootId).toHashCode();
  }
  return new HashCodeBuilder(17,37).append(key).append(enabled).append(rootId).toHashCode();
}
