{
  List<DTO> results=new ArrayList<DTO>();
  for (  KEY key : keys) {
    DTO result=this.getNullableByKey(session,key);
    if (result != null) {
      results.add(result);
    }
  }
  return results;
}
