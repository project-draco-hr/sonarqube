{
  this.treeRootHolder=treeRootHolder;
  this.metricRepository=metricRepository;
  this.measureRepository=measureRepository;
  this.formulas=ImmutableList.<Formula>of(new SumFormula(COMMENTED_OUT_CODE_LINES_KEY),new DocumentationFormula(),new CommentDensityFormula());
}
