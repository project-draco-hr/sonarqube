{
  List<G> result=new ArrayList<G>(unsorted.size());
  result.addAll(unsorted);
  Collections.sort(result,new Comparator<G>(){
    @Override public int compare(    G o1,    G o2){
      return Long.valueOf(o2.totalTime()).compareTo(o1.totalTime());
    }
  }
);
  return result;
}
