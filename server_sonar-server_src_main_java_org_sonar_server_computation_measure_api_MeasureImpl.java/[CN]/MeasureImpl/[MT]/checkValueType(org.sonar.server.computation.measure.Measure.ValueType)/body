{
  checkState(measure.getValueType() == expected,String.format("Value can not be converted to %s because current value type is a %s",expected.toString().toLowerCase(Locale.US),measure.getValueType()));
}
