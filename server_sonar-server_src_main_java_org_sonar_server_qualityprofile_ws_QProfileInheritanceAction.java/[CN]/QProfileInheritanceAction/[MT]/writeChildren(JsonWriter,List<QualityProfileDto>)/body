{
  json.name("children").beginArray();
  for (  QualityProfileDto child : children) {
    json.beginObject().prop("key",child.getKey()).prop("name",child.getName()).endObject();
  }
  json.endArray();
}
