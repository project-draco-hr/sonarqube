{
  userSession.setGlobalPermissions(UserRole.ADMIN);
  TestResponse wsResponse=tester.newRequest().setMediaType(MimeTypes.JSON).execute();
  JsonAssert.assertJson(wsResponse.getInput()).isSimilarTo("{\"tasks\":[]}");
}
