{
  return new Settings(){
    @Override @CheckForNull public String getString(    String key){
      return getComponentSettings().getString(key);
    }
    @Override public String[] getStringArray(    String key){
      return getComponentSettings().getStringArray(key);
    }
  }
;
}
