{
  Map<String,Object> result=null;
  if (o != null) {
    if (o instanceof Map) {
      result=(Map)o;
    }
 else {
      throw new IllegalArgumentException("Unsupported type for map: " + o.getClass());
    }
  }
  return result;
}
