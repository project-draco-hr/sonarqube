{
  Settings settings=new Settings();
  settings.setProperty("sonar.dryRun",true);
  Project project=new Project("struts");
  ServerClient serverClient=mock(ServerClient.class);
  UpdateStatusJob job=new UpdateStatusJob(settings,serverClient,mock(DatabaseSession.class),mock(ResourcePersister.class),project,mock(Snapshot.class));
  job.evictDryRunDB();
  verify(serverClient,never()).request(anyString());
}
