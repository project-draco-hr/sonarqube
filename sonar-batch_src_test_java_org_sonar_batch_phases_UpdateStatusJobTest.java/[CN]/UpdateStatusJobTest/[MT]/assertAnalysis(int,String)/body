{
  setupData("sharedFixture",fixture);
  DatabaseSession session=getSession();
  UpdateStatusJob sensor=new UpdateStatusJob(new Settings().appendProperty(CoreProperties.SERVER_BASE_URL,"http://myserver/"),mock(ServerClient.class),session,new DefaultResourcePersister(session,mock(ResourcePermissions.class)),mock(Project.class),loadSnapshot(snapshotId));
  sensor.execute();
  checkTables(fixture,"snapshots");
}
