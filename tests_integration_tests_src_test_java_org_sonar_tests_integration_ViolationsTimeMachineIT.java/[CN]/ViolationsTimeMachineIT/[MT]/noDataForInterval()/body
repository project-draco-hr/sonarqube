{
  Date date=new Date();
  TimeMachine timemachine=sonar.find(TimeMachineQuery.createForMetrics(PROJECT,CoreMetrics.LINES_KEY).setFrom(date).setTo(date));
  assertThat(timemachine.getCells().length,is(0));
}
