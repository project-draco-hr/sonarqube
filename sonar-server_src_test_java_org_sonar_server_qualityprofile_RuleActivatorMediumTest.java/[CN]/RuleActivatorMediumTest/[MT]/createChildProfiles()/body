{
  db.qualityProfileDao().insert(dbSession,QualityProfileDto.createFor(XOO_CHILD_PROFILE_KEY).setParent(XOO_PROFILE_KEY.name()));
  db.qualityProfileDao().insert(dbSession,QualityProfileDto.createFor(XOO_GRAND_CHILD_PROFILE_KEY).setParent(XOO_CHILD_PROFILE_KEY.name()));
  dbSession.commit();
}
