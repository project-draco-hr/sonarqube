{
  BatchReport.Duplication.Builder builder=BatchReport.Duplication.newBuilder().setOriginPosition(original);
  for (  BatchReport.Duplicate duplicate : duplicates) {
    builder.addDuplicate(duplicate);
  }
  return builder.build();
}
