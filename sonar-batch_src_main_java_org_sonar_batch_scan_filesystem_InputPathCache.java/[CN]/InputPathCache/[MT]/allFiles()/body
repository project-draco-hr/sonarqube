{
  return Iterables.concat(Iterables.transform(inputFileCache.values(),new Function<Map<String,InputFile>,Collection<InputFile>>(){
    @Override public Collection<InputFile> apply(    Map<String,InputFile> input){
      return input.values();
    }
  }
));
}
