{
  return Iterables.concat(Iterables.transform(inputDirCache.values(),new Function<Map<String,InputDir>,Collection<InputDir>>(){
    @Override public Collection<InputDir> apply(    Map<String,InputDir> input){
      return input.values();
    }
  }
));
}
