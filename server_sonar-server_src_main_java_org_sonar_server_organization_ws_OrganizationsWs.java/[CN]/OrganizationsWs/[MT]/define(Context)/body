{
  NewController controller=context.createController("api/organizations").setSince("6.2").setDescription("Manage organizations.");
  for (  OrganizationsAction action : actions) {
    action.define(controller);
  }
  controller.done();
}
