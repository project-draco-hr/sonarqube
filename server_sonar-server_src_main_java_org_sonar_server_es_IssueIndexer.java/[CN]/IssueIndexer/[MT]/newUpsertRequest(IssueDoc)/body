{
  String projectUuid=issue.projectUuid();
  issue.setField("_parent",projectUuid);
  return new UpdateRequest(IssueIndexDefinition.INDEX,IssueIndexDefinition.TYPE_ISSUE,issue.key()).routing(projectUuid).parent(projectUuid).doc(issue.getFields()).upsert(issue.getFields());
}
