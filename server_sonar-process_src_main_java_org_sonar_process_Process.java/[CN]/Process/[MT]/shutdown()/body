{
  LOGGER.trace("Process[{}]::shutdown() START",name);
  this.onTerminate();
  if (pingTask != null) {
    pingTask.cancel(true);
  }
  monitor.shutdownNow();
  LOGGER.trace("Process[{}]::shutdown() END",name);
}
