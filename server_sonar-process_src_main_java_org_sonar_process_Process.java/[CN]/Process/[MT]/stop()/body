{
  LOGGER.info("Process[{}]::shutdown START",name);
  if (pingTask != null) {
    pingTask.cancel(true);
  }
  monitor.shutdownNow();
  this.onStop();
  LOGGER.info("Process[{}]::shutdown END",name);
}
