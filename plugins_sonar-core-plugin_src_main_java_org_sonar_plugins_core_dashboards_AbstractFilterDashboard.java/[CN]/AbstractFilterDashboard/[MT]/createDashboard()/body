{
  Dashboard dashboard=Dashboard.create().setGlobal(true).setLayout(DashboardLayout.ONE_COLUMN);
  dashboard.addWidget("filter",1).setProperty(FilterWidget.FILTER,getFilterKey());
  doCompleteDashboard(dashboard);
  return dashboard;
}
