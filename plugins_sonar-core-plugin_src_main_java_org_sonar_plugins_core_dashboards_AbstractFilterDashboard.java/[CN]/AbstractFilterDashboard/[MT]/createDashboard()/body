{
  Dashboard dashboard=Dashboard.create().setGlobal(true).setLayout(DashboardLayout.ONE_COLUMN);
  Widget filterWidget=dashboard.addWidget("filter",1).setProperty(FilterWidget.FILTER,getFilterKey());
  return dashboard;
}
