{
  List<BatchReport.Symbol> symbols=newArrayList(BatchReport.Symbol.newBuilder().setDeclaration(BatchReport.TextRange.newBuilder().setStartLine(2).setEndLine(2).setStartOffset(3).setEndOffset(4).build()).addReference(BatchReport.TextRange.newBuilder().setStartLine(1).setEndLine(1).setStartOffset(1).setEndOffset(2).build()).build());
  SymbolsLineReader symbolsLineReader=new SymbolsLineReader(symbols.iterator());
  symbolsLineReader.read(line1);
  symbolsLineReader.read(line2);
  assertThat(line1.getSymbols()).isEqualTo("1,2,1");
  assertThat(line2.getSymbols()).isEqualTo("3,4,1");
}
