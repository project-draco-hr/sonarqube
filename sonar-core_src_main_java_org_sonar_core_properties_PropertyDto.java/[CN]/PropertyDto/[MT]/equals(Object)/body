{
  if (obj == null) {
    return false;
  }
  if (getClass() != obj.getClass()) {
    return false;
  }
  final PropertyDto other=(PropertyDto)obj;
  return Objects.equal(this.key,other.key) && Objects.equal(this.value,other.value) && Objects.equal(this.userId,other.userId)&& Objects.equal(this.resourceId,other.resourceId);
}
