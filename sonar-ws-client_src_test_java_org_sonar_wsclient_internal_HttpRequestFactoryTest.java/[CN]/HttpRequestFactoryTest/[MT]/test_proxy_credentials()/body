{
  HttpRequestFactory factory=new HttpRequestFactory(httpServer.url()).setProxyHost("localhost").setProxyPort(5020).setProxyLogin("john").setProxyPassword("smith");
  HttpRequest request=factory.get("/api/issues",Collections.<String,Object>emptyMap());
}
