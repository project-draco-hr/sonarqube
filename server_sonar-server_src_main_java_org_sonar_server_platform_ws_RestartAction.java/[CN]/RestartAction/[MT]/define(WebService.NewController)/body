{
  controller.createAction("restart").setDescription("Restart server. Available only on development mode (sonar.web.dev=true). " + "Ruby on Rails extensions are not reloaded.").setSince("4.3").setPost(true).setHandler(this);
}
