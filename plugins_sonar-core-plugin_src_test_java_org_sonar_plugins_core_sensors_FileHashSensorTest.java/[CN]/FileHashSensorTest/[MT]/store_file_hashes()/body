{
  when(fileCache.byModule("struts")).thenReturn(Lists.<InputFile>newArrayList(new DefaultInputFile("src/Foo.java").setFile(temp.newFile()).setHash("ABC"),new DefaultInputFile("src/Bar.java").setFile(temp.newFile()).setHash("DEF")));
  SensorContext sensorContext=mock(SensorContext.class);
  sensor.analyse(project,sensorContext);
  verify(componentDataCache).setStringData("struts",SnapshotDataTypes.FILE_HASHES,"src/Foo.java=ABC;src/Bar.java=DEF");
  verifyZeroInteractions(sensorContext);
}
