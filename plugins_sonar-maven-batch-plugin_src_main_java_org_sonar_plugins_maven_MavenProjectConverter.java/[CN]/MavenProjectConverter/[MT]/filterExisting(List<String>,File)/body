{
  return Lists.newArrayList(Collections2.filter(filePaths,new Predicate<String>(){
    @Override public boolean apply(    String filePath){
      File file=resolvePath(filePath,baseDir);
      return file != null && file.exists();
    }
  }
));
}
