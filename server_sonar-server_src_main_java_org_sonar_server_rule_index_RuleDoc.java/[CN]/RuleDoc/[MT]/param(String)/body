{
  return Iterables.find(params(),new Predicate<RuleParam>(){
    @Override public boolean apply(    @Nullable RuleParam input){
      return input != null && input.key().equals(key);
    }
  }
,null);
}
