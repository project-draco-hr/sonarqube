{
  String result=ws.newGetRequest(MetricsWs.ENDPOINT,"types").execute().outputAsString();
  assertThat(result).contains(ValueType.INT.name(),ValueType.INT.description(),ValueType.PERCENT.name(),ValueType.PERCENT.description());
}
