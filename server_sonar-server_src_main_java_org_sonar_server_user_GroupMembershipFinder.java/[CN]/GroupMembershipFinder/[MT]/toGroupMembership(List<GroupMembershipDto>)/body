{
  List<GroupMembership> groups=newArrayList();
  for (  GroupMembershipDto dto : dtos) {
    groups.add(new GroupMembership().setId(dto.getId()).setName(dto.getName()).setDescription(dto.getDescription()).setMember(dto.getUserId() != null));
  }
  return groups;
}
