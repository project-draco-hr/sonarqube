{
  userSessionRule.login("obiwan").setUserId(userId.intValue());
  dbClient.userDao().insert(session,new UserDto().setActive(true).setId(userId).setLogin("obiwan"));
  createProfiles();
  session.commit();
}
