{
  StringBuilder sb=new StringBuilder(256);
  sb.append("<tests-details>");
  for (  UnitTestResult result : results) {
    result.appendXml(sb);
  }
  sb.append("</tests-details>");
  return sb.toString();
}
