{
  return Collections2.transform(activeRules.findByRepository(query.getRepositoryKey()),new Function<ActiveRule,Rule>(){
    @Override public Rule apply(    @Nonnull ActiveRule input){
      return toRule(input);
    }
  }
);
}
