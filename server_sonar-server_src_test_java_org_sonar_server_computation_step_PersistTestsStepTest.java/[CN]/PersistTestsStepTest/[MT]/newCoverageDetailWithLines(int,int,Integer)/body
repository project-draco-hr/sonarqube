{
  return CoverageDetail.newBuilder().setTestName("name#" + id).addCoveredFile(CoverageDetail.CoveredFile.newBuilder().addAllCoveredLine(Arrays.asList(lines)).setFileRef(covered_file_ref).build()).build();
}
