{
  return CoverageDetail.newBuilder().setTestName("name#" + id).addCoveredFile(CoverageDetail.CoveredFile.newBuilder().addAllCoveredLine(Arrays.asList(1,2,3)).setFileRef(covered_file_ref).build()).build();
}
