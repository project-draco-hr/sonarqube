{
  sut=serverTester.get(SystemInfoWsAction.class);
  MockUserSession.set().setLogin("login").setName("name").setGlobalPermissions(GlobalPermissions.SCAN_EXECUTION);
  sut.handle(mock(Request.class),mock(Response.class));
}
