{
  modCount++;
  Object oldValue=null;
  Entry e=(Entry)entries.get(key);
  if (e != null) {
    removeEntry(e);
    oldValue=e.setValue(value);
  }
 else {
    e=new Entry(key,value);
    entries.put(key,e);
  }
  insertEntry(e);
  return oldValue;
}
