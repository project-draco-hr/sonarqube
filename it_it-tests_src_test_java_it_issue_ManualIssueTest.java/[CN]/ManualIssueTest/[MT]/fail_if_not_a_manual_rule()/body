{
  try {
    adminIssueClient().create(NewIssue.create().component(COMPONENT_KEY).rule("xoo:OneIssuePerLine").line(3).severity("CRITICAL").message("The name 'Sample' is too generic"));
    fail();
  }
 catch (  Exception e) {
    verifyHttpException(e,400);
  }
}
