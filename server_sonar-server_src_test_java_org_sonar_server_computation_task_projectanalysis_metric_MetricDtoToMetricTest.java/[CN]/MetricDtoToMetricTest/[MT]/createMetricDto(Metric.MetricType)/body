{
  return new MetricDto().setId(metricType.name().hashCode()).setKey(metricType.name() + "_key").setShortName(metricType.name() + "_name").setValueType(metricType.name()).setBestValue(SOME_BEST_VALUE).setEnabled(true);
}
