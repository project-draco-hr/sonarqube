{
  json.beginObject().prop("id",filter.getId()).prop("name",filter.getName()).prop("description",filter.getDescription()).prop("user",filter.getUserLogin()).prop("shared",filter.isShared()).prop("query",filter.getData()).prop("canModify",canModifyFilter(userSession,filter)).endObject();
}
