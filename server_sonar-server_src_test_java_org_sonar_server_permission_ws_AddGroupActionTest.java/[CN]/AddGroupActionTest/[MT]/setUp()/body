{
  permissionUpdater=mock(PermissionUpdater.class);
  permissionChangeCaptor=ArgumentCaptor.forClass(PermissionChange.class);
  dbClient=db.getDbClient();
  ws=new WsTester(new PermissionsWs(new AddGroupAction(dbClient,new PermissionWsCommons(dbClient),permissionUpdater,new ComponentFinder(dbClient))));
  userSession.login("admin").setGlobalPermissions(SYSTEM_ADMIN);
}
