{
  expectedException.expect(BadRequestException.class);
  insertGroup("sonar-administrators");
  commit();
  newRequest().setParam(PARAM_GROUP_NAME,"sonar-administrators").setParam(PARAM_PERMISSION,UserRole.ISSUE_ADMIN).execute();
}
