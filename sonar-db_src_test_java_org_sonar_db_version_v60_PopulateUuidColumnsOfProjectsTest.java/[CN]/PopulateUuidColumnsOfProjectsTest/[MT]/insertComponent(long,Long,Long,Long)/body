{
  String uuid="uuid_" + id;
  db.executeInsert("projects","ID",valueOf(id),"UUID",uuid,"ROOT_ID",toString(rootId),"COPY_RESOURCE_ID",toString(copyResourceId),"PERSON_ID",toString(personId));
  return uuid;
}
