{
  NewController controller=context.createController("api/plugins");
  controller.setDescription("Plugin management").setSince("5.2");
  for (  PluginsWsAction action : actions) {
    action.define(controller);
  }
  controller.done();
}
