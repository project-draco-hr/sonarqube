{
  panel=new VerticalPanel();
  loading=new Loading();
  panel.add(loading);
  initWidget(panel);
  setStyleName("gwt-TestDetailsPanel");
  getElement().setId("gwt-TestDetailsPanel");
  loadData(resource);
}
