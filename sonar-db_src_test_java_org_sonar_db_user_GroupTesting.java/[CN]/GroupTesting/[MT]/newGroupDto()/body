{
  GroupDto group=new GroupDto().setName(randomAlphanumeric(255)).setDescription(randomAlphanumeric(200));
  group.setCreatedAt(new Date(nextLong()));
  group.setUpdatedAt(new Date(nextLong()));
  return group;
}
