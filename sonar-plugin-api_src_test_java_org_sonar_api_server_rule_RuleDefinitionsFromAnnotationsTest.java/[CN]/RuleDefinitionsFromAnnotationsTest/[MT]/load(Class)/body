{
  RuleDefinitions.Context context=new RuleDefinitions.Context();
  RuleDefinitions.NewExtendedRepository newRepository=context.createRepository("squid","java").loadAnnotatedClasses(annotatedClass);
  newRepository.done();
  return context.repository("squid");
}
