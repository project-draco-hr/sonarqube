{
  connector.start();
  assertEquals(1,emfCreationCounter);
  connector.start();
  assertEquals(1,emfCreationCounter);
  connector.stop();
  connector.start();
  assertEquals(2,emfCreationCounter);
}
