{
  checkState();
  if (!loaded) {
    scm=loadScmFromReport();
    loaded=scm != null;
  }
  if (!loaded) {
    scm=loadLinesFromIndexAndBuildScm();
    loaded=true;
  }
  computeLastCommitDateAndAuthor();
}
