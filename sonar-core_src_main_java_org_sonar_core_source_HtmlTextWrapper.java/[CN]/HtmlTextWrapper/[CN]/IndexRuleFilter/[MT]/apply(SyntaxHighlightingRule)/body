{
  if (syntaxHighlightingRule != null) {
    return (characterIndex == syntaxHighlightingRule.getStartPosition() && isNewCharRange) || (characterIndex == syntaxHighlightingRule.getEndPosition() && !isNewCharRange);
  }
  return false;
}
