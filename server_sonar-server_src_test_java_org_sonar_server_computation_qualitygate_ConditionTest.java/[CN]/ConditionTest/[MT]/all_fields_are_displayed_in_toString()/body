{
  when(SOME_METRIC.toString()).thenReturn("metric1");
  assertThat(new Condition(SOME_METRIC,1,SOME_OPERATOR,"error_l","warn").toString()).isEqualTo("Condition{metric=metric1, period=1, operator=ope, warningThreshold=warn, errorThreshold=error_l}");
}
