{
  NewAction action=context.createAction("users").setDescription("Search for users with membership information with respect to a group.").setHandler(this).setSince("5.2").setResponseExample(getClass().getResource("example-users.json")).addSelectionModeParam().addSearchQuery("freddy","names","logins").addPagingParams(25);
  createGroupParameters(action);
}
