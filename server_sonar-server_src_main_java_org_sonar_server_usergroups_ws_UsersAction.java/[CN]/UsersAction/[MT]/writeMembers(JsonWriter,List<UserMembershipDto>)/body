{
  json.name("users").beginArray();
  for (  UserMembershipDto user : users) {
    json.beginObject().prop("login",user.getLogin()).prop("name",user.getName()).prop("selected",user.getGroupId() != null).endObject();
  }
  json.endArray();
}
