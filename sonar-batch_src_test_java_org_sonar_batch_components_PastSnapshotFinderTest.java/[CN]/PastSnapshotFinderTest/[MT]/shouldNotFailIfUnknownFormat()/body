{
  when(finderByPreviousAnalysis.findByPreviousAnalysis(null)).thenReturn(new PastSnapshot(CoreProperties.TIMEMACHINE_MODE_PREVIOUS_ANALYSIS,new Snapshot()));
  assertNull(finder.find(null,2,"foooo"));
}
