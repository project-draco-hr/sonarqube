{
  json.name("rules").beginArray();
  for (  Rule rule : rules) {
    json.beginObject().prop("key",rule.key().toString()).prop("name",rule.name()).prop("desc",rule.htmlDescription()).prop("status",rule.status().toString()).endObject();
  }
  json.endArray();
}
