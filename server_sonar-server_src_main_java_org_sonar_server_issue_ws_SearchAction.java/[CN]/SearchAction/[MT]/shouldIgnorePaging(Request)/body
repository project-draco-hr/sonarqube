{
  List<String> componentUuids=request.paramAsStrings(IssueFilterParameters.COMPONENT_UUIDS);
  return componentUuids != null && componentUuids.size() == 1 && BooleanUtils.isTrue(request.paramAsBoolean(IssueFilterParameters.IGNORE_PAGING));
}
