{
  Notification notification=new Notification("review-assignee-changed").setFieldValue("author","simon").setFieldValue("old.assignee","simon").setFieldValue("assignee","godin");
  dispatcher.dispatch(notification,context);
  verify(context).addUser("godin");
  verifyNoMoreInteractions(context);
}
