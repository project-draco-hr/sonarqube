{
  List<Issue> issues=createIssues();
  assertThat(decorator.countIssues(null,fiveDaysAgo)).isEqualTo(0);
  assertThat(decorator.countIssues(issues,fiveDaysAgo)).isEqualTo(1);
  assertThat(decorator.countIssues(issues,tenDaysAgo)).isEqualTo(3);
}
