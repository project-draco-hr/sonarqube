{
  if (!def.getKey().matches(VALID_MODULE_KEY_REGEXP) || NumberUtils.isDigits(def.getKey())) {
    validationMessages.add(String.format("%s is not a valid project or module key",def.getKey()));
  }
}
