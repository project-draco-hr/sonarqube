{
  return dataproviderFromComponentTypeValues(new Predicate<Component.Type>(){
    @Override public boolean apply(    Component.Type input){
      return input.isReportType() && input != Component.Type.PROJECT;
    }
  }
);
}
