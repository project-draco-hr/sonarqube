{
  return FluentIterable.from(asList(Component.Type.values())).filter(predicate).transform(new Function<Object,Object[]>(){
    @Nullable @Override public Object[] apply(    Object input){
      return new Object[]{input};
    }
  }
).toArray(Object[].class);
}
