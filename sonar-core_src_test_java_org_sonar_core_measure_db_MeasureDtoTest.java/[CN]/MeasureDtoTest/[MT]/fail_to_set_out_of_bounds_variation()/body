{
  try {
    MeasureDto.createFor(MeasureKey.of("component","metric")).setVariation(6,1d);
    fail();
  }
 catch (  Exception e) {
    assertThat(e).isInstanceOf(IndexOutOfBoundsException.class);
  }
}
