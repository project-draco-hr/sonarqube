{
  assertThat(MeasureDto.createFor(MeasureKey.of("component","metric")).setTextValue("text value").setData(null).getData()).isEqualTo("text value");
  assertThat(MeasureDto.createFor(MeasureKey.of("component","metric")).setTextValue(null).setData(new byte[]{}).getData()).isNotNull();
}
